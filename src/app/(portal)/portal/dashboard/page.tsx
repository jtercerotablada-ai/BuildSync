// One Home grid. This used to be a forked copy of /home that had drifted:
// it still hid the card frame and title of six widgets that no longer draw
// their own, and gave them no resize option. Like every other /portal page,
// it now renders the dashboard page itself.
export { default } from "@/app/(dashboard)/home/page";

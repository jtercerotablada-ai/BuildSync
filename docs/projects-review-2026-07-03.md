# Revisión profunda del módulo Projects (10 pestañas) — 3 Jul 2026

Revisión multi-agente (132 agentes: 13 buscadores por área + 119 verificadores adversariales) sobre el código en producción (commit 32e5680).
Áreas: shell de la página, Overview, List, Board, Timeline, Dashboard, Calendar, Workflow, Forms, Messages, Files, Team, y permisos transversales.
Resultado: 117 hallazgos confirmados (2 críticos, 26 altos, 48 medios, 34 bajos, 7 pulido), 2 refutados por los verificadores.
Nota: cuando el verificador corrigió la severidad, aquí se usa la severidad corregida.

---

# HALLAZGOS CONFIRMADOS (verificados adversarialmente)

### [critical] Any workspace member can read, edit, and DELETE tasks of projects the project page 404s them on
FILE: src/app/api/tasks/[taskId]/route.ts:543 (area cross-permissions)
SUMMARY: All task endpoints gate only on workspace membership, never on project-level access. GET/PATCH/DELETE /api/tasks/[taskId] call verifyTaskAccess (src/app/api/tasks/[taskId]/route.ts:55, :237, :543), which resolves to verifyWorkspaceAccess(task.project.workspaceId) in src/lib/auth-guards.ts:143-177 — a bare workspaceMember lookup with no check of project ownership, ProjectMember rows, or visibility. GET /api/tasks?projectId=X (src/app/api/tasks/route.ts:48-67) only scopes `project: { workspaceId }`, returning every task of any project in the workspace including customFieldValues, tags, dependencies. POST /api/tasks (:215) and POST /api/sections (src/app/api/sections/route.ts:25) use verifyProjectAccess (auth-guards.ts:182-199) which is also just workspace membership. The canonical policy (page.tsx:136-159 and the /api/projects GET comment at src/app/api/projects/route.ts:90-97) says L1-L3 …
SCENARIO: An invited L1-L3 engineer in the firm workspace who is NOT a member of confidential project P (page shows 404, project list hides it) sends GET /api/tasks?projectId=P and receives the full task list with custom-field values; PATCH /api/tasks/<id> with {"name":"x"} rewrites a task, and DELETE /api/tasks/<id> permanently deletes it (200 { success }) — destructive writes across a permission boundary the rest of the app enforces.
FIX: In verifyTaskAccess/verifyProjectAccess (or a new shared helper), after loading the project apply the same clause as page.tsx: allow if owner, ProjectMember, visibility PUBLIC, or workspace role OWNER/ADMIN or getLevel(position)>=4; otherwise throw AuthorizationError. For GET /api/tasks with projectId, run that check against the specific project instead of relying on workspace scoping.
VERIFIER: CONFIRMED — full chain reproduced from source. SEVERITY: critical. verifyTaskAccess (src/lib/auth-guards.ts:143-177) is the only gate on GET (src/app/api/tasks/[taskId]/route.ts:55), PATCH (:237), and DELETE (:543). For a task that has a project it does exactly one authz check: `await verifyWorkspaceAccess(userId, task.project.workspaceId)` (auth-guards.ts:175). It even selects project.ownerId and project.visibility (auth-guards.ts:155-156) but never reads them. verifyWorkspaceAccess (auth-guards.ts:7-15) is a bare `prisma.workspaceMember.findUnique` — no ProjectMember, ownerId, or visibility check. DELETE then runs an unconditional `prisma.task.delete({ where: { id: taskId } })` and returns…

### [critical] DueDatePicker round-trips UTC-midnight dates through local time, silently shifting due/start dates one day earlier on every edit
FILE: src/components/views/list-view.tsx:1530 (area list)
SUMMARY: Dates are stored as UTC midnight and serialized as full ISO strings (page.tsx:180-181 `t.dueDate?.toISOString()` -> "2026-07-03T00:00:00.000Z"). list-view.tsx:1528-1529 feeds the picker `parseISO(task.dueDate)`, which in any US timezone is a local Date of Jul 2 (~19:00-20:00). due-date-picker.tsx keeps that shifted Date as its draft (lines 111-119) and its flush() (lines 186-198) calls onChange(localStart, localDue) with BOTH endpoints whenever EITHER changed. list-view.tsx:1530-1534 then formats both with local getters `format(date, "yyyy-MM-dd")` -> "2026-07-02" and PATCHes /api/tasks/:id (route.ts:334 `new Date("2026-07-02")`). So editing only the start date rewrites the untouched due date one day earlier; the write is silent and repeats on every subsequent edit (Jul 3 -> Jul 2 -> Jul 1 ...).
SCENARIO: US user (Eastern time) has a task due Jul 3. They open the date picker on the List tab and add a start date of Jul 1, click Done. The due date in the database is now Jul 2 - one day earlier than what they set - without them touching the due field. Editing the range again shifts it another day back.
FIX: Treat UTC-midnight date-only values as calendar dates: derive the picker's Date from the ISO string's UTC components (e.g. new Date(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()) or slice the "yyyy-MM-dd" prefix of the ISO string) before passing to DueDatePicker, and only send fields whose value actually changed.
VERIFIER: CONFIRMED end-to-end. Chain: (1) Server page serializes Prisma DateTime as full UTC ISO string — src/app/(dashboard)/projects/[projectId]/page.tsx:180-181 `dueDate: t.dueDate?.toISOString() || null`. (2) list-view.tsx:1528-1529 passes `parseISO(task.dueDate)` / `parseISO(task.startDate)` to DueDatePicker; for a stored UTC-midnight value ("2026-07-03T00:00:00.000Z") parseISO yields an instant that is Jul 2 ~19:00-20:00 local in any UTC-negative timezone. (3) src/components/tasks/due-date-picker.tsx:111-112 seeds draft state from those shifted Dates (re-synced on open at 132-144), and flush() at 186-198 explicitly calls `onChange(ls, ld)` with BOTH endpoints when EITHER changed (`if (startChan…

### [high] Cross-column drop persists the card at the column-entry index, not where the user dropped it
FILE: src/components/views/board-view.tsx:259 (area board)
SUMMARY: During a cross-column drag, handleDragOver (board-view.tsx:175-213) inserts the task into the destination section once, at the index of the first card the pointer crosses; every subsequent dragOver inside that column hits the `activeSection.id === overSection.id` early-return (line 189) and never repositions it. On drop, handleDragEnd only runs the final arrayMove when `destSectionId === originalSourceId` (line 259) — originalSourceId is the section recorded at drag START (dragSourceSectionRef, line 151) — so for any cross-column drag the arrayMove is skipped and `orderedIds` (line 279) is taken from the stale entry-point order, which POST /api/tasks/reorder then persists as positions 0..n-1 (reorder/route.ts:73-80). dnd-kit's SortableContext meanwhile shows a live transform preview that follows the pointer, so the on-screen preview and the persisted order disagree. The standard dnd-kit …
SCENARIO: Drag a card from column A into the top of column B (it visually inserts above card X), then keep dragging down and drop it at the bottom of column B — the preview shows it last. On release + router.refresh, the card jumps back up to the position where it first entered the column. Any cross-column drop that involves vertical movement after entering the column lands at the wrong index.
FIX: In handleDragEnd, detect same-container by the active task's current section in localSections (after the dragOver move) instead of originalSourceId, and always run the arrayMove(oldIndex, newIndex) against the destination section before building orderedIds. Alternatively, let handleDragOver also reindex within the destination column (remove the same-section early return for the active task once it has crossed).
VERIFIER: CONFIRMED — full chain reproduced from source. 1. Drag start (board-view.tsx:143-157): `dragSourceSectionRef.current = section.id` (line 151) records the ORIGINAL source column A. 2. handleDragOver (board-view.tsx:175-213): on first pointer entry into column B, the task is removed from A and spliced into B at `overTaskIndex` — the index of the first card the pointer crosses (lines 194-209). After that state update, the active task lives in B, so every subsequent dragOver inside B computes `activeSection = B`, `overSection = B` and hits the early return `if (!overSection || activeSection.id === overSection.id) return prev;` (line 189). The task's index in localSections is frozen at the entry …

### [high] Dragging while a filter/search/sort is active corrupts the column's saved order (positions renumbered from the filtered subset)
FILE: src/components/views/board-view.tsx:279 (area board)
SUMMARY: BoardView receives `filteredSections` from the tab shell (project-content.tsx:941-947, built at 244-321 from search, incomplete/completed/due-this-week/assigned-to-me filters, show-completed, and sort). handleDragEnd builds `orderedIds` from that filtered list (board-view.tsx:279) and POST /api/tasks/reorder rewrites position = array index for ONLY those tasks (reorder/route.ts:73-80). Hidden tasks keep their old positions, producing duplicate/interleaved position values; the project page orders tasks by `position: asc` with no tiebreaker (page.tsx:63), so the column order becomes nondeterministic and the hidden tasks shuffle relative to the visible ones. Additionally, when a Sort (e.g. due date) is active the board displays sorted order, so any single drag persists the entire sorted order as the new canonical manual positions, silently destroying the user's hand-made order.
SCENARIO: Turn on the 'Incomplete tasks' filter (or type in the search box) on the Board tab, drag one card up within its column. The server renumbers only the visible cards to 0..k-1 while the hidden completed cards keep positions overlapping that range. Clear the filter: the column's task order is scrambled and differs between reloads (duplicate positions, no tiebreaker). With Sort=due date active, one drag permanently rewrites every task's position to the sorted order.
FIX: Either disable drag-and-drop (or persist-on-drop) while any filter/search/sort is active, or compute orderedTaskIds server-side by merging: fetch the full section task list, remove the dragged task, and re-insert it relative to its visible neighbors so hidden tasks keep coherent positions. Also add a createdAt tiebreaker to the page.tsx section-tasks orderBy.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) src/components/projects/project-content.tsx:244-321 builds `filteredSections` by filtering each section's tasks (search 251-259, incomplete/completed/due_this_week/assigned_to_me 262-282, showCompleted 285-290) and optionally re-sorting them (sortBy 293-318: due_date/alphabetical/priority/created). (2) Line 943 passes it straight to `<BoardView sections={filteredSections}>` — BoardViewProps (board-view.tsx:86-91) has no drag-disable or "filters active" prop; DnD is always live. (3) board-view.tsx:121 seeds `localSections` from that filtered prop; handleDragEnd at 277-279 does `orderedIds = destSection.tasks.map(t => t.id)` — i.e. only the VI…

### [high] Delete-section confirmation shows the filtered task count, understating (or hiding) how many tasks will be permanently deleted
FILE: src/components/views/board-view.tsx:650 (area board)
SUMMARY: BoardColumn.handleDelete builds its confirm message from `section.tasks.length` (board-view.tsx:650-654), but `section` comes from localSections which mirror `filteredSections` (project-content.tsx:244-321, 943) — the list after search/filters/show-completed pruning. The server, however, hard-deletes ALL tasks in the section: `prisma.task.deleteMany({ where: { sectionId } })` plus the section (sections/[sectionId]/route.ts:97-100), and subtasks cascade via parentTask onDelete: Cascade (schema.prisma:684). There is no undo.
SCENARIO: User enables the 'Incomplete tasks' filter; a 'Done' column shows 0 cards. They open the column menu → Delete section, and the confirm reads just 'Delete "Done"?' with no task count — implying it's empty. Confirming permanently deletes the section's 30 completed tasks and all their subtasks, comments, and attachments.
FIX: Base the confirmation count on the unfiltered section (pass the raw project.sections count down, or fetch the true count via the API before confirming), e.g. use the section's real _count.tasks; ideally also mention that completed/hidden tasks are included.
VERIFIER: CONFIRMED — full chain reproduced: 1. Filtered data source: src/components/projects/project-content.tsx:244-321 builds `filteredSections` by pruning each section's `tasks` array via search (l.251-259), activeFilters — "incomplete" removes completed tasks (l.266), "completed", "due_this_week", "assigned_to_me" — and the showCompleted toggle (l.285-290). Line 943 passes `filteredSections` as the `sections` prop to `<BoardView>`. 2. Client mirror: src/components/views/board-view.tsx:121 `localSections` is initialized from that prop and re-synced on prop change (l.131-134); l.552-553 renders `<BoardColumn section={section}>` mapping over `localSections`. So `section.tasks` inside BoardColumn is …

### [high] Calendar mutations never refresh the UI: dragged tasks snap back and quick-added tasks never appear
FILE: src/components/projects/project-content.tsx:957 (area calendar)
SUMMARY: CalendarView renders purely from the `sections` prop (server-fetched, no client task state) and after a successful mutation calls only `onTaskMutated?.()` (src/components/views/calendar-view.tsx:214 after the drag PATCH, :247 after the quick-add POST). The component's own doc comment (calendar-view.tsx:66-69) claims the prop 'Defaults to a router.refresh() in the parent', but project-content.tsx:957-961 mounts <CalendarView sections={filteredSections} onTaskClick={...} projectId={...}/> WITHOUT onTaskMutated, and there is no default. There is no optimistic update either — the bar position comes from segmentsByWeek which is derived from the unchanged props (calendar-view.tsx:296-389).
SCENARIO: User drags a task bar to another day: the PATCH to /api/tasks/:id succeeds, but the bar immediately renders back on its original day because props never change — the drag looks broken and the user retries or gives up, not knowing the DB date DID change. User clicks an empty cell, types a name, presses Enter: a success toast shows ('Created "X" for ...') but no bar ever appears on the calendar until a full page reload.
FIX: Pass onTaskMutated={() => router.refresh()} from ProjectContent (it already has `router` from useRouter), or make CalendarView keep local task state and update it optimistically from the PATCH/POST responses.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) CalendarView renders purely from props: src/components/views/calendar-view.tsx:90-93 flattens the `sections` prop into `tasks` via useMemo; there is no local task state and no optimistic update anywhere in the file (grep for router/refresh/mutate in calendar-view.tsx matches only the doc comment at :67-68 and the onTaskMutated declarations at :69/:86/:214/:247 — the component never imports useRouter). (2) Drag path: handleDayDrop PATCHes /api/tasks/:id (calendar-view.tsx:208-213) and on success calls only `onTaskMutated?.()` (:214). (3) Quick-add path: commitInlineTask POSTs /api/tasks (:231-239), shows the success toast (:244-246), then onl…

### [high] Quick-add from a day cell creates a task with no section, which is invisible everywhere in the project — even after reload
FILE: src/components/views/calendar-view.tsx:234 (area calendar)
SUMMARY: commitInlineTask POSTs only { name, dueDate, projectId } (calendar-view.tsx:231-239). POST /api/tasks creates the task with sectionId: data.sectionId, i.e. null — unlike the PATCH route, the create route has no auto-assign-to-first-section fallback (src/app/api/tasks/route.ts:248-263). The project page loads tasks exclusively through `sections.include.tasks` (src/app/(dashboard)/projects/[projectId]/page.tsx:56-119), so a task with sectionId null is never returned for the project. CalendarView, ListView and BoardView all consume `project.sections`, so the new task appears in NO project view.
SCENARIO: User clicks July 15 on the project calendar, types 'Submit permit set', hits Enter, sees the success toast. The task never appears in Calendar, List, or Board — even after reloading the page. The task exists in the DB (self-assigned, so it may surface in My Tasks), but from the project's perspective it is silently lost.
FIX: Include a sectionId in the POST body (e.g. pass the project's first section id into CalendarView, as List/Board quick-adds do), or add the same 'auto-assign to first section when projectId is set and sectionId is not' fallback to POST /api/tasks that PATCH already has (src/app/api/tasks/[taskId]/route.ts:292-301).
VERIFIER: CONFIRMED — full chain reproduced from source. (1) src/components/views/calendar-view.tsx:231-239: commitInlineTask POSTs to /api/tasks with body { name, dueDate, projectId } only — no sectionId. (2) src/app/api/tasks/route.ts:10-22: createTaskSchema has sectionId optional/nullable, so the request parses with data.sectionId undefined. (3) route.ts:248-263: tx.task.create({ data: { ... sectionId: data.sectionId ... }}) — task is created with sectionId null; there is NO auto-assign-to-first-section fallback in POST. In fact the position calc at route.ts:239-246 explicitly has a branch for `projectId set, sectionId: null`, so the create succeeds (201), and calendar-view.tsx:244-246 shows the su…

### [high] Tasks render one day early for US users (local getters on UTC-midnight due dates), and dropping a task on its own cell silently rewrites the stored date a day back
FILE: src/components/views/calendar-view.tsx:306 (area calendar)
SUMMARY: Due dates in this app are stored as UTC midnight — composers send 'YYYY-MM-DD' and the API stores new Date(str) = 00:00 UTC; src/lib/date-only.ts:1-17 documents exactly this and provides dueDateToLocalMidnight() (UTC getters) for widgets. CalendarView ignores that helper and buckets with LOCAL getters: `new Date(startRaw.getFullYear(), startRaw.getMonth(), startRaw.getDate())` (calendar-view.tsx:306-315). For any viewer west of UTC (the firm is US-based, UTC-4/-5), 2026-07-10T00:00:00Z becomes local July 9 → the bar renders on July 9. Compounding it: the drop handler anchors the new date to LOCAL noon of the rendered cell (calendar-view.tsx:179-197), and the no-op guard at :199-205 compares ISO strings (noon-local vs midnight-UTC), which never match — so dropping a task back onto the very cell it is displayed in issues a PATCH that moves the stored dueDate from July 10 (UTC day) to July …
SCENARIO: US Eastern user sets a task due 'Jul 10' from the list view; the project Calendar shows it under Jul 9. If they drag the bar within the calendar to 'fix' or even just re-drop it on the same visible day, the stored due date genuinely shifts a day earlier, and Home/Inbox/calendar-feed widgets (which bucket by UTC day via date-only.ts) now show Jul 9 too — the date was silently corrupted by a display bug.
FIX: Use UTC getters (or dueDateToLocalMidnight from src/lib/date-only.ts) when computing `start`/`due` at calendar-view.tsx:306-315, and when writing dates on drop, send the UTC calendar day (e.g. Date.UTC(y,m,d) of the target cell) instead of local noon so drag output matches the storage convention.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Storage convention: composers send date-only strings — src/components/views/list-view.tsx:1531-1532 (`format(due, "yyyy-MM-dd")`), timeline-view.tsx:518-531 same — and the API stores `new Date(str)`: src/app/api/tasks/route.ts:256 (POST) and src/app/api/tasks/[taskId]/route.ts:333-336 (PATCH, no normalization — stores whatever instant arrives). JS parses "YYYY-MM-DD" as 00:00 UTC; src/lib/date-only.ts:1-17 explicitly documents this convention and provides dueDateToLocalMidnight() with UTC getters. (2) Display bug: calendar-view.tsx:302-315 parses task.dueDate (ISO string per the Task interface at :43) then buckets with LOCAL getters `new Dat…

### [high] Eleven project tab sub-routes still grant read access on visibility=WORKSPACE (the schema default), leaking tab data the page and project list deliberately hide
FILE: src/app/api/projects/[projectId]/messages/route.ts:53 (area cross-permissions)
SUMMARY: Project.visibility defaults to WORKSPACE (prisma/schema.prisma:400). The page (page.tsx:136-159) and /api/projects/[projectId] GET (route.ts:82-111) do NOT grant access for WORKSPACE visibility — /api/projects/route.ts:90-97 documents that this auto-grant was removed as a leak. But every tab sub-route keeps a local assertProjectAccess with the legacy clause `if (project.visibility === "WORKSPACE") { wsMember? -> ok }`: messages/route.ts:53-60, activity/route.ts:58-65, status-updates/route.ts:88-95, status-highlights/route.ts:55-65, companies/route.ts:64-71, dependencies/route.ts:35-42, forms/route.ts:78-85, workflow/route.ts:40-47, objectives/route.ts:32-39, custom-fields/route.ts:77-84, custom-fields/values/route.ts:40-47. So the project detail endpoint returns 403 while all of its sub-resources return 200.
SCENARIO: An invited L1-L3 user who cannot open /projects/P (404) and never sees P in any list fetches /api/projects/P/messages and gets the last 100 team messages with authors and attachment URLs; /api/projects/P/companies returns every participant's name, email, and position; /api/projects/P/custom-fields/values returns all task custom-field values — for effectively every project, since WORKSPACE is the default visibility.
FIX: Replace the copy-pasted assertProjectAccess in these 11 routes with one shared helper that mirrors page.tsx: owner || ProjectMember || PUBLIC || (workspace role OWNER/ADMIN || level>=4). Drop the bare WORKSPACE-visibility grant.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Default: prisma/schema.prisma:400 `visibility ProjectVisibility @default(WORKSPACE)`. (2) Intended policy: src/app/api/projects/route.ts:90-97 comment states L1-L3 see ONLY owned/explicit-member projects and that the WORKSPACE auto-grant "was leaking projects to invited users" and was removed. (3) Page enforces it: src/app/(dashboard)/projects/[projectId]/page.tsx:132-159 grants only owner || ProjectMember || PUBLIC || (workspace role OWNER/ADMIN || getLevel>=4), else notFound(); no WORKSPACE clause. (4) Detail API enforces it identically: src/app/api/projects/[projectId]/route.ts:82-111 returns 403. (5) Sub-routes do NOT: verified messages/…

### [high] Non-member workspace users can POST messages and status updates into projects they cannot open
FILE: src/app/api/projects/[projectId]/messages/route.ts:211 (area cross-permissions)
SUMMARY: The write half of the WORKSPACE-visibility hole: POST /api/projects/:id/messages (messages/route.ts:200-239) only requires assertProjectAccess, which passes for any workspace member on a default-visibility project (member=null path at :53-60), then creates the message with no role check. POST /api/projects/:id/status-updates (status-updates/route.ts:180-260) likewise creates a StatusUpdate record for member=null callers (only the project.status sync is gated by canEditProject at :235-245), and the notification fan-out at :279-308 then pushes the outsider's update into every real member's inbox.
SCENARIO: An L1-L3 user with no membership on project P (page 404s) POSTs {"content":"hi"} to /api/projects/P/messages — the message appears in the project's Messages tab authored by them; POSTing a status update to /api/projects/P/status-updates inserts it into the status history and notifies all project members, even though the author cannot view the project.
FIX: Apply the same corrected access helper as the read routes, and additionally require project membership (any role for messages; keep the existing editor gate for status sync) before creating rows.
VERIFIER: CONFIRMED. The claimed chain reproduces from source end-to-end. ACCESS-DIVERGENCE (the core of the vulnerability): - The project PAGE (src/app/(dashboard)/projects/[projectId]/page.tsx:132-159) grants access to a non-member only if: OWNER/ADMIN workspace role, OR position level >= 4 (getLevel, people-types.ts:581-584). A plain WORKSPACE member who is L1-L3 with no project membership hits notFound() at :158. So an L1-L3 workspace user genuinely CANNOT open project P in the UI. - The API helpers use a LOOSER rule. In both messages/route.ts:53-60 and status-updates/route.ts:88-95, any workspaceMember row (any role, any level) on a WORKSPACE-visibility project returns ok:true with member=null. v…

### [high] Re-editing a just-created form shows a blank field list and saving wipes the real fields
FILE: src/app/api/forms/route.ts:214 (area forms)
SUMMARY: The form builder creates forms via POST /api/forms (form-builder-dialog.tsx:419-420) and does `const saved = (await res.json()) as FormRow` (line 430), but the POST handler returns only {id, name, projectName, responsesCount, isActive, visibility} (api/forms/route.ts:214-224) — no fields, description, projectId, defaultSectionId, defaultAssigneeId, confirmationMessage, notifyOnSubmission, submissionCount. workflow-view.tsx handleFormSaved (lines 334-343) prepends this partial object to the forms list. When the user clicks Edit on that row (workflow-view.tsx:576), the builder hydrates from it: `Array.isArray(initial.fields)` is false so it falls back to `[emptyField("TEXT")]` (form-builder-dialog.tsx:208-211), and every setting hydrates to undefined. If the user clicks "Save changes", handleSave PATCHes `fields: [blank 'New field']` to /api/forms/:id, which overwrites the stored fields ([…
SCENARIO: User creates a form with 8 questions, hits Create, then immediately clicks Edit on the new row to fix a typo. The editor shows a single blank "New field" instead of their 8 questions; hitting Save changes permanently replaces the 8 questions with the blank field. Even without saving, the editor showing an empty form is broken. The Inbox button on the fresh row is similarly broken: FormSubmissionsDialog reads form.fields (undefined) so answers render with raw fieldId keys instead of labels.
FIX: Return the full FormRow shape (same as the PATCH response, including fields/description/projectId/settings/submissionCount) from POST /api/forms, or have handleFormSaved refetch /api/projects/:id/forms after create.
VERIFIER: CONFIRMED — reproduced the entire chain from source (note: the client files live in src/components/views/, not src/components/workflow/ as the finding cites, but all line numbers match exactly). Evidence chain: 1. POST /api/forms response is partial: src/app/api/forms/route.ts:214-224 returns only {id, name, projectName, responsesCount, isActive, visibility} — no fields/description/projectId/defaultSectionId/defaultAssigneeId/confirmationMessage/notifyOnSubmission/settings, even though all were persisted (route.ts:194-212). 2. Builder trusts it as full: src/components/views/form-builder-dialog.tsx:419-420 picks POST for create, :430 does `const saved = (await res.json()) as FormRow` (FormRow…

### [high] Print page leaks submission PII to any signed-in user when project visibility is PUBLIC
FILE: src/app/forms/[formId]/submissions/[submissionId]/print/page.tsx:74 (area forms)
SUMMARY: The printable submission view claims to use "the same access check as the submissions inbox / CSV export" (comment lines 17-18) but its check is `allowed = isOwner || !!member || form.project.visibility === "PUBLIC"` (line 74). The JSON submissions endpoint (api/forms/[formId]/submissions/route.ts:42-63) and CSV export (submissions/export/route.ts:63-83) both deliberately exclude the PUBLIC-project grant because submissions contain external submitters' PII (documented as audit SEC-08). The print page re-opens that hole: any authenticated account — including workspace GUEST/WORKER/CLIENT users with no relation to the project — can read every answer, attachment URL, and the submitter's name/email of any submission on a PUBLIC-visibility project by URL.
SCENARIO: A client-portal user signs in, obtains or guesses a /forms/<formId>/submissions/<submissionId>/print URL (submission IDs appear in notification emails and tracking links), and reads another client's RFI submission including their email and uploaded documents — data the submissions API and CSV export explicitly refuse to serve them.
FIX: Drop the `form.project.visibility === "PUBLIC"` grant on line 74 and mirror the SEC-08 check exactly: owner, project member, or workspace member only.
VERIFIER: CONFIRMED, evidence chain reproduced from source. (1) Print page src/app/forms/[formId]/submissions/[submissionId]/print/page.tsx: comment lines 17-18 claim "same access check as the submissions inbox / CSV export", but line 74 is `let allowed = isOwner || !!member || form.project.visibility === "PUBLIC";` — the PUBLIC-project grant is present. Lines 75-85 only add a workspaceMember check for WORKSPACE visibility; line 86 gates on `allowed`. The page then renders full submission PII: all answers (lines 100-114, 152-174), attachment URLs (line 159 `href={att.url}`), and submitter name/email (lines 91, 140-147). The only other gate is a signed-in session (lines 43-52) — no middleware.ts exists…

### [high] All due-date displays in the List tab parse UTC-midnight dates with local-time formatters, showing the wrong day and flagging tasks Overdue on their due date
FILE: src/components/views/list-view.tsx:1131 (area list)
SUMMARY: dueDate arrives as "YYYY-MM-DDT00:00:00.000Z" (page.tsx:180). Every renderer uses parseISO + local-time helpers: DueDateBadge (list-view.tsx:1131-1136, format/isToday/isTomorrow), the range label (1546-1559), the mobile helpers isOverdue/formatMobileDate (647-656), and the Status pill's derived Overdue check (1675-1679). In US timezones the parsed Date is the PREVIOUS local day, so the row shows "Jul 2" for a Jul 3 due date, "Today" appears on the wrong day, and isPast(date) && !isToday(date) is true all day on the actual due date, so a task due today renders as Overdue/black and the Status pill says "Overdue". Custom DATE field columns have the same bug: custom-field-cell.tsx:88-94 does new Date(iso).toLocaleDateString() on the UTC-midnight string stored by the custom-field PATCH route (route.ts:167-174).
SCENARIO: Eastern-time user sets a task due Jul 3. The List tab shows "Jul 2"; on Jul 3 (the actual due date) the row shows a black Overdue badge and Overdue status all day even though it is not yet late. A custom Date column value picked as Jul 3 also displays "Jul 2".
FIX: Add a shared parseDateOnly(iso) helper that builds the Date from UTC components (or from the yyyy-MM-dd prefix) and use it in DueDateBadge, isOverdue, formatMobileDate, the range label, the Status Overdue check, and CustomFieldCell's DATE case.
VERIFIER: CONFIRMED, full chain reproduced. (1) Storage: List tab inline editor sends format(due,"yyyy-MM-dd") (list-view.tsx:1531-1534, PATCH body built at :617-618); API stores new Date("yyyy-MM-dd") = UTC midnight (src/app/api/tasks/[taskId]/route.ts:334). (2) Serialization: projects/[projectId]/page.tsx:180 emits t.dueDate?.toISOString() -> "YYYY-MM-DDT00:00:00.000Z". (3) Renderers all parse with local-time date-fns helpers: DueDateBadge parseISO + isPast/isToday/isTomorrow/format (list-view.tsx:1131-1136), range label (1546-1558), mobile isOverdue/formatMobileDate (647-656), Status pill Overdue check (1675-1679); imports at line 38 confirm plain date-fns, no UTC-safe wrapper exists anywhere in th…

### [high] Drag-reorder while a filter or sort is active permanently rewrites the section's real task order from the filtered/sorted view
FILE: src/components/views/list-view.tsx:411 (area list)
SUMMARY: ListView receives `filteredSections` from project-content.tsx (line 935), which removes tasks (search, incomplete/completed, due-this-week, assigned-to-me, hide-completed; project-content.tsx:244-321) and/or re-sorts them (sortBy block, lines 293-318). handleDragEnd builds `orderedIds` from the DISPLAYED destination section (list-view.tsx:409-411) and POSTs /api/tasks/reorder, which renumbers position = array index for exactly those ids (reorder/route.ts:73-80). With a sort active, one drag persists the entire sorted display order as the section's canonical positions, destroying the manual order. With a filter active, only the visible subset gets positions 0..k-1 while hidden tasks keep their old positions, so visible and hidden tasks collide and interleave unpredictably once the filter is cleared (page.tsx orders only by position asc, ties unstable).
SCENARIO: User sorts the List by Priority, drags one task up a row, clears the sort - the whole section is now permanently stored in priority order and the previous manual order is gone. Or: user hides completed tasks, drags a task within a section, un-hides completed - completed tasks now appear shuffled in among the renumbered ones.
FIX: Disable row drag when sortBy or any task-removing filter is active (Asana's behavior), or compute orderedTaskIds against the unfiltered section task list by splicing the moved task into the full server order.
VERIFIER: CONFIRMED, full chain reproduced. (1) src/components/projects/project-content.tsx:244-321 — filteredSections removes tasks (search 251-259; incomplete/completed/due_this_week/assigned_to_me 262-282; hide-completed 285-290) and re-sorts (sortBy block 293-318); line 934-935 passes filteredSections as ListView's `sections` prop. (2) src/components/views/list-view.tsx — ListViewProps (152-157) carries no filter/sort awareness and no drag-disable flag; localSections is synced from the filtered prop (278, 284-287); DndContext (665), SortableContext over section.tasks (858-861), and useSortable rows (1292) are unconditionally enabled (grep for hasActiveFilters/dragDisabled/disabled in list-view.tsx…

### [high] Cross-section drag persists the task at the position where it first ENTERED the section, not where it was dropped
FILE: src/components/views/list-view.tsx:387 (area list)
SUMMARY: handleDragOver inserts the dragged task into the destination section once, at the index of the row first hovered (list-view.tsx:335 returns prev when srcSection.id === destSection.id, so all later over-events inside the destination are ignored). During continued movement dnd-kit's SortableContext still previews the gap at the cursor, but handleDragEnd only runs the corrective arrayMove when `originalSourceId === destSectionId` (line 387) - i.e. only for same-section drags. For cross-section drags it posts the destination order from localSections (lines 409-420), which still holds the entry-point index, so after router.refresh the task snaps to the row where the pointer first crossed the section boundary.
SCENARIO: User drags a task from section A, enters section B near its top, keeps dragging to the bottom and drops there. The preview shows it at the bottom; after the server round-trip refreshes, the task appears near the top of section B.
FIX: In handleDragEnd, when the drag was cross-section, arrayMove the task within the destination section from its current index to the index of overId before building orderedIds (mirroring the same-section branch without the originalSourceId guard).
VERIFIER: CONFIRMED — full chain reproduced from source. 1) Cross-section insert happens exactly once, at the entry point: src/components/views/list-view.tsx handleDragOver (317-354). On the first over-event after crossing into section B, the task is removed from A (line 342) and spliced into B at the index of the first hovered row (lines 345-348). On every subsequent over-event inside B, the active task is now IN B, so `srcSection.id === destSection.id` and line 335 `return prev` discards the update — localSections keeps the entry-point index no matter where the pointer moves afterward. (dnd-kit's SortableContext, line 858, still shifts rows visually via transforms, so the drop gap tracks the cursor …

### [high] Background poll never shows new replies: no-op comparator ignores replyCount/lastReplyAt
FILE: src/components/views/messages-view.tsx:424 (area messages)
SUMMARY: The silent-poll merge in fetchMessages short-circuits with an `unchanged` check (lines 424-447) that compares only id, updatedAt, isPinned, reactions and attachments. A new reply from another user changes only the root's `replyCount`/`lastReplyAt` (served by the GET route via _count.replies and replies[0], route.ts:116-121, 180-181) — the root row's updatedAt does not change when a reply is created. So `unchanged` evaluates true, the poll returns `prev`, and the fresh reply counts are discarded on every cycle.
SCENARIO: Two engineers have the Messages tab open. Engineer A posts a reply in a thread. Engineer B's tab polls every 10s but the '3 replies · last 5m ago' badge never updates and a thread with zero replies never gains its 'N replies' bar — B has no signal at all that a reply arrived until they reload the page (expanded threads do refresh via the separate replies poller, but collapsed ones — the default — never do).
FIX: Add `p.replyCount === m.replyCount && p.lastReplyAt === m.lastReplyAt` to the per-message comparison inside the `unchanged` check.
VERIFIER: CONFIRMED end-to-end. Evidence chain: (1) Comparator — src/components/views/messages-view.tsx:424-447: `unchanged` compares only p.id, p.updatedAt, p.isPinned, reactions (length+emoji/count/mine) and attachments.length; replyCount/lastReplyAt are absent, and line 447 returns `prev` when unchanged, discarding the fresh `result` built from server rows. (2) Server payload — src/app/api/projects/[projectId]/messages/route.ts:116-121 (replies take-1 + _count.replies) and 180-181 (`replyCount: m._count.replies`, `lastReplyAt: m.replies[0]?.createdAt`): a new reply changes ONLY these two fields on the root row's shaped output. (3) Root updatedAt does not move — src/app/api/messages/[messageId]/repl…

### [high] Overview feed endpoints deny workspace admins/L4+ users who can open the page, silently showing empty status updates, activity, and goals
FILE: src/app/api/projects/[projectId]/status-updates/route.ts:84 (area overview)
SUMMARY: The project page grants access to PRIVATE projects for workspace OWNER/ADMIN or Position L4+ users (src/app/(dashboard)/projects/[projectId]/page.tsx:138-155, same branch in src/app/api/projects/[projectId]/route.ts:88-107). But all four Overview feed endpoints use a narrower assertProjectAccess that only allows project owner, explicit project member, PUBLIC, or (WORKSPACE visibility + workspace member): status-updates/route.ts:81-97, activity/route.ts:50-67, objectives/route.ts:24-41, status-highlights/route.ts:52-68. None has the OWNER/ADMIN/L4+ branch. For a PRIVATE project these endpoints return 403 to a user the page just rendered for. The client swallows non-ok responses (project-overview.tsx:502-513 only commits state when res.ok), so the tab renders 'No status updates yet', 'No activity yet', and the empty goals state as if the project genuinely has no data.
SCENARIO: A firm principal (workspace ADMIN or L4+ position) who is not an explicit member opens a PRIVATE project. The page loads fine, but the Overview tab shows zero status updates, zero activity, no connected goals, and 'Status updates: 0' in the pulse — even though the project has months of history. Posting an update from the composer also fails with 'Forbidden'.
FIX: Extract the page's access check (including the workspace OWNER/ADMIN/L4+ branch) into a shared helper and use it in status-updates, activity, objectives, and status-highlights routes so API access matches page access.
VERIFIER: CONFIRMED, evidence chain reproduced from source: (1) PRIVATE visibility exists — prisma/schema.prisma:576-580 `enum ProjectVisibility { PRIVATE WORKSPACE PUBLIC }`. (2) Page grants PRIVATE-project access to workspace OWNER/ADMIN or L4+: src/app/(dashboard)/projects/[projectId]/page.tsx:136-158 — after owner/member/PUBLIC fails, it looks up workspaceMember and sets hasAccess when `role === "OWNER" || role === "ADMIN" || level >= 4`. Same branch in src/app/api/projects/[projectId]/route.ts:86-111. (3) All four Overview endpoints use a narrower check with NO OWNER/ADMIN/L4+ branch — only owner || explicit member || PUBLIC || (WORKSPACE visibility + any workspaceMember): status-updates/route.ts…

### [high] Composer hardcodes syncProjectStatus:true, so anyone who is not owner/ADMIN/EDITOR can never post a status update
FILE: src/components/projects/project-overview.tsx:667 (area overview)
SUMMARY: handlePostUpdate always sends syncProjectStatus: true (project-overview.tsx:667). The API deliberately allows any reader to post an update record but gates the status sync to owner/ADMIN/EDITOR (status-updates/route.ts:100-112 canEditProject, and 235-245 returns 403 when wantsSync && !canSync). Because the client never sends false and offers no fallback or retry-without-sync, every COMMENTER/VIEWER project member — and every non-member workspace colleague viewing a default WORKSPACE-visibility project — gets a hard 403 on Post. The composer UI is fully shown and enabled for these users.
SCENARIO: A workspace member opens a colleague's project (default WORKSPACE visibility, not a project member), fills in the four status blocks, and clicks 'Post update'. The request 403s with 'You don't have permission to change this project's status. Ask an editor or admin.' Their typed update cannot be posted at all, even though the API supports posting without the badge sync.
FIX: Compute the user's edit permission client-side (or retry the POST with syncProjectStatus:false after a 403), or have the server downgrade wantsSync to false for non-editors instead of rejecting the whole update.
VERIFIER: CONFIRMED. Full chain reproduced: (1) Client: src/components/projects/project-overview.tsx:667 — handlePostUpdate hardcodes `syncProjectStatus: true` in the POST body; the !res.ok branch (671-680) only surfaces the server error as a toast (700-703), with no retry-without-sync or fallback. (2) Composer is shown unconditionally: the "Post a status update…" trigger at project-overview.tsx:1030-1039 has no permission gate; ProjectOverviewProps (73-79) carries only `project` and `onManageMembers` — no role/canEdit prop exists anywhere in the file (grep for canEdit/role/readOnly finds none), and project-content.tsx:928 renders it ungated. (3) API: src/app/api/projects/[projectId]/status-updates/ro…

### [high] Archive menu item silently does nothing — 'ARCHIVED' is not a valid ProjectStatus
FILE: src/components/projects/project-content.tsx:417 (area shell)
SUMMARY: The header dropdown's Archive item PATCHes /api/projects/[projectId] with { status: 'ARCHIVED' } (project-content.tsx:413-424). The PATCH route validates status with z.enum(['ON_TRACK','AT_RISK','OFF_TRACK','ON_HOLD','COMPLETE']) (src/app/api/projects/[projectId]/route.ts:11), and the Prisma ProjectStatus enum has no ARCHIVED value either (prisma/schema.prisma:568-574). Zod throws, the route returns 400, and the client handler only acts on res.ok (`if (res.ok) { toast...; router.push(...) }`) with a .catch that only fires on network errors — so the 400 is swallowed with zero feedback.
SCENARIO: User opens the project-name dropdown in the project header and clicks 'Archive'. Nothing happens: no toast, no navigation, and the project is not archived. The feature is broken for 100% of users.
FIX: Either add an ARCHIVED value to the ProjectStatus enum (DB migration + zod enum) and filter archived projects out of GET /api/projects, or repoint the menu item to an existing semantic (e.g. status ON_HOLD or a dedicated archived boolean). Also add an else branch that shows toast.error with the server's error message.
VERIFIER: CONFIRMED — full chain reproduced. (1) Client: src/components/projects/project-content.tsx:413-424 — Archive DropdownMenuItem does fetch(`/api/projects/${project.id}`, PATCH, body {status:'ARCHIVED'}); success path is `if (res.ok) { toast.success('Project archived'); router.push('/projects/all'); }` with no else branch, and `.catch(() => toast.error(...))` only fires on network rejection, so an HTTP 400 (res.ok=false) is silently swallowed. (2) API: src/app/api/projects/[projectId]/route.ts:11 — updateProjectSchema.status is z.enum(["ON_TRACK","AT_RISK","OFF_TRACK","ON_HOLD","COMPLETE"]); line 137 `updateProjectSchema.parse(body)` throws ZodError for 'ARCHIVED', caught at lines 187-193 retur…

### [high] '% Comp' on /projects/all is computed with mismatched numerator/denominator (subtasks inflate the denominator)
FILE: src/app/(dashboard)/projects/all/page.tsx:494 (area shell)
SUMMARY: GET /api/projects returns `tasks` filtered to root tasks only (where parentTaskId: null, src/app/api/projects/route.ts:202-210) but `_count.tasks` counts ALL Task rows including subtasks (route.ts:211-216; subtasks are created with projectId set — see POST route.ts:500-510). The list view then computes completedTasks from the root-only array but totalTasks from the unfiltered _count (all/page.tsx:493-495, and again at 606-608 for mobile), and feeds that into computePmiSnapshot, whose percentComplete = completed/total (src/lib/pmi-metrics.ts:98-104).
SCENARIO: A template-created project (all engineering templates ship subtasks) with 10 root tasks (5 done) and 20 subtasks shows 5/30 = 17% complete instead of 50% in the '% Comp' column, and the derived SPI/health pill is dragged down accordingly, showing 'At risk'/'Off track' for healthy projects.
FIX: Use taskList.length as the denominator (`const totalTasks = taskList.length`) instead of p._count.tasks, or make the API's _count use the same parentTaskId: null filter (Prisma _count supports `where`).
VERIFIER: CONFIRMED — full chain reproduced. (1) Client fetch: src/app/(dashboard)/projects/all/page.tsx:152 fetches /api/projects. (2) API: src/app/api/projects/route.ts GET includes `tasks: { where: { parentTaskId: null }, ... }` (lines 202-210) but `_count: { select: { tasks: true, sections: true } }` with NO filter (lines 211-216), so _count.tasks counts all Task rows including subtasks. (3) Subtasks do carry projectId: POST route.ts:500-510 createMany's subtasks with `projectId: created.id` and `parentTaskId: parent.id` for template-gallery projects, so they are included in the unfiltered _count. (4) Client mismatch: all/page.tsx:493-495 sets `totalTasks = p._count.tasks ?? taskList.length` (the …

### [high] Project members without a company (including every project's creator) are invisible on the Team tab and cannot be re-affiliated
FILE: src/components/views/project-team-view.tsx:218 (area team)
SUMMARY: The Team tab fetches only /api/projects/:id/companies (project-team-view.tsx:131) and renders exclusively companies.map(...c.members) (lines 218-229, 407-416). The companies GET route returns only ProjectMember rows attached to a ProjectCompany (src/app/api/projects/[projectId]/companies/route.ts:96-135); ProjectMember rows with companyId=null are never returned or rendered anywhere in this view. Unaffiliated rows are the norm, not an edge case: (a) project creation auto-adds the creator as a ProjectMember with role ADMIN and no companyId (src/app/api/projects/route.ts:411-416); (b) ProjectMembersDialog's addMember POSTs {userId, role} without companyId (src/components/projects/project-members-dialog.tsx:145 -> members/route.ts:196-202, companyId ?? null); (c) deleting a firm SetNulls its members' companyId (prisma/schema.prisma:470, companies/[companyId]/route.ts:170) — the confirm dial…
SCENARIO: Owner creates a project, opens Team tab, adds their firm as a company, then tries 'Add member' to put themselves (or anyone added earlier via the header Members dialog) into the firm — the person doesn't show in any card, yet adding them fails with 'User is already a member of this project'. Likewise, after 'Remove firm', all of that firm's people keep full project permissions but vanish from the Team tab with no way to see, re-role, re-affiliate, or remove them from this screen.
FIX: In the companies GET route, also return ProjectMember rows where companyId=null (e.g. as an 'Unaffiliated' pseudo-group) and render that group in ProjectTeamView; add a 'Move to firm' action that PATCHes /members with {userId, companyId}; build alreadyOnProject from all project members (fetch /members or include unaffiliated in the companies payload).
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Team tab renders ProjectTeamView (project-content.tsx:999-1005), which fetches only /api/projects/:id/companies (project-team-view.tsx:131) and renders exclusively companies.map → CompanyCard → company.members.map (project-team-view.tsx:218-229, 407-416); no code path renders members outside a company. (2) The companies GET queries prisma.projectCompany.findMany with include:{members} (companies/route.ts:96-135), so ProjectMember rows with companyId=null never appear in the payload. (3) Unaffiliated rows are the norm: project creation creates the creator as ProjectMember ADMIN with no companyId (projects/route.ts:411-416); ProjectMembersDial…

### [high] Gantt drag/resize saves dates one day short for US-timezone users; 1-day drags are silent no-ops that snap back
FILE: src/components/views/timeline-view.tsx:518 (area timeline)
SUMMARY: Task dates arrive as UTC-midnight ISO strings (page.tsx:180-181 serializes `t.dueDate?.toISOString()` -> "2026-07-15T00:00:00.000Z"). On mouseup the drag handler does `parseISO(dragState.originalDue)` (timeline-view.tsx:520/516/527), which for any user west of UTC yields the PREVIOUS local day at 19:00-20:00, then `addDays(..., deltaDays)` and `format(newDue, "yyyy-MM-dd")` (lines 518, 521, 530-531) formats in LOCAL time, producing a calendar date one day earlier than intended. The PATCH body therefore carries storedDate + delta - 1. Worse, for delta = +1 the string equals the currently stored date, so the server's change detector (src/app/api/tasks/[taskId]/route.ts:333-343, `newDueDate?.getTime() !== existingTask.dueDate?.getTime()`) sees no change, writes nothing, and `router.refresh()` (timeline-view.tsx:541) snaps the bar back to its original position.
SCENARIO: A user in Florida (UTC-4) drags a task bar one day to the right on the Timeline tab. The preview shows the bar moved +1 day; on release the bar snaps back to where it was, with no error. Dragging +3 days moves the task only +2 days. The same off-by-one applies to the left resize handle (startDate) and whole-bar move.
FIX: Derive new dates in UTC: strip the time from the ISO string (e.g. `dragState.originalDue.slice(0, 10)`), add deltaDays with pure date arithmetic (or use the UTC getters), and send that yyyy-MM-dd. Never round-trip a UTC-midnight instant through local-time parseISO+format.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Storage: task dates are persisted as UTC midnight for the common write paths — quick-create-task-modal.tsx:237-258 sends fmtIso yyyy-MM-dd; api/tasks/route.ts:256 and api/tasks/[taskId]/route.ts:334/346 do `new Date("yyyy-MM-dd")` → UTC 00:00; the timeline drag itself also writes yyyy-MM-dd, so any task ever dragged is UTC-midnight. (2) Serialization: src/app/(dashboard)/projects/[projectId]/page.tsx:180-181 sends `t.dueDate?.toISOString()` → "2026-07-15T00:00:00.000Z"; passed to TimelineView via project-content.tsx:950. (3) Client: timeline-view.tsx:484 stores that string as originalDue; on mouseup lines 513-531 do `parseISO(originalDue)` (…

### [high] Timeline bars and tooltips render every task one day early for US-timezone users (task due today appears overdue, left of the Today line)
FILE: src/components/views/timeline-view.tsx:351 (area timeline)
SUMMARY: getTaskPosition (timeline-view.tsx:351-367) computes `taskEnd = parseISO(task.dueDate)` from a UTC-midnight ISO string, giving a local Date on the PREVIOUS day (e.g. Jul 14 19:00 EST for a Jul 15 due date). `differenceInDays(taskEnd, timelineStart)` (line 364) truncates against the local-midnight timeline start, so the bar's right edge lands in the Jul 14 column instead of Jul 15. The Today line, by contrast, is computed purely in local time (lines 436-454) and is correct, so bars sit one day left of where they should relative to it. The hover tooltip (lines 1354-1359) formats the same shifted dates and displays "Jul 14" for a task the List view/task panel shows as due Jul 15. isTaskDueSoon (lines 626-633) and the look-ahead filter (line 205) use the same shifted parse.
SCENARIO: A task is due July 15. A US user opens the Timeline tab on July 15: the bar ends in the July 14 cell, entirely to the left of the black Today marker, so the task reads as overdue; hovering shows "→ Jul 14" while the task detail panel says July 15.
FIX: Normalize UTC-midnight date strings to local dates before positioning/formatting, e.g. `parseISO(task.dueDate.slice(0, 10))` or add back the timezone offset (`new Date(d.getTime() + d.getTimezoneOffset()*60000)`), and use that helper everywhere in the view (position, tooltip, due-soon, look-ahead).
VERIFIER: CONFIRMED (core claim). Evidence chain: (1) Due dates are stored as UTC midnight — the date picker sends date-only strings (list-view.tsx:1531-1534 `format(due, "yyyy-MM-dd")`), the API stores `new Date(data.dueDate)` (src/app/api/tasks/[taskId]/route.ts:334, src/app/api/tasks/route.ts:256), which JS parses as 00:00 UTC; the repo itself documents this exact hazard in src/lib/date-only.ts:4-9 ("a task due 'today' renders as overdue 'Yesterday'" for viewers west of UTC) and ships `dueDateToLocalMidnight` as the fix. (2) The server page serializes with toISOString (src/app/(dashboard)/projects/[projectId]/page.tsx:180), so TimelineView receives "2026-07-15T00:00:00.000Z". (3) timeline-view.tsx:…

### [high] Milestone diamonds and approval icons are plotted 7 days before their actual date when the task has no startDate
FILE: src/components/views/timeline-view.tsx:1223 (area timeline)
SUMMARY: For MILESTONE/APPROVAL tasks the chart renders an icon at `left: renderLeft` (timeline-view.tsx:1216-1245), which is `position.left` from getTaskPosition. getTaskPosition computes `taskStart = task.startDate ? parseISO(...) : new Date(taskEnd.getTime() - 7*24*60*60*1000)` (lines 352-354) and derives `left` from taskStart (lines 363-366). Milestones are typically created with only a due date, so the diamond is drawn at dueDate − 7 days, not at the milestone date. isTaskMilestone (lines 646-654) returns true for explicit taskType=MILESTONE regardless of dates, so this hits every date-only milestone; APPROVAL tasks (line 658) hit the same path.
SCENARIO: A PM marks "Permit approval" as a MILESTONE due July 22 (no start date). On the Timeline tab the gold diamond appears in the July 15 cell — a full week early — while List/Calendar show July 22.
FIX: For milestone/approval markers, position the icon at the due-date coordinate (compute left from taskEnd, or `pos.left + pos.width` / a dedicated point position), not at the bar's implied start.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Creation: src/app/api/tasks/route.ts:17,20 accepts taskType MILESTONE/APPROVAL with startDate optional, and line 257 stores `startDate: data.startDate ? new Date(...) : null` — no server-side default ties startDate to dueDate for milestones; the quick-create modal (src/components/tasks/quick-create-task-modal.tsx:129,257) likewise sends startDate null unless explicitly picked, so date-only milestones (the normal case) have startDate=null. (2) Position: src/components/views/timeline-view.tsx:352-354 — `taskStart = task.startDate ? parseISO(...) : new Date(taskEnd.getTime() - 7*24*60*60*1000)`; lines 363-366 derive `left` solely from taskStart…

### [high] Deleting an intake form permanently cascades-deletes all its submissions while the confirm dialog promises they are kept
FILE: src/components/views/workflow-view.tsx:346 (area workflow)
SUMMARY: handleFormDelete shows confirm("Delete this form? Past submissions are kept but new ones will be rejected.") (workflow-view.tsx:346) and then calls DELETE /api/forms/:formId. That route does a hard prisma.form.delete (src/app/api/forms/[formId]/route.ts:299), and the FormSubmission model declares `form Form @relation(..., onDelete: Cascade)` (prisma/schema.prisma:1430), so every FormSubmission row (the RFI/change-order/inspection intake data, including the JSON answers and attachment references) is destroyed with the form. The message describes a soft-delete (isActive=false) that the code never performs.
SCENARIO: A PM deletes an old 'RFI Request' form from the Workflow tab, reading the prompt's assurance that past submissions are kept. All contractor submissions for that form are permanently deleted from the production database; the submissions inbox history is unrecoverable.
FIX: Either make delete a soft-delete (PATCH isActive:false, which the public GET already honors with a 410) matching the message, or change the confirm text to warn that all submissions will be permanently deleted; ideally set FormSubmission.form onDelete to Restrict/SetNull if history must survive.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Client: src/components/views/workflow-view.tsx:346 — handleFormDelete shows confirm("Delete this form? Past submissions are kept but new ones will be rejected.") then fetches DELETE `/api/forms/${formId}` (line 352). No other guard or soft-delete path exists in the handler. (2) API: src/app/api/forms/[formId]/route.ts:299 — DELETE handler runs `await prisma.form.delete({ where: { id: formId } })`, a hard delete; it never sets isActive=false (isActive is only mutable via PATCH, route.ts:221). (3) Schema: prisma/schema.prisma:1430 — `form Form @relation(fields: [formId], references: [id], onDelete: Cascade)` on FormSubmission, so all submissio…

### [high] "Add to another project" rule creates the copy as a hidden subtask of the source task — it never appears in the target project
FILE: src/lib/workflow-engine.ts:250 (area workflow)
SUMMARY: runAction ADD_TO_PROJECT creates the twin task with `parentTaskId: ctx.taskId` (workflow-engine.ts:239-252) purely as a de-dup marker. But every root-level task query filters `parentTaskId: null` — the project page loads section tasks with `where: { parentTaskId: null }` (src/app/(dashboard)/projects/[projectId]/page.tsx:61) and GET /api/projects does the same (src/app/api/projects/route.ts:203). So the copied task is invisible in the target project's List/Board/Timeline. Worse, because parentTaskId points at the source task, the copy shows up as a phantom SUBTASK of the original task (subtasks include at src/app/api/tasks/[taskId]/route.ts:393-395) and inflates its subtask count.
SCENARIO: User configures 'When task moves to Issued → Add to another project: As-Builts Archive'. A task moves to Issued: nothing ever appears in the As-Builts Archive project (its list filters out non-root tasks), while the original task suddenly grows a duplicate-named subtask in the source project. The user concludes the rule is broken and the source task looks corrupted.
FIX: Create the twin with parentTaskId: null and track provenance differently (e.g. a sourceTaskId field, an Activity row, or de-dup by name+projectId only), and give it a proper position at the end of the target section.
VERIFIER: CONFIRMED — full chain reproduced. (1) src/lib/workflow-engine.ts:195-253: ADD_TO_PROJECT creates the twin via prisma.task.create with projectId: action.projectId, sectionId: target.sections[0].id, and parentTaskId: ctx.taskId (line 250); the parentTaskId is used as the de-dup key (findFirst at lines 229-237 matches on projectId+name+parentTaskId). No position is set. (2) The action is a real user-facing feature: exposed in the rule builder UI (src/components/views/workflow-action-dialog.tsx:171-176, 327-345; label "Add to another project" at src/lib/workflow-types.ts:68) and accepted by the rules API zod schema (src/app/api/projects/[projectId]/workflow/rules/route.ts:40). (3) Target-projec…

### [high] Rule create/patch endpoints accept arbitrary target IDs, letting the engine write into foreign projects and assign arbitrary users
FILE: src/app/api/projects/[projectId]/workflow/rules/route.ts:152 (area workflow)
SUMMARY: POST /workflow/rules (and PATCH on [ruleId]/route.ts) validate only the JSON shape: SET_ASSIGNEE userId (line 30), ADD_COLLABORATORS userIds (line 33), ADD_TO_PROJECT projectId (line 40), and trigger.sectionId are never checked against the project or workspace before prisma.workflowRule.create at line 152. The engine then executes them with no access check: ADD_TO_PROJECT looks up ANY project id and creates a task in it (workflow-engine.ts:202-252), SET_ASSIGNEE writes any userId onto the task (workflow-engine.ts:122-126), ADD_COLLABORATORS inserts any userIds. Contrast with /api/tasks/bulk which explicitly calls assertUserInWorkspace to prevent exactly this cross-workspace write (src/app/api/tasks/bulk/route.ts:53-58).
SCENARIO: An EDITOR member of project A sends POST /api/projects/A/workflow/rules with action {type:"ADD_TO_PROJECT", projectId:"<id of a private project in another workspace>"}. Every time a task moves into the trigger section, the engine silently creates tasks inside that foreign project, and SET_ASSIGNEE can pin tasks to users outside the workspace — writes the project-page access check would 403/404.
FIX: In the rules POST/PATCH handlers, validate trigger.sectionId belongs to projectId, ADD_TO_PROJECT projectId is a project the caller can edit (same workspace + access check), and all userIds are members of the project's workspace, returning 400/403 otherwise.
VERIFIER: CONFIRMED. The entire chain reproduces from source. CREATE handler (src/app/api/projects/[projectId]/workflow/rules/route.ts): - createRuleSchema (lines 51-54) + actionSchema (29-49) validate ONLY the JSON shape. SET_ASSIGNEE takes any userId string (line 30), ADD_COLLABORATORS any userIds (33), ADD_TO_PROJECT any projectId string (40), TASK_MOVED_TO_SECTION any sectionId (24). - The only authorization is on the CALLER's relationship to the URL projectId: assertProjectAccess (110) + canEditWorkflow (118) confirm the caller is owner/ADMIN/EDITOR of project A. Nothing validates that action payload targets (target projectId, userIds, trigger sectionId) belong to project A or its workspace. - pr…

### [high] Removing one action chip silently deletes the whole rule including its other actions
FILE: src/components/views/workflow-view.tsx:800 (area workflow)
SUMMARY: SectionCard/CompletionCard flatten each rule into one chip per action (`rules.flatMap((r) => r.actions.map(...))`, workflow-view.tsx:747-753 and 853-859), presenting each action as an independent automation. But the X button on a chip calls onRemoveRule(chip.ruleId) (line 800/894), which DELETEs the entire WorkflowRule. Template rules bundle 2-4 actions per rule (e.g. calc-package-review 'Stamping' = ADD_COMMENT + 2 ADD_SUBTASKs, src/lib/workflow-templates.ts:70-79), so deleting the 'Add a subtask' chip also deletes the comment and the other subtask, and all sibling chips vanish with no warning beyond the 'Rule removed' toast.
SCENARIO: User applies the 'Permit submittal cycle' template, then decides they don't want the 'Draft response narrative' subtask on the Corrections card and clicks its X. The ADD_COMMENT, SET_PRIORITY HIGH, and 'Revise affected sheets' subtask actions disappear too — three automations lost from one click, with no undo.
FIX: When a rule has multiple actions, PATCH the rule with the remaining actions array (the PATCH endpoint already supports `actions`) and only DELETE the rule when its last action is removed.
VERIFIER: CONFIRMED — entire chain reproduced from source. Evidence chain: 1. Chips flatten per-action: SectionCard builds `ruleChips = rules.flatMap((r) => (r.actions as WorkflowAction[]).map((a, idx) => ({ ruleId: r.id, actionIdx: idx, type: a.type })))` (src/components/views/workflow-view.tsx:746-753); CompletionCard does the identical flatten (:853-859). Each action renders as its own chip labeled by ACTION_LABELS, visually indistinguishable from a standalone automation. 2. X button deletes by ruleId: both cards' chip X buttons call `onRemoveRule(chip.ruleId)` (workflow-view.tsx:800 and :894) — the actionIdx is ignored. No confirm dialog (the only confirm() in the file, line 346, is for form delet…

### [medium] No onDragCancel handler: pressing Escape mid-drag permanently freezes board sync and leaves a phantom move plus a stuck drag overlay
FILE: src/components/views/board-view.tsx:538 (area board)
SUMMARY: The DndContext (board-view.tsx:538-544) wires onDragStart/onDragOver/onDragEnd but not onDragCancel. dnd-kit fires onDragCancel — not onDragEnd — when a drag is cancelled (Escape key). All cleanup lives only in handleDragEnd: isDraggingRef.current=false (line 233), setActiveTask(null) (line 230), and rollback. After a cancel: (1) isDraggingRef stays true forever, so the effect at lines 131-134 never syncs localSections from the `sections` prop again — router.refresh results, the toolbar search box, filters, sort, and show-completed all stop affecting the board until the component unmounts (tab switch); (2) activeTask is never cleared, so `{activeTask && <TaskCardOverlay/>}` (line 586-588) keeps the floating overlay card rendered; (3) if the drag had crossed columns, handleDragOver's optimistic move is never rolled back, so the card displays in the new column even though nothing was persi…
SCENARIO: Start dragging a card, drift it over another column, press Escape. The ghost card stays stuck on screen, the task appears moved to the other column (but isn't on the server), and from then on typing in the task search box or completing tasks (router.refresh) has no visible effect on the board until the user switches tabs.
FIX: Add an onDragCancel handler to DndContext that sets isDraggingRef.current = false, dragSourceSectionRef.current = null, setActiveTask(null), and setLocalSections(sections) to roll back the optimistic cross-column move.
VERIFIER: CONFIRMED (2 of 3 claimed effects; overlay claim refuted). Evidence chain: (a) board-view.tsx:538-544 wires onDragStart/onDragOver/onDragEnd only — no onDragCancel. (b) dnd-kit fires onDragCancel, not onDragEnd, on Escape: PointerSensor's AbstractPointerSensor attaches a document keydown listener (node_modules/@dnd-kit/core/dist/core.cjs.development.js:1453), handleKeydown on Escape calls handleCancel (:1600-1603), which calls onCancel = createHandler(Action.DragCancel) (:3114), which dispatches 'onDragCancel' (:3159 — eventName is 'onDragEnd' ONLY for Action.DragEnd). Cancel also fires on window resize, visibilitychange (tab switch mid-drag), and pointercancel (:1445-1451), so the trigger s…

### [medium] Due-date badges show the previous day and flag tasks overdue a day early for US-timezone users
FILE: src/components/views/board-view.tsx:1039 (area board)
SUMMARY: Due dates are stored as UTC midnight and serialized as e.g. "2026-07-03T00:00:00.000Z" (page.tsx:180). DueDateBadge does `parseISO(dueDate)` (board-view.tsx:1039) producing a local Date of Jul 2, 7:00 PM for a US Central user, then uses local-time `isPast`/`isToday`/`isTomorrow`/`format(date, "MMM d")` (lines 1040-1046). The mobile helpers isMobileOverdue/formatMobileDate (lines 341-350) have the identical bug. A task due today renders as "Jul 2" in black overdue styling; every date label on every card is off by one day for any user west of UTC.
SCENARIO: US user (UTC-5/-6) opens the Board tab on July 3. A task due July 3 shows the badge "Jul 2" in bold black overdue styling; a task due tomorrow shows "Today". Every card's date is shifted one day early and overdue status fires a day too soon.
FIX: Interpret UTC-midnight due dates in UTC: extract the date part (dueDate.slice(0,10)) and construct a local Date from year/month/day (or compare using the UTC getters) before running isPast/isToday/format, in both DueDateBadge and the mobile helpers — same pattern needed anywhere the app renders task.dueDate.
VERIFIER: CONFIRMED but overstated. Chain verified: page.tsx:180 serializes t.dueDate.toISOString(); board-view.tsx:1039-1046 (DueDateBadge) and 341-350 (isMobileOverdue/formatMobileDate) do parseISO + local-time isPast/isToday/isTomorrow/format with no normalization anywhere. The "stored as UTC midnight" premise holds ONLY for some write paths: quick-create-task-modal.tsx:237-258 sends fmtIso() = "YYYY-MM-DD" which api/tasks/route.ts:256 turns into new Date("YYYY-MM-DD") = UTC midnight; same for AI-generated tasks (api/ai/generate-tasks/route.ts:34 ISO date string via add-tasks-ai-modal.tsx:192) and form submissions (api/forms/[formId]/submit/route.ts:318). For those tasks the claimed symptom reprodu…

### [medium] Quick-add task has no in-flight guard: repeated/held Enter creates duplicate tasks
FILE: src/components/views/board-view.tsx:296 (area board)
SUMMARY: handleAddTaskSubmit (board-view.tsx:296-322) POSTs to /api/tasks and only clears newTaskName after the response resolves (line 318); the Enter keydown handler (line 794) calls it unconditionally with no isSubmitting flag and the input is never disabled. Key auto-repeat from holding Enter, or two quick presses on a slow connection, fires multiple POSTs with the same name — each one creates a separate task row (tasks/route.ts:229-312). handleMobileAddTask (lines 354-378, keydown at 504) has the same flaw.
SCENARIO: User types 'Pour footing inspection' in the inline add box and holds Enter a beat too long (or double-taps Enter on a slow connection). Two or more identical tasks are created in the column, each self-assigned and position-appended.
FIX: Add an in-flight guard (e.g. a submitting ref/state checked at the top of handleAddTaskSubmit and handleMobileAddTask), and/or clear newTaskName synchronously before awaiting the fetch.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Desktop keydown: board-view.tsx:793-794 `if (e.key === "Enter") onSubmitTask();` — no e.repeat check, no guard; input (789-803) has no disabled prop. (2) onSubmitTask wiring: board-view.tsx:565 `onSubmitTask={() => handleAddTaskSubmit(section.id)}`. (3) handleAddTaskSubmit (296-322): only early-returns on empty `newTaskName.trim()` (297); it awaits fetch POST /api/tasks (304-312) and clears newTaskName only AFTER the response resolves (setNewTaskName("") at 318). During the in-flight window newTaskName is still populated, so a repeated Enter (auto-repeat or second press) re-enters the handler past the empty-check and fires another POST with …

### [medium] Inline quick-add can create duplicate tasks: no in-flight guard, and Enter-commit disables the input which triggers the blur-commit
FILE: src/components/views/calendar-view.tsx:847 (area calendar)
SUMMARY: commitInlineTask (calendar-view.tsx:222-255) has no re-entrancy guard — it only checks `!name`. The input commits on Enter (:838-841) AND on blur (:847). When Enter fires, commitInlineTask sets creatingInline=true; React re-renders with `disabled={creatingInline}` (:848) before the fetch resolves, and a focused element that becomes disabled loses focus, firing onBlur → commitInlineTask runs a second time while newTaskName is still populated (it is only cleared in the finally block at :253) → a second POST /api/tasks with the identical payload. Even in browsers that don't blur on disable, any click elsewhere during the in-flight POST triggers the same double-create.
SCENARIO: User types 'Order rebar' in a day cell and presses Enter (or presses Enter and immediately clicks another cell). Two identical 'Order rebar' tasks are created for that date; because of the missing refresh (finding 1) the user doesn't even see it until later, then finds duplicated tasks in the project.
FIX: Guard the function: `if (creatingInline) return;` at the top of commitInlineTask, and/or clear newTaskName + addingForDate synchronously before awaiting the fetch so the blur path sees an empty name.
VERIFIER: CONFIRMED — full chain reproduced from source plus an empirical browser test. Evidence: (1) src/components/views/calendar-view.tsx:222-255 — commitInlineTask's only guard is `if (!name)` (:223-228); there is no `if (creatingInline) return`. Grep shows creatingInline is used only at :133 (useState), :229 (set true), :251 (set false), :848 (disabled prop) — no re-entrancy check anywhere. newTaskName is cleared only in the `finally` block (:253), i.e. after the awaited fetch settles. (2) The inline input (calendar-view.tsx:832-851) commits on both Enter (:838-841, calls commitInlineTask) AND onBlur (:847, calls commitInlineTask), with `disabled={creatingInline}` (:848). The input stays mounted …

### [medium] Dropping a dragged task on top of an existing task bar is rejected (no-drop cursor), so busy days are hard to drop onto
FILE: src/components/views/calendar-view.tsx:710 (area calendar)
SUMMARY: onDragOver/onDrop are attached only to the background day cells (calendar-view.tsx:655-659). The task bars live in a SIBLING absolute overlay (:695-733) — pointer-events-none container with pointer-events-auto <button draggable> bars — so dragover events over a bar bubble to the overlay/week div, never to the day cell, and nothing calls preventDefault there. Per HTML5 DnD, without preventDefault on dragover the drop is disallowed: the browser shows the no-drop cursor and the drop event never fires.
SCENARIO: User drags a task onto a day that already has tasks and releases the mouse while the cursor is over one of the existing bars (the natural 'put it with these' gesture — bars occupy the top of every populated cell). Nothing happens; the bar snaps back. The drop only works if they aim at the sliver of empty cell background.
FIX: Add onDragOver={e => { if (draggingTaskId) { e.preventDefault(); setDragOverDate(dateForPoint); } }} and onDrop on the week container (deriving the day from the pointer X / column), or give each bar/overlay cell the same dragOver/drop handlers as the underlying day cell.
VERIFIER: CONFIRMED. Evidence chain, all from src/components/views/calendar-view.tsx: (1) The only dragover/drop preventDefault calls in the file are in handleDayDragOver (:157-162, preventDefault at :159) and handleDayDrop (:164-172, preventDefault at :168). (2) These handlers are attached ONLY to the background day-cell divs at :655 (onDragOver) and :659 (onDrop), which live inside the background grid `<div className="grid grid-cols-7 h-full">` (:635). (3) The bars overlay is a SIBLING absolute div (:695-697) with `pointer-events-none`; each segment wrapper is also `pointer-events-none` (:708); the draggable `<button>` bar re-enables hits via `pointer-events-auto` (:720) but has only onDragStart/onD…

### [medium] Workspace OWNER/ADMIN/L4+ viewing a PRIVATE project gets a working page shell but every tab's data request 403s
FILE: src/app/api/projects/[projectId]/custom-fields/route.ts:85 (area cross-permissions)
SUMMARY: page.tsx:148-155 and /api/projects/[projectId] GET (route.ts:98-107) grant access to workspace OWNER/ADMIN or getLevel>=4 users for any project including PRIVATE ones. None of the tab sub-routes' assertProjectAccess variants contain that leadership clause — for a PRIVATE project a non-member leader falls through owner/member/PUBLIC and the WORKSPACE branch (visibility is PRIVATE) and hits `return { ok:false, status:403 }` (custom-fields/route.ts:85, values/route.ts:48, messages/route.ts:62, activity/route.ts:67, status-updates/route.ts:97, forms/route.ts:87, companies/route.ts:72, dependencies/route.ts:44, workflow/route.ts:49, objectives/route.ts:41, status-highlights/route.ts:66-68). The clients swallow the failures: list-view.tsx:219/:232 fetches custom-fields and values (columns silently never render), project-overview.tsx:498 fetches activity, messages-view.tsx:143 fetches messages.
SCENARIO: A PM (L4) or workspace ADMIN opens a PRIVATE project they are not an explicit member of — exactly the cross-project oversight case the page grants. The page renders, but custom-field columns are missing from List, Overview shows no activity/status/goals, Messages/Files/Workflow tabs error or render empty, with no explanation.
FIX: Include the leadership clause (workspace role OWNER/ADMIN or level>=4 in the project's workspace) in the shared project-access helper used by all sub-routes, matching page.tsx:148-155.
VERIFIER: CONFIRMED end-to-end. Evidence chain: (1) Grant side — src/app/(dashboard)/projects/[projectId]/page.tsx:138-155 and src/app/api/projects/[projectId]/route.ts:88-107 both grant access to a non-member if they are workspace OWNER/ADMIN or getLevel(position)>=4 in the project's workspace, explicitly including PRIVATE projects (comment: "see any project in the workspace"). So the page shell renders for a non-member leader. (2) Deny side — every tab sub-route uses a local access helper with only owner/member/PUBLIC/(WORKSPACE+wsMember) branches and no leadership clause; for a PRIVATE project a non-member leader falls through to 403: custom-fields/route.ts:58-85 (403 at :85), custom-fields/values/…

### [medium] Files tab endpoint leaks all attachment URLs workspace-wide and returns 500 instead of 403/404 on denial
FILE: src/app/api/projects/[projectId]/attachments/route.ts:20 (area cross-permissions)
SUMMARY: GET /api/projects/:id/attachments gates on verifyProjectAccess (attachments/route.ts:20), which is only workspace membership (auth-guards.ts:182-199), so any workspace member can enumerate every attachment (name, url, size, uploader at :51-61) of any project including PRIVATE ones the page 404s. Additionally the route imports getErrorStatus (:4) but its catch block (:64-70) never uses it — AuthorizationError/NotFoundError thrown by verifyProjectAccess fall through to a generic 500 'Failed to fetch attachments'. A cross-workspace visitor on a PUBLIC project (page grants access at page.tsx:134-136) therefore gets a 500 and files-view.tsx:143 renders the Files tab in its error/empty state.
SCENARIO: (1) A restricted L1-L3 user fetches /api/projects/P/attachments for a confidential project and gets direct download URLs for every file. (2) Any visitor of a PUBLIC project from another workspace opens the Files tab and it fails with a server error instead of listing files.
FIX: Use the shared project-access helper matching page.tsx, and add the `if (error instanceof AuthorizationError || error instanceof NotFoundError)` mapping (as members/route.ts:104-107 does) to the catch block.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Leak: GET route (src/app/api/projects/[projectId]/attachments/route.ts:20) gates only via verifyProjectAccess, which is defined at src/lib/auth-guards.ts:182-199 and does nothing but findUnique the project then verifyWorkspaceAccess (auth-guards.ts:7-15, a bare workspaceMember lookup) — it selects `visibility` (:189) but never uses it. Meanwhile the project page (src/app/(dashboard)/projects/[projectId]/page.tsx:124-159) enforces a stricter model: for a PRIVATE project (ProjectVisibility enum PRIVATE/WORKSPACE/PUBLIC, prisma/schema.prisma:576-580; field at :400) a workspace member who is not owner/projectMember and lacks role OWNER/ADMIN or …

### [medium] Project roles VIEWER/COMMENTER are never enforced on task or custom-field-value mutations
FILE: src/app/api/tasks/[taskId]/route.ts:237 (area cross-permissions)
SUMMARY: The app assigns per-project roles ADMIN/EDITOR/COMMENTER/VIEWER (members/route.ts:14, ProjectRole in schema) and gates project PATCH/forms/workflow/custom-field definitions on ADMIN|EDITOR (e.g. projects/[projectId]/route.ts:151-161, custom-fields/route.ts:88-96). But task mutations never read ProjectMember.role: PATCH /api/tasks/:id (:237), DELETE (:543), POST /api/tasks (tasks/route.ts:215), POST /api/sections (sections/route.ts:25), and PATCH /api/tasks/:id/custom-fields/:fieldId (tasks/[taskId]/custom-fields/[fieldId]/route.ts:46) all use verifyTaskAccess/verifyProjectAccess which check only workspace membership.
SCENARIO: A project admin adds an external reviewer as VIEWER expecting read-only access; the reviewer inline-edits a task name in the List view, changes due dates, deletes tasks, and overwrites custom-field values — all requests succeed because no route consults the VIEWER role.
FIX: In task mutation routes, load the caller's ProjectMember row for the task's project and reject write verbs for COMMENTER/VIEWER (and non-members without leadership), mirroring the canEdit() gate used in custom-fields/route.ts:88-96.
VERIFIER: CONFIRMED. Full chain reproduced from source: (1) ProjectRole enum ADMIN/EDITOR/COMMENTER/VIEWER exists (prisma/schema.prisma:582-587) and ProjectMember.role defaults to EDITOR (:458). (2) Roles are genuinely assignable: POST/PATCH /api/projects/:id/members accept role z.enum(["ADMIN","EDITOR","COMMENTER","VIEWER"]) (src/app/api/projects/[projectId]/members/route.ts:14, :343). (3) The role IS enforced on other mutations, proving intent: project PATCH requires owner or ADMIN|EDITOR member (src/app/api/projects/[projectId]/route.ts:151-161), custom-field definition creation uses canEdit() requiring ADMIN|EDITOR (src/app/api/projects/[projectId]/custom-fields/route.ts:88-96, :165), member manag…

### [medium] Tasks due today are counted as Overdue in the KPI card and donut chart for any user west of UTC
FILE: src/components/views/dashboard-view.tsx:114 (area dashboard)
SUMMARY: Due dates are stored as UTC midnight and serialized via t.dueDate.toISOString() (src/app/(dashboard)/projects/[projectId]/page.tsx:180), so a task due July 3 arrives as "2026-07-03T00:00:00.000Z". dashboard-view.tsx:113 runs parseISO(t.dueDate), which for a US user (e.g. EDT, UTC-4) yields a local Date of July 2, 8:00 PM. The overdue check at line 114, `isPast(dueDate) && !isToday(dueDate)`, then evaluates isPast=true (July 2 8PM is in the past) and isToday=false (local calendar date is July 2, not July 3), so the intended today-exclusion never fires. Every task due today is counted overdue for the entire day. This wrong count feeds the "Overdue tasks" KPI card (line 225), its danger highlight (line 228), and both the "Overdue" and "Incomplete" slices of the donut chart (lines 140-141, where Incomplete = incomplete - overdue is shrunk by the inflated overdue count).
SCENARIO: A US user opens the Dashboard tab on the morning of July 3 with 5 incomplete tasks due July 3. The "Overdue tasks" KPI shows 5 with the alert highlight, and the donut shows those 5 in the black "Overdue" slice, even though nothing is actually overdue until end of day.
FIX: Compare calendar dates instead of instants. Since dueDate is UTC midnight, the true due day is t.dueDate.slice(0, 10); mark overdue when that string is < format(new Date(), "yyyy-MM-dd") (local today). Alternatively normalize with new Date(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()) and compare to local start-of-day. (The same broken pattern exists in list-view.tsx:647-649/1132 and board-view.tsx:341-343/1040 — worth a shared helper.)
VERIFIER: CONFIRMED chain, reproduced from source: (1) Server serialization — src/app/(dashboard)/projects/[projectId]/page.tsx:180 `dueDate: t.dueDate?.toISOString() || null`, passed to ProjectContent (line 189), which renders DashboardView with those sections (src/components/projects/project-content.tsx:963-967). (2) UTC-midnight storage premise — confirmed for major write paths: POST /api/tasks does `new Date(data.dueDate)` (src/app/api/tasks/route.ts:256) and PATCH does the same (src/app/api/tasks/[taskId]/route.ts:334), while clients send date-only "yyyy-MM-dd" strings: quick-create-task-modal.tsx:237-240/258 (fmtIso), timeline-view.tsx:521/531 (`format(newDue, "yyyy-MM-dd")`), teams/[teamId]/cal…

### [medium] Dashboard silently computes all KPIs/charts from filters, search, and 'Show completed' set on other tabs, with no indicator and no way to clear
FILE: src/components/projects/project-content.tsx:965 (area dashboard)
SUMMARY: ProjectContent passes `filteredSections` (not project.sections) to DashboardView at project-content.tsx:964-967. filteredSections (lines 244-321) applies searchQuery, activeFilters (incomplete/completed/due_this_week/assigned_to_me) and the showCompleted toggle — state that persists across tab switches because ?view= navigation is a soft navigation that keeps ProjectContent mounted. But the toolbar containing the Filter/Sort/Options/Search controls and the "Clear all" button is rendered only when showToolbar is true, and showToolbar = ["list","board","timeline"].includes(currentView) (line 348), so on the Dashboard tab nothing indicates a filter is active and there is no control to clear it. DashboardView then renders these filtered numbers as absolute project statistics ("Total tasks", "Completed tasks", donut, per-section and per-assignee bars).
SCENARIO: User enables the "Incomplete tasks" filter (or types a search, or unchecks Show completed) on the List tab, then clicks the Dashboard tab. The dashboard reports "Completed tasks: 0" and a Total equal to only the filtered subset, the donut has no Completed slice, and the section/assignee charts undercount — with zero visual hint that a filter from another tab is applied and no button on the page to remove it.
FIX: Pass the unfiltered project.sections to DashboardView (matching Asana, where dashboards have their own widget-level filters), or alternatively render the toolbar / an active-filter chip with "Clear all" on the dashboard tab so the filtered basis is visible and reversible.
VERIFIER: CONFIRMED end-to-end. Evidence chain: (1) src/app/(dashboard)/projects/[projectId]/page.tsx:19,189 — server page reads ?view= and renders <ProjectContent currentView={view}/> with no key, so switching tabs via handleViewChange (project-content.tsx:323-325, router.push same route with new ?view=) is a soft App Router navigation: ProjectContent is reconciled in place and its useState survives. (2) Filter state lives in ProjectContent: searchQuery (line 195), activeFilters (200), showCompleted (203); nothing in handleViewChange or elsewhere resets them on tab change (clearAllFilters at 233-239 is only invoked from the toolbar's "Clear all" button at 909-914). (3) filteredSections (244-321) appl…

### [medium] Message attachments never appear in the Files tab despite the UI explicitly promising them
FILE: src/app/api/projects/[projectId]/attachments/route.ts:23 (area files)
SUMMARY: The Files tab empty state says 'All task and message attachments from this project will appear here' (src/components/views/files-view.tsx:277) and 'Upload files to tasks or messages and they'll be collected here automatically' (line 280). But the endpoint the tab fetches (files-view.tsx:143 -> GET /api/projects/[projectId]/attachments) only queries prisma.attachment with a mandatory task relation (route.ts:23-31: where task.OR projectId/section.projectId). Project message attachments are stored in a completely different model, MessageAttachment (prisma/schema.prisma:1101), created by POST /api/messages/[messageId]/attachments (src/app/api/messages/[messageId]/attachments/route.ts:60) when users attach files in the project Messages tab (messages-view.tsx uploads via endpoints.attachments, line 877). MessageAttachment is never queried by the project attachments route, so those files are pe…
SCENARIO: A user posts a message in the project's Messages tab and attaches a marked-up drawing PDF. They later open the Files tab to find it. The file is not there; if the project has no task attachments the tab even shows the empty state claiming message attachments 'will appear here'. The user concludes the upload was lost.
FIX: In the GET handler, additionally query prisma.messageAttachment.findMany({ where: { message: { projectId } } }), map the rows into the same response shape (taskId/taskName null, maybe a messageId source field), merge with the task attachments, and sort by createdAt desc. Update the delete handler in files-view.tsx to route message attachments to DELETE /api/messages/[messageId]/attachments/[attachmentId].
VERIFIER: Chain reproduced end-to-end. (1) UI promise: src/components/views/files-view.tsx:277 empty state reads "All task and message attachments from this project will appear here" and line 280 "Upload files to tasks or messages and they'll be collected here automatically." (2) Fetch: files-view.tsx:143 — the tab's only data source is GET /api/projects/${projectId}/attachments; no other fetch in the component. (3) API: src/app/api/projects/[projectId]/attachments/route.ts:23-41 queries only prisma.attachment.findMany with a mandatory `task: { OR: [{ projectId }, { section: { projectId } }] }` relation filter; MessageAttachment is never touched (grep of src/app/api confirms messageAttachment is only …

### [medium] Any fetch failure on the Files tab silently renders the 'no files' empty state instead of an error
FILE: src/components/views/files-view.tsx:144 (area files)
SUMMARY: In the fetch effect (files-view.tsx:140-155), a non-ok response is simply ignored (`if (res.ok)` with no else branch) and a thrown network error is only console.error'd. Either way `files` stays [] and `loading` becomes false, so line 199-201 renders <EmptyState/> — a screen that affirmatively tells the user the project has no attachments. There is no error state, no toast, and no retry. This is guaranteed to trigger whenever the server errors, including the 500 the route currently returns for authorization failures (see separate finding).
SCENARIO: A project has 40 attachments. A transient DB hiccup (or any 500 from the attachments route) occurs while the user opens the Files tab. Instead of an error, the user sees 'All task and message attachments from this project will appear here' — i.e. the app asserts the project has zero files. In a construction firm this reads as data loss.
FIX: Add an `error` state: set it when `!res.ok` or in the catch block, and render an error panel with a Retry button (re-run fetchFiles) instead of EmptyState. Only show EmptyState when the request succeeded with an empty array.
VERIFIER: CONFIRMED — full chain reproduced from source. Client: src/components/views/files-view.tsx:140-155 — fetch(`/api/projects/${projectId}/attachments`) at line 143; line 144 `if (res.ok)` has no else branch, so a non-ok response is silently discarded; the catch (148-149) only does console.error; finally (150-152) sets loading=false. State: `files` initialized to [] at line 137 and never touched on failure. Render: lines 191-201 — once loading is false, `if (files.length === 0) return <EmptyState/>`, with no error state variable anywhere in the component. EmptyState (lines 271-285) affirmatively says "All task and message attachments from this project will appear here" / "Upload files to tasks o…

### [medium] Public submit endpoint accepts forged attachment objects that become owner-attributed Task attachments
FILE: src/app/api/forms/[formId]/submit/route.ts:299 (area forms)
SUMMARY: In the JSON path, POST /api/forms/:id/submit takes `answers` verbatim (lines 166-178). pruneHiddenAnswers only filters by field id/visibility, not value shape. For ATTACHMENT fields, filesToMirror collects any object matching {name,url,size} from the answers (lines 292-302) with no verification that the URL came from uploadFile, then creates real Attachment rows with `uploaderId` set to the project OWNER for anonymous submissions (lines 290-291, 335-346). The same forged objects are stored in FormSubmission.data and rendered as clickable <a href={att.url}> links in the submissions dialog (form-submissions-dialog.tsx:220-231), the print page (print/page.tsx:159), and the task's Attachments panel.
SCENARIO: An anonymous attacker POSTs JSON to a PUBLIC form with answers[attachmentFieldId] = [{"name":"Revised_Drawing.pdf","url":"https://evil.example/phish","size":48211,"mimeType":"application/pdf"}]. A task is created with an attachment named Revised_Drawing.pdf attributed to the project owner as uploader; team members click it from the task panel or submissions inbox and land on a phishing page (or a javascript: URL depending on renderer). No file was ever uploaded.
FIX: For ATTACHMENT fields, ignore attachment-shaped values supplied in the answers JSON; only accept files actually uploaded via the multipart path (i.e., set answers[fieldId] exclusively from the uploadFile results), or at minimum validate that URLs point at the app's own blob-storage host.
VERIFIER: CONFIRMED. Full chain reproduced against source. 1) Anonymous acceptance: submit/route.ts:90-99 only rejects anonymous when form.visibility === "ORGANIZATION". A PUBLIC form is accepted with submitterUserId = null. 2) JSON path takes answers verbatim: route.ts:167-177 — body.answers is cast straight to FormSubmissionPayload with no value-shape validation. (The multipart uploadFile path at 132-165 is the only one that actually verifies files; the JSON path bypasses it entirely.) 3) pruneHiddenAnswers does NOT validate value shape: src/lib/form-types.ts:186-198 only filters by field presence/visibility (isFieldVisible) and copies answers[f.id] through unchanged. 4) Forged attachment objects ar…

### [medium] ORGANIZATION-visibility forms are fully readable (and fillable) by anonymous visitors
FILE: src/app/api/forms/[formId]/route.ts:143 (area forms)
SUMMARY: The public branch of GET /api/forms/:formId (lines 143-165) performs no auth and no visibility check — it returns name, description, and the complete fields definition for ORGANIZATION forms too. Only the submit endpoint enforces visibility (submit/route.ts:91-99). The builder UI promises "Only members of your organization can access and submit this form" (form-builder-dialog.tsx:656-659) and the public page merely shows a passive "sign-in required" pill (forms/[formId]/page.tsx:406-411) with no login redirect, while rendering the whole form for anonymous users.
SCENARIO: An admin sets a form to Organization-only, assuming the link is safe to circulate. Anyone without an account opens the link, sees every question/option/help text (which may reveal internal process details), fills the entire form, and only on Submit gets a 401 toast "restricted to signed-in members" — with no way to sign in from the page.
FIX: In the public GET branch, when form.visibility === "ORGANIZATION", require getCurrentUserId() (and optionally workspace membership) before returning the definition; on the page, redirect to /login?callbackUrl=/forms/:id when the API returns 401.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) src/app/api/forms/[formId]/route.ts:99-165 — GET has two branches; the non-settings (public) branch at lines 143-165 runs prisma.form.findUnique and returns { id, name, description, fields, isActive, projectId, confirmationMessage, visibility } with NO getCurrentUserId() call and NO visibility check — the only gates are 404 (line 156) and !isActive→410 (line 159). ORGANIZATION forms are returned to anonymous callers identically to PUBLIC ones. No middleware.ts exists in the app to compensate (only node_modules matches), and getCurrentUserId is imported (line 5) but used solely in the ?settings=1 branch (line 111) and PATCH/DELETE. (2) Visibi…

### [medium] Cover image feature is write-only: never returned by any endpoint, never rendered, and silently deleted on every re-save
FILE: src/components/views/form-builder-dialog.tsx:417 (area forms)
SUMMARY: The builder persists settings.coverImageUrl via POST/PATCH (form-builder-dialog.tsx:417, 456-483) and PATCH stores it ([formId]/route.ts:244-247). But no read path returns it: the Workflow list endpoint (api/projects/[projectId]/forms/route.ts:127-144) omits `settings`, both branches of GET /api/forms/:formId omit it (lines 126-140 and select at 143-155), and the public form page never renders a cover at all (forms/[formId]/page.tsx has no reference). So hydration `initial.settings?.coverImageUrl` (form-builder-dialog.tsx:221-223) is always undefined → coverImageUrl state resets to null. Because handleSave ALWAYS sends `settings: { coverImageUrl: coverImageUrl || null }` (line 417) and PATCH replaces the whole settings bag, any subsequent "Save changes" overwrites the stored cover with null.
SCENARIO: User pastes a cover URL, sees "Cover image saved", closes the dialog. Reopening the form shows the empty striped placeholder; the public form never shows the image; and editing any field then saving erases the stored URL from the DB. The entire cover-image UI (modal, preview, toasts) is a no-op presented as working.
FIX: Include `settings` in the GET /api/projects/:id/forms and GET /api/forms/:id?settings=1 responses (and in the PATCH response), and render settings.coverImageUrl on the public form page — or remove the cover UI until wired.
VERIFIER: CONFIRMED — every link in the chain reproduces from source. WRITE PATHS WORK: POST /api/forms accepts settings (src/app/api/forms/route.ts:49-55) and stores it (route.ts:205-207); PATCH /api/forms/:formId accepts settings ([formId]/route.ts:61-72) and stores it, replacing the ENTIRE Json bag (`data.settings = (p.settings ?? Prisma.JsonNull)`, [formId]/route.ts:244-247). The `settings Json?` column exists in prisma/schema.prisma:1410, so persistence itself succeeds. NO READ PATH RETURNS IT: (1) GET /api/projects/:id/forms response object omits settings (src/app/api/projects/[projectId]/forms/route.ts:128-143) — directly contradicting its own doc comment at line 8 which claims "Returns the ful…

### [medium] Deleting a form from the builder's More menu leaves the deleted form in the Workflow list
FILE: src/components/views/form-builder-dialog.tsx:525 (area forms)
SUMMARY: handleDelete in the dialog (lines 510-531) DELETEs /api/forms/:id and only calls onOpenChange(false); the comment admits "the parent should refetch when the dialog closes" — but the parent doesn't: workflow-view's onOpenChange (lines 679-686) just sets formDialogOpen=false and editingForm=null, with no refetch and no setForms filter. The workflow list keeps the deleted form until a full page reload.
SCENARIO: User opens Edit form → More → Delete form → confirms. Toast says "Form deleted" and the dialog closes, but the form card is still in the Intake forms list. Clicking Preview shows "Form not found"; clicking Edit reopens the editor whose Save then fails with "Form not found" (PATCH 404); clicking Inbox errors loading submissions.
FIX: Add an onDeleted(formId) callback prop to FormBuilderDialog and have workflow-view remove the form from state (or refetch the list) when it fires.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Dialog delete path: src/components/views/form-builder-dialog.tsx — More menu "Delete form" item at line 628 calls handleDelete (510-531), which after window.confirm does fetch DELETE `/api/forms/${initial.id}` and on success only toasts and calls onOpenChange(false); the comment at lines 507-509 explicitly says "the parent should refetch when the dialog closes". The dialog's props interface (lines 106-110) exposes only onOpenChange and onSaved — there is no onDeleted, and onSaved is called only from the save path (line 432), never from delete. (2) API: src/app/api/forms/[formId]/route.ts:299 does a hard `prisma.form.delete`, so the form is t…

### [medium] Renaming a dropdown option orphans dependent showWhen rules — conditional fields become permanently hidden on the public form
FILE: src/components/views/form-builder-dialog.tsx:1402 (area forms)
SUMMARY: OptionsEditor edits only field.options (form-builder-dialog.tsx:1398-1419); nothing updates other fields whose showWhen.equals references the old option string (rules are created from option text at lines 1460-1469). At runtime, isFieldVisible (form-types.ts:154-180) compares showWhen.equals against the answer; since the old string no longer exists as an option, the condition can never be true, so the dependent field is never shown (page.tsx:116-121) and its answer is pruned server-side (submit/route.ts:200). The BranchingEditor's equals Select also renders blank because its value isn't among the current options.
SCENARIO: User builds "Request type" dropdown with option "RFI" and makes "Drawing reference" show only when Request type = RFI. Later they rename the option to "RFI Request" and save. From then on, submitters can never see or answer "Drawing reference" no matter what they pick, and no error or warning surfaces anywhere.
FIX: When an option is renamed in OptionsEditor, propagate the rename to every field whose showWhen.fieldId matches and whose equals contains the old value; when an option is deleted, clear or flag dependent rules.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Rename edits only the option array: OptionsEditor input onChange (src/components/views/form-builder-dialog.tsx:1400-1406) calls onChange(next) -> onUpdate({ options: opts }) (line 1319-1321) -> updateField (lines 326-333), which patches ONLY the field with matching id; no other field's showWhen is touched. (2) Rules are string-coupled to option text: BranchingEditor sets equals from option strings (lines 1463-1467, 1480-1484, 1505-1507), and its equals <Select> at 1499-1522 lists only the dep field's CURRENT options, so a stale equals value matches no SelectItem and Radix renders an empty trigger — also as claimed. (3) The codebase handles t…

### [medium] POST /api/forms lets COMMENTER/VIEWER project members create forms, bypassing the role check the project-scoped endpoint enforces
FILE: src/app/api/forms/route.ts:162 (area forms)
SUMMARY: The builder dialog creates forms via POST /api/forms, whose access check is only `OR: [{ownerId: userId}, {members: {some: {userId}}}]` (lines 159-168) — any ProjectMember role qualifies, including COMMENTER and VIEWER (schema.prisma:582-587). The parallel POST /api/projects/:id/forms requires ADMIN/EDITOR via canEditForms (api/projects/[projectId]/forms/route.ts:90-98, 172-180), as do PATCH and DELETE on /api/forms/:id ([formId]/route.ts:90-95). So a read-only member can create live public intake forms they then cannot edit or delete.
SCENARIO: A VIEWER-role member opens the Workflow tab, clicks "New form", and successfully creates a PUBLIC form that auto-creates tasks in the project — despite having a read-only role. When they try to fix a typo in it, PATCH returns 403 "You don't have permission to edit this form.", leaving an uneditable live form in the project.
FIX: Apply the same ADMIN/EDITOR/owner role check in POST /api/forms (mirror canEditForms) before creating.
VERIFIER: CONFIRMED. Full chain reproduced from source: (1) Client — form-builder-dialog.tsx:419-421: create path is `POST /api/forms` (PATCH /api/forms/:id only when editing); workflow-view.tsx:473 renders the "New form" button with no role gating (no canEdit/role check anywhere in the file), so a VIEWER/COMMENTER member sees and can use it. (2) Server — src/app/api/forms/route.ts:159-174: the only access check before `prisma.form.create` (line 194) is `project.findFirst({ where: { id, OR: [{ownerId: userId}, {members: {some: {userId}}}] } })` — membership existence only, role ignored. (3) prisma/schema.prisma:582-587: ProjectRole enum = ADMIN | EDITOR | COMMENTER | VIEWER, so read-only roles satisfy…

### [medium] Tasks cannot be dragged into an empty section - no droppable is registered for section containers
FILE: src/components/views/list-view.tsx:858 (area list)
SUMMARY: List rows are the only droppables (via useSortable). SortableContext (list-view.tsx:858-862) does not register a droppable for the section itself, and no useDroppable is used anywhere in list-view.tsx. kanbanCollisionDetection (kanban-collision-detection.ts:26-31) filters droppables into "columns" (containers without sortable data) - in the List view that set is always empty, so it falls through to closestCenter over task rows only (line 73). An empty section therefore has zero drop targets; handleDragOver/handleDragEnd can never resolve it as a destination. (Collapsed sections are also unreachable, but that is expected; empty EXPANDED sections are not.)
SCENARIO: User creates a new section, then tries to drag an existing task into it. The drag never highlights the empty section and dropping over it returns the task to its origin. The only workaround is the bulk 'Move to' action.
FIX: Wrap each section's task area in a useDroppable with id = section.id (matching the Board view pattern) so kanbanCollisionDetection's column path and handleDragOver's `s.id === overId` lookup work for empty sections.
VERIFIER: CONFIRMED. Evidence chain: (1) list-view.tsx imports only DndContext/SortableContext/useSortable (lines 74-86) — grep confirms useDroppable appears nowhere in the file; the only useDroppable in src/components is board-view.tsx:624 (the Board column pattern the fix cites). (2) The only droppables registered in List view are task rows via useSortable({ id: task.id }) in SortableTaskRow (list-view.tsx:1292); SortableContext at 858-862 (items = section.tasks ids, id = section.id) registers no droppable for the section container itself — for an empty section, items is [] and the section contributes zero droppables. (3) DndContext (line 665-671) uses kanbanCollisionDetection; in kanban-collision-d…

### [medium] Bulk Complete skips goal-progress recalc, workflow rules, and notifications, and overwrites completedAt on already-completed tasks
FILE: src/app/api/tasks/bulk/route.ts:30 (area list)
SUMMARY: The single-task PATCH path fires GoalProgressService.recalculateForTask (tasks/[taskId]/route.ts:425-432), executeRulesOnTaskCompleted (455-462) and notifyTaskCompleted (492-505) on completion. The bulk endpoint used by the List tab's floating bar (handleBulkAction 'complete', list-view.tsx:450-470) just does updateMany({ completed: true, completedAt: new Date() }) (bulk/route.ts:30-33): no goal progress update, no workflow rules, no notifications, no Activity rows. It also stamps completedAt = now on tasks that were ALREADY completed, destroying their original completion dates (shown in the pinned 'Completion date' built-in column).
SCENARIO: User selects 10 tasks (2 already done) and clicks Complete in the bulk bar. Linked goal progress does not move, project workflow automations that should fire on completion never run, the task creators get no notification, and the 2 previously-done tasks now show today as their completion date.
FIX: In the bulk complete branch, filter to tasks where completed=false before updating, then run the same post-completion side effects as the single PATCH (goal recalc, executeRulesOnTaskCompleted, notifyTaskCompleted, activity rows) per task.
VERIFIER: CONFIRMED end-to-end. Chain: (1) Client — src/components/views/list-view.tsx:1043 bulk bar button calls handleBulkAction("complete"); handleBulkAction (450-470) POSTs {taskIds, action} to /api/tasks/bulk (453-461) and just router.refresh()es — no client-side compensation. (2) API — src/app/api/tasks/bulk/route.ts:30-33 "complete" branch is exactly `prisma.task.updateMany({ where: { id: { in: taskIds } }, data: { completed: true, completedAt: new Date() } })` and returns; no `completed: false` filter in the where, so already-completed tasks get completedAt overwritten with now. The route (full file, 112 lines) contains no Activity creation, no GoalProgressService call, no workflow-rule execut…

### [medium] Inline custom-field edits never refresh the list's value map, so the SUMA footer and formula columns show stale numbers
FILE: src/components/views/list-view.tsx:957 (area list)
SUMMARY: EditableCustomFieldCell PATCHes /api/tasks/:id/custom-fields/:fieldId and updates only its own local `optimistic` state (editable-custom-field-cell.tsx:95-116); list-view mounts it without an onChange (list-view.tsx:1756-1762) and the cell never calls router.refresh. The parent's customFieldValues map is refetched only when the `sections` prop identity changes (list-view.tsx:250-253), which requires some OTHER mutation to call router.refresh. The per-section SUMA footer sums from that stale map (943-1003), and the server-side formula recompute triggered by the edit (custom-fields/[fieldId]/route.ts:296-307) is likewise never pulled down, so FORMULA/ROLLUP columns keep old results.
SCENARIO: Project has a NUMBER column 'Cost' with a SUMA footer of 500. User edits one task's Cost from 100 to 200 inline - the cell shows 200 but the footer still reads 500 (and any formula column depending on Cost still shows the old computed value) until the user performs an unrelated mutation or reloads the page.
FIX: Pass an onChange from list-view to EditableCustomFieldCell that updates customFieldValues[taskId][fieldId] in state, and call reloadCustomFieldValues() after a successful commit to pick up recomputed formulas.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) list-view.tsx:1749-1762 mounts EditableCustomFieldCell with only taskId/fieldId/type/options/value — the optional `onChange` prop (editable-custom-field-cell.tsx:74-81) is NOT passed. (2) editable-custom-field-cell.tsx:95-116 `commit()` PATCHes /api/tasks/:taskId/custom-fields/:fieldId and only sets local `optimistic` state (plus calling the undefined onChange); the file imports no router and never calls router.refresh (verified by reading the entire 451-line file). (3) The parent's `customFieldValues` map (list-view.tsx:204-206) is populated by `reloadCustomFieldValues` (229-241) which runs only on mount (243-246) and when the `sections` pr…

### [medium] Newly created sections render collapsed, and the section menu's 'Add task' silently does nothing for a collapsed section
FILE: src/components/views/list-view.tsx:840 (area list)
SUMMARY: expandedSections is initialized once from the sections present at mount (list-view.tsx:180-182) and is never reconciled when new sections arrive via router.refresh, so a section created with 'Add section' (handleAddSection, 538-558) is not in the set and renders collapsed. Separately, the section dropdown's 'Add task' item (line 840) only sets addingTaskInSection, but the inline add-task input is rendered inside the `expandedSections.has(section.id) &&` branch (857-1008) - for a collapsed section the click has no visible effect at all.
SCENARIO: User clicks 'Add section': the new section appears collapsed with its 'Add task...' row hidden. User opens the collapsed section's ... menu and clicks 'Add task': nothing happens on screen; they must first notice the chevron and expand the section manually.
FIX: When sections prop changes, add unknown section ids to expandedSections (or default unknown ids to expanded); and in the 'Add task' menu handler also add the section id to expandedSections before setting addingTaskInSection.
VERIFIER: CONFIRMED, both halves reproduced from source. Chain: (1) expandedSections initialized once from mount-time sections (list-view.tsx:180-182); the ONLY other setExpandedSections call in the file is toggleSection (472-480, user click) — no useEffect reconciles it with the sections prop (grep of setExpandedSections/useEffect confirms; effects at 243, 250, 284 touch only custom fields and localSections). (2) handleAddSection (538-558) POSTs /api/sections then router.refresh(); parent project-content.tsx:934-939 renders <ListView sections={filteredSections}> with no key prop, so router.refresh updates props without remounting — state persists, and the new section id is absent from expandedSection…

### [medium] Bulk endpoint authorizes projectless tasks for ANY authenticated user - verifyBulkTaskAccess skips the workspace check when task.project is null
FILE: src/lib/auth-guards.ts:233 (area list)
SUMMARY: verifyBulkTaskAccess (auth-guards.ts:218-240) only rejects tasks whose project.workspaceId differs from the caller's workspace; tasks with project == null (personal/My-Tasks tasks) pass for any caller. The single-task guard verifyTaskAccess handles this case correctly by requiring creatorId or assigneeId to match (143-171). POST /api/tasks/bulk (used by the List bulk bar) and POST /api/tasks/reorder both rely on verifyBulkTaskAccess, so a user who obtains another user's projectless task ids (e.g. from a shared link or log) can bulk delete/complete/reprioritize them across workspace boundaries.
SCENARIO: User B in another workspace learns the id of user A's personal (projectless) task and POSTs /api/tasks/bulk {taskIds:[id], action:"delete"} - the task is deleted even though B has no relationship to it; the same ids also pass /api/tasks/reorder's access gate.
FIX: In verifyBulkTaskAccess, for tasks with no project require creatorId === userId || assigneeId === userId (same rule as verifyTaskAccess), selecting those columns in the findMany.
VERIFIER: CONFIRMED. Full chain reproduced from source: (1) Projectless tasks exist by design — prisma/schema.prisma:673 `projectId String?` and :680 `project Project?` on model Task (:654); these are the My-Tasks personal tasks. (2) src/lib/auth-guards.ts:218-240 verifyBulkTaskAccess resolves the CALLER's workspace (:219) then loops tasks with `if (task.project && task.project.workspaceId !== workspaceId) throw` (:233-236) — a task with project == null short-circuits the condition and passes with no creator/assignee check; the only other gate is existence (:229). The single-task guard verifyTaskAccess handles the same case correctly at :166-172 (`if (!task.project) { if (task.creatorId !== userId && …

### [medium] Attachments on thread replies cannot be opened — viewer only searches root messages
FILE: src/components/views/messages-view.tsx:1454 (area messages)
SUMMARY: Clicking an attachment on a reply calls onReplyOpenAttachment which does setViewer({ messageId: replyId, index }) (messages-view.tsx:1313-1315, wired from MessageItem's onOpenAttachment at 1935-1937). But the viewer render block at 1452-1470 resolves the message with `messages.find((x) => x.id === viewer.messageId)` — `messages` holds only root messages; replies live in the separate `repliesByThread` map (line 279-281). The find returns undefined and the IIFE returns null, so FileViewerModal never mounts.
SCENARIO: User expands a thread, a reply has an image or PDF attachment, user clicks the attachment thumbnail → nothing happens at all (no modal, no error). The only workaround is the small hover Download button.
FIX: In the viewer resolution, fall back to searching repliesByThread: `const m = messages.find(...) ?? Object.values(repliesByThread).flat().find((x) => x.id === viewer.messageId);`
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Reply attachments render a clickable button: MessageItem's attachment grid (src/components/views/messages-view.tsx:1748-1760) renders for every message including replies, and the button's onClick calls onOpenAttachment(idx) at line 1757. (2) For replies rendered inside an expanded thread, MessageItem wires onOpenAttachment={(idx) => onReplyOpenAttachment?.(r.id, idx)} at lines 1935-1937. (3) The parent wires onReplyOpenAttachment={(replyId, idx) => setViewer({ messageId: replyId, index: idx })} at lines 1313-1315. (4) The viewer render block at lines 1452-1470 resolves via `const m = messages.find((x) => x.id === viewer.messageId)` (line 145…

### [medium] "Copy link" produces a #message-<id> hash URL that no code (and no deep-link handler) consumes
FILE: src/components/views/messages-view.tsx:1234 (area messages)
SUMMARY: copyLink builds `${location.href.split("#")[0]}#message-${id}` (line 1233-1237). The only deep-link mechanism in the component reads the `?message=` / `?thread=` query params (lines 241-243, 643-685); nothing anywhere in src handles location.hash or hashchange (grep confirms the only `#message-` occurrence is copyLink itself). Native browser hash-scroll also fails because the `message-<id>` element is rendered only after the client-side fetch completes. For replies inside collapsed threads the target element is not even in the DOM.
SCENARIO: User clicks the Link2 'Copy link' button on a message, pastes it to a colleague. Colleague opens it: the messages tab loads scrolled to the bottom with no scroll-to or highlight of the linked message; for a reply, the message is entirely invisible until the thread is manually expanded.
FIX: Build the same URL format the inbox uses: keep pathname + `?view=messages&message=<id>` (plus `&thread=<rootId>` for replies, which copyLink would need to receive), instead of the hash fragment.
VERIFIER: CONFIRMED — reproduced the full chain in src/components/views/messages-view.tsx. (1) copyLink at lines 1233-1237 builds `${window.location.href.split("#")[0]}#message-${messageId}` and copies it; it is wired to both root messages (onCopyLink, line 1280 -> button onClick line 1664) and thread replies (onReplyCopyLink line 1312 -> line 1934). (2) The only deep-link consumer reads query params, not the hash: lines 241-243 read searchParams.get("message")/get("thread"), and the effect at lines 643-685 does getElementById(`message-${target}`) + scrollIntoView/highlight — the comment at 238-239 explicitly documents the inbox format as `?view=messages&message=ID(&thread=ROOTID)`. (3) Grep across al…

### [medium] Sending only files can create an orphan "📎" message with zero attachments — storage MIME allowlist rejects common engineering files with a generic 500
FILE: src/components/views/messages-view.tsx:923 (area messages)
SUMMARY: The composer accepts any file type (handleSelectFiles at 1020-1034 checks only the 10MB size). handleSend first POSTs the message — using placeholder content "📎" when there is no text (line 923) — then uploads each file to /api/messages/:id/attachments. The attachments route calls uploadFile (src/lib/storage.ts:40-63) which throws for any MIME type outside a narrow allowlist (storage.ts:5-25) — DWG/DXF/RVT and any file with an empty `file.type` (common for CAD formats on Windows) are rejected. The route's catch returns a generic 500 "Failed to upload attachment" (attachments/route.ts:81-89), which the client just toasts per-file (messages-view.tsx:894-899) with no retry and no removal of the already-created message.
SCENARIO: At a civil/structural firm, a user attaches drawing.dwg with no text and hits send. A message containing just "📎" is permanently posted to the channel, every upload fails with the unexplaining toast "drawing.dwg: Failed to upload attachment", and the channel is left with a contentless paperclip message.
FIX: Pre-validate staged files against the server allowlist in handleSelectFiles (or add an accept attribute + explicit type check) with a clear message; extend ALLOWED_MIME_TYPES with the CAD formats the firm actually uses (and handle empty file.type by extension); on total upload failure of a files-only message, delete or flag the placeholder message. Also return 400 (not 500) from the attachments route for validation failures.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Client-side staging: handleSelectFiles (src/components/views/messages-view.tsx:1020-1034) checks ONLY the 10MB size (lines 1026-1030); no type check, and neither hidden file input has an `accept` attribute (messages-view.tsx:1411-1416 and 1856-1861), so DWG/DXF/RVT stage freely. (2) Placeholder message: handleSend line 923 sets `safeContent = content || (files.length > 0 ? "📎" : "")`, POSTs the message first (lines 968-975), and only then fires attachment uploads via Promise.all(uploadFileToMessage) (lines 990-993). (3) Server rejection: attachments route (src/app/api/messages/[messageId]/attachments/route.ts:58) calls uploadFile; src/lib/s…

### [medium] Message history hard-capped at 100 — older messages unreachable and old pinned messages vanish
FILE: src/app/api/projects/[projectId]/messages/route.ts:90 (area messages)
SUMMARY: GET /api/projects/:id/messages fetches only the newest 100 root messages (`take: 100`, orderBy createdAt desc, route.ts:87-90). The client (fetchMessages, messages-view.tsx:349-461) has no cursor/load-more UI — it just reverses and renders the array. Pinning is done client-side by sorting pinned rows to the top (messages-view.tsx:1240-1244), so once a pinned message ages past the 100 newest roots it drops out of the payload and disappears from the UI entirely, defeating the purpose of pinning.
SCENARIO: An active project channel exceeds 100 root messages. A safety notice pinned three months ago silently vanishes from the top of the feed, and no one can scroll back to messages older than the newest 100 by any means.
FIX: Implement the `before` cursor the route comment already promises, add a 'Load older messages' control at the top of the list, and include pinned messages unconditionally in the GET (e.g., a second query `where: { projectId, parentMessageId: null, isPinned: true }` merged into the response).
VERIFIER: CONFIRMED — full chain reproduced. (1) Server: src/app/api/projects/[projectId]/messages/route.ts GET signature is `(_req: Request, ...)` (line 66) — the request object is never read, so no `before`/cursor/limit query param exists; the query at lines 87-90 is `where: { projectId, parentMessageId: null }, orderBy: { createdAt: "desc" }, take: 100` with no secondary query for pinned rows; the doc comment (lines 17-19) admits pagination is "paginatable later via a `before` cursor", i.e. unimplemented. (2) Client: src/components/views/messages-view.tsx builds `list: /api/projects/${pid}/messages` with no params (line 143), fetchMessages does a bare `fetch(endpoints.list)` (line 352) and reverses…

### [medium] Thread replies poller races the optimistic reply — a just-sent reply can disappear for up to 10 seconds
FILE: src/components/views/messages-view.tsx:710 (area messages)
SUMMARY: The per-thread poll (lines 710-742) calls fetchReplies for every expanded thread every 10s and on visibilitychange, with no guard equivalent to the sendingRef check the root poll has (line 482). fetchReplies replaces the thread list wholesale: `setRepliesByThread(prev => ({...prev, [rootId]: data}))` (line 632). If a tick fires while a reply POST is in flight (or the GET was already in flight when the send started), the server response predates the new reply, so the optimistic `temp-` row is wiped; when the POST then resolves, the swap `list.map(m => m.id === tempId ? created : m)` (lines 823-829) finds no temp row and the created reply is never inserted. The same class of race exists for root sends when a silent GET was already in flight before handleSend started (the merge at 392-396 re-appends only `temp-` prefixed rows, so a freshly swapped real-id message gets dropped by a stale GET…
SCENARIO: User has a thread expanded, hits Enter to send a reply just as the 10s poll ticks (or right after re-focusing the tab, which triggers an immediate tick). Their reply visibly appears, then vanishes, and only re-appears on the next poll ~10s later — looking like the send was lost.
FIX: Skip fetchReplies for threads present in the replySending set (and check a ref for it inside the tick), and in the POST resolution append `created` if the temp row is no longer in the list instead of relying solely on map-replace. For the root case, ignore GET responses issued before the most recent send (e.g., a request-sequence counter).
VERIFIER: CONFIRMED. Chain reproduced in src/components/views/messages-view.tsx: (1) replies poll effect 710-742 — tick() at 712-717 calls fetchReplies for every expanded thread with only a document.hidden check; no replySending guard (root poll has `if (sendingRef.current) return;` at 482, ref defined 337/synced 342). visibilitychange handler fires tick() immediately on refocus (728-734), matching the scenario. (2) fetchReplies 626-638 replaces the thread list wholesale at 632 (`setRepliesByThread(prev => ({...prev, [rootId]: data}))`) with no temp-row preservation, unlike the root silent merge that re-appends `temp-` rows at 392-396. (3) handleSendReply: optimistic temp insert 763/785-788, POST 809-…

### [medium] Structured status-update sections are unviewable when a project has 3 or fewer updates
FILE: src/components/projects/project-overview.tsx:1297 (area overview)
SUMMARY: Full section rendering (Accomplished / Blocked / Next steps blocks) only happens when historyExpanded is true (project-overview.tsx:1232-1247); collapsed cards show only the SUMMARY teaser with line-clamp-3 (1248-1266). But the only toggle for historyExpanded renders exclusively when statusUpdates.length > 3 (1297-1307). With 1-3 updates there is no way to expand, so everything the author wrote outside the Summary block (and anything past 3 lines) is permanently invisible on the Overview.
SCENARIO: A PM posts the project's first status update with detailed 'What's blocked' and 'Next steps' sections. Teammates open the Overview and see only the 3-line summary teaser; the blocked/next-steps content they were meant to read is nowhere in the UI until a 4th update exists and someone clicks 'Show all'.
FIX: Add a per-card expand/collapse (or always render filled sections) independent of the 'Show all N updates' pagination toggle.
VERIFIER: CONFIRMED. Chain: (1) project-overview.tsx:305 — historyExpanded = useState(false); the only setter in the codebase is the toggle at line 1300. (2) Lines 1232-1247 — Accomplished/Blocked/Next-steps blocks render only when historyExpanded is true. (3) Lines 1248-1266 — collapsed cards show a single teaser section (SUMMARY-type preferred, else first non-empty) with line-clamp-3; the `|| u.summary` fallback never fires for section-bearing updates because POST filters empty sections before persist (api/projects/[projectId]/status-updates/route.ts:216), so all stored sections are non-empty. (4) Lines 1297-1307 — the toggle button renders only when statusUpdates.length > 3; with 1-3 updates histor…

### [medium] Milestone due dates in the 'Next steps' prefill shift back one day for US users (UTC-midnight dates rendered in local time)
FILE: src/components/projects/project-overview.tsx:354 (area overview)
SUMMARY: buildNextStepsFromHighlights formats m.dueDate with new Date(m.dueDate).toLocaleDateString('en-US', {month, day}) (project-overview.tsx:353-358). Due dates are stored as UTC midnight (Task.dueDate; the highlights API returns dueDate.toISOString(), status-highlights/route.ts:184), so for any US timezone (UTC-4..-7) the Date resolves to 5pm-8pm the previous day and the formatted label is one day early. This wrong date is written into the composer's 'Next steps' block and gets posted verbatim in the status update the whole team reads.
SCENARIO: A milestone 'Foundation pour' is due July 10. A PM in Florida opens the composer; the Next steps block prefills '→ Foundation pour (due Jul 9)'. They post it, and the team plans around the wrong day.
FIX: Format with the UTC calendar date, e.g. toLocaleDateString('en-US', { month: 'short', day: 'numeric', timeZone: 'UTC' }).
VERIFIER: CONFIRMED — full chain reproduced from source, with one premise correction (storage is UTC-midnight only on SOME write paths, which is enough to trigger the bug). Evidence chain: 1. Format bug (cited line): src/components/projects/project-overview.tsx:353-358 — buildNextStepsFromHighlights does `new Date(m.dueDate).toLocaleDateString("en-US", { month: "short", day: "numeric" })` with NO timeZone option, so the label uses the browser's local calendar day. The result is spliced into the NEXT_STEPS composer section at project-overview.tsx:412-415 and posted verbatim. 2. API returns raw ISO instant: src/app/api/projects/[projectId]/status-highlights/route.ts:131-144 (milestonesUpcoming query, ta…

### [medium] Highlights count tasks/milestones due today as overdue and exclude today's milestones from 'upcoming'
FILE: src/app/api/projects/[projectId]/status-highlights/route.ts:112 (area overview)
SUMMARY: tasksOverdue uses dueDate: { lt: now } with completed: false (status-highlights/route.ts:107-114). Due dates are stored as UTC midnight of the due day, so a task due today (2026-07-03T00:00Z) is counted overdue from the moment the day starts. Symmetrically, milestonesUpcoming filters dueDate: { gte: now } (131-144), so a milestone due today never appears in the upcoming list. Both the '⚠ N overdue' chip and the prefilled 'What's blocked' text (buildBlockedFromHighlights, project-overview.tsx:368-376) inherit the inflated count, and the wrong text can be posted to the team.
SCENARIO: On July 3 a project has 4 open tasks all due July 3. The PM opens the composer at 10am: the highlights strip shows '4 overdue' and the Blocked block prefills '⚠ 4 tasks are currently overdue — review and rebaseline...'. Nothing is actually overdue, and today's milestone is missing from Next steps.
FIX: Compare against end-of-today (UTC) instead of now: overdue = dueDate < startOfTodayUTC; upcoming = dueDate >= startOfTodayUTC.
VERIFIER: CONFIRMED. Full chain reproduced: (1) Storage convention — src/lib/date-only.ts:4-9 explicitly documents "Due dates reach the client as UTC-midnight timestamps: composers send date-only strings ('YYYY-MM-DD') and the API stores `new Date(str)`, which the JS spec parses as 00:00 UTC"; write path confirmed at src/app/api/tasks/route.ts:256 (`dueDate: data.dueDate ? new Date(data.dueDate) : null`). Some composers (create-task-dialog.tsx:64) instead send local-midnight `toISOString()`, but either way the stored instant is start-of-day, so the bug holds. (2) API — status-highlights/route.ts:108-114 counts tasksOverdue with `completed: false, dueDate: { lt: now }`: a task due today (00:00 of today…

### [medium] Activity feed shows 'Deleted user completed a task' for any unassigned completed task and misattributes completions to the assignee
FILE: src/app/api/projects/[projectId]/activity/route.ts:215 (area overview)
SUMMARY: task_completed events use actor: t.assignee ?? null (activity/route.ts:207-217) — there is no completedBy tracking. When a completed task has no assignee, actor is null, and the client renders null actors as 'Deleted user' (project-overview.tsx:1338-1340: a.actor ? ... : 'Deleted user'). So every unassigned completed task shows 'Deleted user completed a task'. Additionally, when someone other than the assignee checks a task off, the feed credits the assignee.
SCENARIO: A PM creates a quick unassigned checklist task and marks it done. The Overview activity feed shows 'Deleted user completed a task — <task name>', implying data corruption / a removed account to everyone on the project.
FIX: For task_completed events with a null assignee, fall back to a neutral label (e.g. actor null rendered as 'Someone' for this event type), or track/attribute the actual completing user; at minimum change the client fallback for task events from 'Deleted user' to 'Someone'.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Client fetch: src/components/projects/project-overview.tsx:498 fetches `/api/projects/${project.id}/activity` and stores the result in `activities`. (2) API: src/app/api/projects/[projectId]/activity/route.ts:108-125 selects completed tasks with only `assignee` (no completer), and lines 207-216 build the task_completed event with `actor: t.assignee ?? null` (line 215). There is no completedBy tracking anywhere: prisma/schema.prisma model Task (lines 654-684) has `completedAt DateTime?` and optional `assigneeId` but no completedBy/completedById field, and a repo-wide grep for completedBy finds only unrelated aggregation variables in users/rou…

### [medium] Description editor and 'Change' status dropdown are enabled for read-only users; saves always 403 and the quick-status handler discards the server's reason
FILE: src/components/projects/project-overview.tsx:760 (area overview)
SUMMARY: The description textarea (project-overview.tsx:862-869) and the status 'Change' dropdown (1001-1026) are rendered enabled for every viewer, but PATCH /api/projects/:id requires owner or ADMIN/EDITOR member (route.ts:151-161). For COMMENTER/VIEWER members and non-member workspace viewers, description blur-save fails with a toast and handleQuickStatusChange fails too — and its handler throws new Error('Failed') on !res.ok (project-overview.tsx:760), discarding the server's "You don't have permission to edit this project" message, so the user only sees the generic 'Failed to update status'. Typed description text is never persisted and is lost on navigation.
SCENARIO: A COMMENTER member spends five minutes writing a project description, clicks away, and gets 'You don't have permission to edit this project'; their text silently evaporates when they leave the tab. Trying the status dropdown just says 'Failed to update status' with no explanation.
FIX: Derive canEdit from the project prop (ownerId/member role vs session user), render the textarea readOnly and hide/disable the status dropdown for read-only users; in handleQuickStatusChange parse the response body and surface body.error like the other handlers do.
VERIFIER: CONFIRMED — full chain reproduced from source. 1) Read-only users reach the Overview: src/app/(dashboard)/projects/[projectId]/page.tsx:132-159 grants page access to any project member (COMMENTER/VIEWER included), any viewer of a PUBLIC project, and workspace OWNER/ADMIN/L4+ non-members, then renders ProjectContent unconditionally (line 189). 2) No edit gating exists in the component: ProjectOverviewProps is only { project, onManageMembers? } (src/components/projects/project-overview.tsx:73-79) and ProjectContent passes exactly those (src/components/projects/project-content.tsx:928-931). No canEdit prop, no wrapper, no readOnly/disabled on the affected controls (grep of the file shows `disab…

### [medium] Duplicate project silently fails for any project without a description (zod rejects null), and never copies sections/tasks
FILE: src/components/projects/project-content.tsx:404 (area shell)
SUMMARY: The Duplicate menu item POSTs { name, color, description: project.description } (project-content.tsx:400-408). When project.description is null, JSON.stringify keeps `description: null`, but createProjectSchema declares description as z.string().optional() (src/app/api/projects/route.ts:12) — optional() accepts undefined, not null — so parse throws and the route returns 400 (route.ts:610-616). The client only handles res.ok, so nothing happens. Additionally, even when it succeeds (description present), the POST creates a fresh project with default 'To do / In progress / Done' sections and zero tasks (route.ts:353-368) — nothing from the original project is actually duplicated.
SCENARIO: User clicks Duplicate on a project that has no description: no toast, no navigation, no new project — silent no-op. On a project with a description: a new '(copy)' project appears but contains none of the original's sections, tasks, members, or metadata, which is not what 'Duplicate' promises.
FIX: Send `description: project.description ?? undefined` (or make the zod field .nullable()), surface non-ok responses with toast.error, and implement a real duplicate endpoint that copies sections/tasks/custom fields, or rename the action to reflect what it does.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Client: src/components/projects/project-content.tsx:400-408 — Duplicate menu item POSTs `JSON.stringify({ name: `${project.name} (copy)`, color: project.color, description: project.description })`. The project prop types description as `string | null` (project-content.tsx:69 and :101), matching Prisma `Project.description String?` (prisma/schema.prisma:154). JSON.stringify preserves `"description":null` (only undefined is dropped). (2) Server: src/app/api/projects/route.ts:12 — `description: z.string().optional()` (zod ^4.3.5 per package.json:67); .optional() accepts string|undefined, NOT null, so `createProjectSchema.parse(body)` at route.t…

### [medium] Create-project dialog's 'Target completion' (endDate) is silently discarded on create
FILE: src/components/projects/create-project-dialog.tsx:234 (area shell)
SUMMARY: In create mode the dialog includes `endDate` in the POST payload (create-project-dialog.tsx:234), but createProjectSchema in src/app/api/projects/route.ts:10-79 has no endDate field — zod's default object behavior strips unknown keys, so no error is raised — and the tx.project.create data block (route.ts:392-427) never writes endDate. The value the user typed is silently thrown away.
SCENARIO: User creates a 'Blank project' from the gallery, fills 'Target completion' with 2026-12-15, and clicks Create. The project is created with endDate = null; the Gantt view on /projects/all can't place it, percentPlanned/health read as if no deadline exists, and reopening 'Edit details' shows an empty Target-completion field.
FIX: Add `endDate: z.string().optional()` to createProjectSchema and `endDate: endDate ? new Date(endDate) : null` to the project.create data (mirroring the PATCH route which already supports endDate).
VERIFIER: CONFIRMED. Full chain reproduced from source: (1) The 'Target completion' date input is rendered unconditionally in the dialog — create and edit modes alike (src/components/projects/create-project-dialog.tsx:445-453, no isEdit gate). (2) In create mode, buildPayload(false) includes endDate in the POST body whenever the user filled it (create-project-dialog.tsx:219-234 — setField only omits when empty; a filled value hits the `else p[key] = value` branch), then POSTs to /api/projects (lines 255-259). (3) Server-side, createProjectSchema (src/app/api/projects/route.ts:10-79) has fields for startDate (line 18) but NO endDate anywhere; the body is parsed with `createProjectSchema.parse(body)` at…

### [medium] /projects/all and GanttTimeline test status against 'COMPLETED' but the enum value is 'COMPLETE'
FILE: src/app/(dashboard)/projects/all/page.tsx:508 (area shell)
SUMMARY: prisma ProjectStatus is ON_TRACK/AT_RISK/OFF_TRACK/ON_HOLD/COMPLETE (prisma/schema.prisma:568-574), and project-content.tsx correctly uses COMPLETE. But all/page.tsx defines ProjectStatus with 'COMPLETED' (line 72), keys STATUS_DOT with COMPLETED (line 120), and computes isOverdue with `p.status !== "COMPLETED"` (lines 506-508). gantt-timeline.tsx repeats the same comparison at lines 458 and 540. Since the API can never return 'COMPLETED', these branches always treat completed projects as not complete.
SCENARIO: A project marked Complete whose target date has passed (the normal case for every finished project) shows the '▲' overdue marker in the Health column of the projects list and gets the overdue glow in the Gantt view; its status dot in the grid falls back to the generic gray instead of gold.
FIX: Rename all 'COMPLETED' literals in src/app/(dashboard)/projects/all/page.tsx and src/components/projects/gantt-timeline.tsx to 'COMPLETE' to match the ProjectStatus enum.
VERIFIER: CONFIRMED. Full chain reproduced: (1) prisma/schema.prisma:568-574 defines enum ProjectStatus { ON_TRACK, AT_RISK, OFF_TRACK, ON_HOLD, COMPLETE } — the value is COMPLETE, not COMPLETED. (2) src/app/api/projects/route.ts:170-222 GET returns `prisma.project.findMany(...)` verbatim via NextResponse.json(projects) — no status remapping anywhere, so the wire value for a completed project is always "COMPLETE" (the generated Prisma client cannot even deserialize a value outside the schema enum, so DB drift cannot introduce "COMPLETED" through this route). (3) src/app/(dashboard)/projects/all/page.tsx:152 fetches /api/projects; its local ProjectStatus type (line 72) and STATUS_DOT keys (line 120) us…

### [medium] Star/favorite button in the project header is a pure client-side no-op
FILE: src/components/projects/project-content.tsx:443 (area shell)
SUMMARY: The star button toggles local useState `isStarred` (initialized false at line 193) and fires toast.success('Added to favorites') (lines 439-446), but never calls any API and nothing reads the value. There is no persistence — the state resets on every navigation or reload, and no favorites list anywhere consumes it.
SCENARIO: User stars a project, sees 'Added to favorites', navigates away and back — the star is empty again and the project appears in no favorites/starred list anywhere in the app. The toast presents fake functionality as real.
FIX: Persist the star (e.g. a UserFavoriteProject table or user preferences JSON) via an API call, initialize isStarred from server data, and surface starred projects in the sidebar; or remove the button and toast until the feature exists.
VERIFIER: CONFIRMED. Evidence chain: (1) src/components/projects/project-content.tsx:193 — `const [isStarred, setIsStarred] = useState(false);` plain local state, no server init. (2) Lines 439-446 — star Button whose onClick (line 443) does only `setIsStarred(!isStarred); toast.success(isStarred ? 'Removed from favorites' : 'Added to favorites');` — no fetch, no localStorage, no callback prop. (3) Grep of the entire file shows isStarred appears only at lines 193, 442, 443, 445 (all styling/toggle); no useEffect hydrates it and nothing else consumes it. (4) Repo-wide grep for favorite/starred: no /api route touches project favorites, prisma/schema.prisma has zero favorite/starred matches, and no sideba…

### [medium] 'Due this week' filter shifts UTC-midnight due dates a day earlier for US users
FILE: src/components/projects/project-content.tsx:271 (area shell)
SUMMARY: Due dates are stored as UTC midnight and serialized as ISO strings (page.tsx:180). The toolbar filter does isThisWeek(parseISO(task.dueDate), { weekStartsOn: 1 }) (project-content.tsx:268-274). parseISO('2026-07-06T00:00:00.000Z') becomes Sun Jul 5, 8:00 PM in US Eastern, so date-fns evaluates the task's week using the previous local day.
SCENARIO: US user filters List view by 'Due this week' on a week starting Monday: a task due Monday of the current week is evaluated as Sunday (previous week, since weekStartsOn:1) and is wrongly excluded; conversely a task due Monday of NEXT week is wrongly included in this week's results.
FIX: Compare the UTC calendar date, e.g. parse the date part only (`parseISO(task.dueDate.slice(0, 10))` gives a local-midnight date) before calling isThisWeek — matching however the task rows themselves render due dates.
VERIFIER: CONFIRMED — full chain reproduced. (1) Storage as UTC midnight: the List view's own inline date picker sends date-only strings — list-view.tsx:1531-1534 `format(due, "yyyy-MM-dd")` -> saveInlineEdit -> PATCH /api/tasks/[taskId]; the route does `new Date(data.dueDate)` at src/app/api/tasks/[taskId]/route.ts:334, and `new Date("2026-07-06")` is UTC midnight. Same for quick-create (quick-create-task-modal.tsx:237-240,258 fmtIso "YYYY-MM-DD" -> api/tasks/route.ts:256 `new Date(data.dueDate)`). (2) Serialization: src/app/(dashboard)/projects/[projectId]/page.tsx:180 `t.dueDate?.toISOString()` -> "2026-07-06T00:00:00.000Z". (3) Filter: src/components/projects/project-content.tsx:271 `isThisWeek(pa…

### [medium] Team tab returns 403 and shows an error/empty state for workspace admins and senior staff who can legitimately open a private project
FILE: src/app/api/projects/[projectId]/companies/route.ts:63 (area team)
SUMMARY: The project page grants access to PRIVATE projects for workspace OWNER/ADMIN or users with position level >= 4 (src/app/(dashboard)/projects/[projectId]/page.tsx:138-155). But loadProjectAccess in the companies GET only allows owner, project member, PUBLIC, or (WORKSPACE visibility + workspace membership) (companies/route.ts:59-72) — it lacks the page's OWNER/ADMIN/level>=4 bypass. Those users get a 403, ProjectTeamView's fetchTeam catches it and toasts 'Couldn't load project team' (project-team-view.tsx:132-137), then renders the 'No firms on this project yet' empty state (companies=[]) as if the team were empty.
SCENARIO: A workspace ADMIN (or a Principal Engineer, level 5) opens a colleague's PRIVATE project — the page loads fine — then clicks the Team tab: an error toast fires and the tab claims 'No firms on this project yet' even though the project has several firms and members.
FIX: Extend loadProjectAccess to mirror the page's check: when not owner/member/PUBLIC, look up the caller's WorkspaceMember role and position level for the project's workspace and grant read for OWNER/ADMIN or getLevel(position) >= 4.
VERIFIER: CONFIRMED. Chain reproduced: (1) page.tsx:136-155 grants PRIVATE-project access to non-members when their WorkspaceMember role is OWNER/ADMIN or getLevel(position)>=4, so the project page renders; (2) project-content.tsx:999-1005 renders ProjectTeamView on the Team tab; (3) project-team-view.tsx:129-141 fetches GET /api/projects/:id/companies, and on !res.ok toasts "Couldn't load project team" (line 137) leaving companies=[] so the "No firms on this project yet" empty state renders (lines 193-199); (4) companies/route.ts loadProjectAccess (lines 59-72) allows only owner, project member, PUBLIC, or (visibility==="WORKSPACE" + workspace membership) — no OWNER/ADMIN/level>=4 branch — so those u…

### [medium] GET /api/projects/:id/members leaks the member list of PRIVATE projects to any workspace member, including GUEST/CLIENT/WORKER
FILE: src/app/api/projects/[projectId]/members/route.ts:84 (area team)
SUMMARY: The members GET gate is only verifyWorkspaceAccess (members/route.ts:84), which passes for ANY WorkspaceMember row regardless of role (src/lib/auth-guards.ts:7-15). It never checks project visibility or project membership. The project page 404s non-members on PRIVATE projects unless workspace OWNER/ADMIN or level>=4 (page.tsx:136-158), and the sibling companies GET enforces visibility (companies/route.ts:59-72). So a GUEST/CLIENT/WORKER-role workspace member who cannot open the project at all can still fetch the full roster — names, emails, jobTitle, roles, join dates (members/route.ts:86-102).
SCENARIO: An external CLIENT user in the workspace issues GET /api/projects/<privateProjectId>/members (or any client code does it on their behalf) and receives every team member's name, email and role for a private project whose page would 404 for them.
FIX: In the members GET, apply the same read gate as the companies route (owner || member || PUBLIC || WORKSPACE+wsMember, plus the page's admin/level bypass) instead of bare verifyWorkspaceAccess.
VERIFIER: CONFIRMED. Full chain reproduced: (1) GET /api/projects/:projectId/members (src/app/api/projects/[projectId]/members/route.ts:63-102) authenticates (line 68-73), loads only workspaceId (75-78), then gates solely on `await verifyWorkspaceAccess(userId, project.workspaceId)` at line 84 — no visibility or project-membership check — and returns ALL ProjectMember rows with user id/name/email/image/jobTitle + role + joinedAt (86-102). (2) verifyWorkspaceAccess (src/lib/auth-guards.ts:7-15) only checks a WorkspaceMember row exists; it ignores role entirely. (3) WorkspaceRole enum includes GUEST, WORKER, CLIENT (prisma/schema.prisma:194-201), so low-trust workspace members pass the gate. (4) The pro…

### [medium] Project owner is listed twice in the Project Members dialog, with role/remove controls that always fail, and the member count is inflated
FILE: src/components/projects/project-members-dialog.tsx:266 (area team)
SUMMARY: Project creation always inserts the owner as a ProjectMember (src/app/api/projects/route.ts:411-416), and GET /members returns all ProjectMember rows (members/route.ts:86-102) with no owner exclusion. ProjectMembersDialog renders a hardcoded crown 'Owner' row (project-members-dialog.tsx:269-287) AND maps every fetched member (294-347), so the owner appears a second time as a regular row with a role dropdown and a Remove item. Both controls are doomed: PATCH rejects role changes for the owner with 400 'Cannot change owner role here' (members/route.ts:350-355) and DELETE rejects with 400 'Cannot remove the project owner' (members/route.ts:281-286); the dialog's changeRole/removeMember discard the response body (throw new Error() at lines 170 and 186) so the user only sees generic 'Failed to update role'/'Failed to remove member'. The header 'Members (N + 1)' count (project-members-dialog.t…
SCENARIO: On any project, open the members dialog: the creator shows up twice (crown row + regular row) and the count reads e.g. 'Members (4)' for 3 people. Clicking the duplicate row's role dropdown or Remove always errors with an unexplained 'Failed to update role' / 'Failed to remove member'.
FIX: Filter the owner out of the rendered member list (members.filter(m => m.userId !== owner.id)) and compute the count from the deduped set; also surface the server's error message in changeRole/removeMember instead of throw new Error().
VERIFIER: Reproduced the full chain from source. (1) Owner is always inserted as a ProjectMember with role ADMIN at project creation: src/app/api/projects/route.ts:411-416 (`members: { create: { userId, role: "ADMIN" } }` inside the creation transaction, where userId is the owner). (2) GET /api/projects/[projectId]/members returns ALL ProjectMember rows with no owner exclusion: src/app/api/projects/[projectId]/members/route.ts:86-102 (plain findMany where {projectId}). (3) ProjectMembersDialog fetches that list into `members` (project-members-dialog.tsx:96-98), renders a hardcoded crown "Owner" row (269-287) AND maps every fetched member with role dropdown + Remove item (294-347) with no `m.userId !==…

### [medium] Add Member picker on the Team tab uses the caller's arbitrary first workspace directory, not the project's workspace
FILE: src/app/api/team/directory/route.ts:66 (area team)
SUMMARY: ProjectTeamView populates the Add Member typeahead from GET /api/team/directory (project-team-view.tsx:150), but that route resolves the workspace via getCallerWorkspace = prisma.workspaceMember.findFirst({ where: { userId } }) with no orderBy and no multi-member heuristic (team/directory/route.ts:65-70). Per the codebase's own SEC-06 note (src/lib/auth-guards.ts:22-30), every user has an auto-generated single-member personal workspace plus the firm workspace, and a bare findFirst can return the personal singleton. The directory is also never scoped to the project's workspace. Candidates from the wrong workspace fail the members POST target check (members/route.ts:148-161) with 404 'User is not a member of this workspace'.
SCENARIO: A user who belongs to a personal workspace plus the firm workspace opens Add Member on a firm project: depending on which membership findFirst returns, the picker either lists only themselves ('No matches' for every colleague) or lists people from the wrong workspace whose add fails with 'User is not a member of this workspace'.
FIX: Accept a workspaceId (or projectId) query param on /api/team/directory and scope to the project's workspace when called from ProjectTeamView (verifying the caller is a member of that workspace); at minimum reuse getUserWorkspaceId's multi-member heuristic instead of a bare findFirst.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Client: src/components/views/project-team-view.tsx:150 fetches "/api/team/directory" with no workspace/project param; the response populates workspaceMembers (line 153), which AddMemberDialog uses as the typeahead candidate pool (lines 854-866) and POSTs the picked user to /api/projects/{projectId}/members (lines 872-879). (2) API: src/app/api/team/directory/route.ts:65-70 — getCallerWorkspace = prisma.workspaceMember.findFirst({ where: { userId } }) with no orderBy and no multi-member heuristic; GET (line 72) accepts no request args at all, so the directory can never be scoped to the project's workspace, and lines 84-85 list members of what…

### [medium] Month and quarter zoom misalign bars/Today line against header columns — up to ~2 months off at quarter zoom
FILE: src/components/views/timeline-view.tsx:346 (area timeline)
SUMMARY: Positions are computed with one linear px-per-day mapping: `totalDays = differenceInDays(timelineEnd, timelineStart)` where `timelineEnd = lastColumnStart + (day?1 : week?7 : 30)` (timeline-view.tsx:346-349, 443-446). For quarter zoom the last column represents ~91 days but only 30 are added, so 8 columns × 200px = 1600px are mapped over ~669 days instead of ~730. Header columns are drawn at fixed 200px each (line 989), so a task at the start of the 8th quarter (day ~639) renders at 639/669×1600 ≈ 1528px — 128px (≈2 months) into the wrong column. Month zoom has smaller drift because months of 28–31 days are all mapped as equal 120px columns while positions use the real day count (several days of drift mid-range). pixelsToDays (lines 461-473) inherits the same distortion for drags at these zooms.
SCENARIO: User switches the Timeline to Quarter zoom. A task due at the start of Q4 renders under the Q3/Q4 boundary shifted by up to two months, and the Today line does not fall inside the column labeled with the current month/quarter.
FIX: Compute each column's true day span (differenceInDays between consecutive column dates) and position bars by accumulating per-column widths, or make timelineEnd = addMonths(lastColumn, 1|3) and derive per-column pixel scale instead of a single global linear mapping.
VERIFIER: CONFIRMED. Evidence chain (src/components/views/timeline-view.tsx): (1) Quarter zoom config: 8 columns, 200px each, column dates generated by repeatedly adding 3 months (lines 250-262); month zoom: 12 columns of 120px via eachMonthOfInterval (lines 241-248). (2) getTaskPosition computes timelineEnd = addDays(lastColumnDate, zoomLevel==="day" ? 1 : "week" ? 7 : 30) — quarter falls into the `: 30` branch (lines 346-349), so for 8 quarters totalDays = differenceInDays ≈ 21 months + 30 days ≈ 669, not the true ~730. Positions are a single linear map: left = (startOffset/totalDays) * (columns.length * columnWidth) (lines 360-367). (3) Header columns are drawn at fixed config.columnWidth (line 989…

### [medium] Timeline drag endpoint lets any workspace member edit task dates in private projects they cannot open
FILE: src/app/api/tasks/[taskId]/route.ts:237 (area timeline)
SUMMARY: The drag/resize PATCH hits /api/tasks/:taskId, which guards with verifyTaskAccess (route.ts:237). verifyTaskAccess (src/lib/auth-guards.ts:143-177) only checks membership in the task's WORKSPACE (`verifyWorkspaceAccess`), ignoring project visibility entirely. The project page itself 404s plain workspace members on PRIVATE projects (page.tsx:136-158 requires owner/member/PUBLIC, or workspace role OWNER/ADMIN or level>=4). So the write check is strictly weaker than the read check: a member who cannot even view a private project can PATCH (rename, complete, reschedule, reassign) or DELETE (route.ts:543) any of its tasks by ID.
SCENARIO: A junior workspace member obtains a task ID from a private management project (e.g. from a notification or activity payload) and PATCHes its dueDate/completed — succeeding despite the project page returning 404 for them.
FIX: In verifyTaskAccess, when the task has a project, apply the same project-visibility rules used by the page/dependencies route (owner, project member, PUBLIC, or WORKSPACE visibility + workspace membership) instead of bare workspace membership.
VERIFIER: CONFIRMED. Evidence chain: (1) Timeline drag/resize sends PATCH to /api/tasks/:taskId with startDate/dueDate — src/components/views/timeline-view.tsx:535-539. (2) PATCH handler's only authz is `await verifyTaskAccess(userId, taskId)` — src/app/api/tasks/[taskId]/route.ts:237; DELETE handler identically at route.ts:543; updateTaskSchema (route.ts:20-39) accepts name, completed, assigneeId, dueDate, projectId, etc., so the endpoint permits full rename/complete/reassign/reschedule. (3) verifyTaskAccess (src/lib/auth-guards.ts:143-177) SELECTs project.visibility (line 156) but never reads it — for project tasks it only calls verifyWorkspaceAccess (line 175), i.e. bare workspaceMember existence c…

### [medium] Bulk complete and bulk move-to-section never fire workflow rules, contradicting the 'rules fire from any view' promise
FILE: src/app/api/tasks/bulk/route.ts:86 (area workflow)
SUMMARY: The Workflow tab states 'Rules fire automatically when a task is moved into the matching section, from any view' (workflow-view.tsx:428-431). The engine is hooked into PATCH /api/tasks/:id (route.ts:446,458) and POST /api/tasks/reorder (reorder/route.ts:87), but POST /api/tasks/bulk performs `updateMany({data:{sectionId: value}})` (bulk/route.ts:86-89) and `updateMany({data:{completed:true}})` (lines 30-33) without calling executeRulesOnSectionChange / executeRulesOnTaskCompleted.
SCENARIO: User multi-selects 8 submittal tasks in List view and uses the bulk toolbar 'Move to section → Under Review'. None of the Under Review rules run — no comments posted, no QC subtasks created, priorities untouched — whereas dragging each task individually would have triggered them. Same for bulk-complete vs TASK_COMPLETED rules.
FIX: In the bulk handler, snapshot each task's prior sectionId/completed, then after the updateMany loop call executeRulesOnSectionChange for tasks whose section changed and executeRulesOnTaskCompleted for tasks that flipped to completed.
VERIFIER: CONFIRMED. Chain reproduced: (1) workflow-view.tsx:429-430 promises "Rules fire automatically when a task is moved into the matching section, from any view" (comment at 426-427 enumerates views). (2) list-view.tsx bulk toolbar: line 1043 handleBulkAction("complete"), line 1059 handleBulkAction("move_section", section.id); handleBulkAction (450) POSTs to /api/tasks/bulk (453) — bulk UI really uses the bulk endpoint, not per-task PATCH. (3) src/app/api/tasks/bulk/route.ts has no workflow-engine import (imports lines 1-5); "complete" is bare updateMany({completed:true}) at 30-33 and "move_section" bare updateMany({sectionId:value}) at 86-89; no executeRulesOn* anywhere in the file. (4) Grep sho…

### [medium] Rules whose trigger section was deleted become invisible and undeletable but still count and still exist in the DB
FILE: src/components/views/workflow-view.tsx:245 (area workflow)
SUMMARY: WorkflowRule.trigger stores sectionId as JSON with no FK (prisma/schema.prisma:1351-1360), so deleting a Section leaves its rules behind. The view groups rules into rulesBySection keyed by trigger.sectionId (workflow-view.tsx:245-255); rules pointing at a deleted section match no rendered SectionCard (cards render only `sections` from the live project), so they appear nowhere and their X button is unreachable — yet the header still counts them ('N rules configured', lines 421-423) and GET /workflow keeps returning them forever.
SCENARIO: User deletes the 'Corrections' section from the Board view. The Workflow tab now says '6 rules configured' but only 4 chips are visible across all cards; the 2 orphaned Corrections rules can never be removed from the UI and permanently skew the count.
FIX: Render an 'Orphaned rules' card for rules whose trigger.sectionId matches no existing section (with a delete button), or cascade-delete matching workflow rules in the section DELETE endpoint.
VERIFIER: CONFIRMED — full chain reproduced. (1) prisma/schema.prisma:1351-1360: WorkflowRule.trigger is Json ({type:"TASK_MOVED_TO_SECTION", sectionId}); only FK is workflowId→Workflow, no relation to Section. (2) src/app/api/sections/[sectionId]/route.ts:97-100: section DELETE transaction deletes only tasks + the section; no workflowRule cleanup, and grep confirms this is the sole section-delete path in src. (3) src/app/api/projects/[projectId]/workflow/route.ts:96-107: GET returns all rules verbatim, no orphan filtering. (4) src/components/views/workflow-view.tsx:245-255 groups rules into rulesBySection[trigger.sectionId]; lines 625-639 render SectionCards only for the `sections` prop = live projec…

### [medium] Engine actions bypass all task-mutation side effects: no chained completion rules, goal progress, activity log, or notifications
FILE: src/lib/workflow-engine.ts:153 (area workflow)
SUMMARY: runAction writes directly with prisma (MARK_COMPLETE at workflow-engine.ts:153-157, SET_ASSIGNEE at 119-126), skipping everything PATCH /api/tasks/:id does on the same transitions: GoalProgressService.recalculateForTask (tasks/[taskId]/route.ts:425-432), executeRulesOnTaskCompleted chaining (455-462), Activity rows (413-422), notifyTaskAssigned (472-486) and notifyTaskCompleted (492-505). So a rule that completes or assigns a task produces none of those effects.
SCENARIO: The 'Calc package review' template marks tasks complete when moved to Issued. A task tied to a company goal moves to Issued: it flips to completed, but the goal's progress bar doesn't move, the project's TASK_COMPLETED rules ('On completion' card) never fire, no activity entry is logged, and a SET_ASSIGNEE rule assigns an engineer who receives no inbox/email notification and can't tell who assigned them or why.
FIX: In runAction, after mutating, invoke the same side-effect helpers the PATCH route uses (recalculate goal progress and fire executeRulesOnTaskCompleted on MARK_COMPLETE with a recursion guard; call notifyTaskAssigned on SET_ASSIGNEE; write Activity rows).
VERIFIER: CONFIRMED — full chain reproduced from source. (1) Engine writes raw prisma with no side effects: src/lib/workflow-engine.ts runAction MARK_COMPLETE does only `prisma.task.update({data:{completed:true,completedAt}})` (153-157) and SET_ASSIGNEE only `prisma.task.update({data:{assigneeId}})` (119-126); the file imports nothing but prisma and workflow-types (23-27) — no GoalProgressService, no notify*, no Activity writes. No Prisma middleware/extension exists that could add them (src/lib/prisma.ts is a bare PrismaClient, lines 7-14). (2) The PATCH route's equivalent transitions DO fire side effects, all gated on the request payload `data`, not on DB state after rules run: Activity rows from pay…

### [medium] Assignee/collaborator pickers in the action dialog only ever show the first 10 workspace users and have no search box
FILE: src/components/views/workflow-action-dialog.tsx:110 (area workflow)
SUMMARY: The dialog fetches `/api/users/search?q=` once (workflow-action-dialog.tsx:110) and renders the result as the full pick list. That endpoint hard-caps results with `take: 10` ordered by name asc (src/app/api/users/search/route.ts:49-50). The dialog renders no search input, so users alphabetically after the 10th can never be selected as rule assignee or collaborator.
SCENARIO: A workspace has 14 members. The PM opens 'Set assignee' for the Stamping section to pick 'Walter (PE)'. The list shows only the first 10 names alphabetically; Walter is absent and there is no way to find him — the rule can't be configured for him at all.
FIX: Add a search input in the dialog wired to the q parameter (like AssigneeSelector), and/or raise the take limit / support pagination for the empty-query case.
VERIFIER: CONFIRMED. Chain reproduced: (1) workflow-action-dialog.tsx:110 — for SET_ASSIGNEE/ADD_COLLABORATORS the load effect does fetch('/api/users/search?q=') with a hardcoded empty query; effect deps [open, actionType, projectId] (line 144) mean it can never re-fetch with a different q, and setUsers is only called once (line 113). (2) No search input exists: SET_ASSIGNEE branch (lines 209-248) and ADD_COLLABORATORS branch (lines 252-304) render only the static users list; the only text inputs in the component are the comment Textarea (line 312) and subtask name (line 395), unrelated to the user list. (3) src/app/api/users/search/route.ts:49-50 — take: 10, orderBy name asc; with q='' the contains-'…

### [low] Tasks created from the board quick-add are silently self-assigned to the creator
FILE: src/components/views/board-view.tsx:307 (area board)
SUMMARY: The board quick-add sends only { name, projectId, sectionId } (board-view.tsx:307-311). POST /api/tasks then applies `const assigneeId = data.assigneeId ?? userId` (tasks/route.ts:226) — a default written 'for My Tasks' but applied to every create path — so each board-created task gets the creator as assignee. The card immediately renders the creator's avatar and the task lands in their My Tasks, even though they never chose an assignee (Asana leaves project-created tasks unassigned).
SCENARIO: A PM quick-adds 15 punch-list tasks into a column intending to distribute them later. All 15 show the PM's avatar and flood the PM's My Tasks list; teammates filtering by 'Assigned to me' never see them as unassigned work.
FIX: Only default assigneeId to the current user when the create is a My Tasks create (e.g. when myTaskSection is present or projectId is null); pass assigneeId: null explicitly from the board quick-add otherwise.
VERIFIER: CONFIRMED. Evidence chain: (1) Board quick-add handler `handleAddTaskSubmit` at src/components/views/board-view.tsx:304-312 POSTs to /api/tasks with body exactly `{ name, projectId, sectionId }` — no assigneeId. (2) createTaskSchema at src/app/api/tasks/route.ts:15 makes assigneeId optional/nullable, so parse succeeds with it undefined. (3) route.ts:226 `const assigneeId = data.assigneeId ?? userId` (comment says "for My Tasks" but it runs on every create path, including project/board creates that passed verifyProjectAccess at :214-216). (4) route.ts:254 writes `assigneeId: assigneeId` in tx.task.create, so the creator is persisted as assignee. No guard or fallback elsewhere: the only other …

### [low] Mobile board '+ Add task' button is a silent no-op when the project has no sections
FILE: src/components/views/board-view.tsx:522 (area board)
SUMMARY: On mobile, when project.sections is empty, mobileActiveSection is undefined but the '+ Add task' button still renders (the adding-input branch at line 497 is false since addingTaskInSection null !== undefined). Its onClick (board-view.tsx:523-528) is guarded by `if (mobileActiveSection)` and does nothing — no input, no toast, no way to create a section on mobile either (the 'Add section' button is inside the desktop-only container, line 574-582).
SCENARIO: User opens a freshly created project (zero sections) on a phone and switches to the Board tab. Tapping '+ Add task' does nothing, with no feedback, and there is no mobile affordance to create a section — the board is a dead end.
FIX: When there are no sections, either hide the button and show an empty-state that creates a default section (POST /api/sections) before opening the task input, or have the tap create a 'To do' section and then start the inline add.
VERIFIER: CONFIRMED, with one scenario correction. Evidence chain (all in src/components/views/board-view.tsx unless noted): (1) line 352 `const mobileActiveSection = localSections[mobileColumnIndex]` — undefined when localSections is empty (localSections mirrors the sections prop, lines 121/131-134). (2) Line 497 branch `addingTaskInSection === mobileActiveSection?.id` evaluates `null === undefined` → false, so the '+ Add task' button (lines 522-532) renders inside the mobile container (`md:hidden`, line 383). (3) Its onClick (523-528) is wrapped in `if (mobileActiveSection)` with no else — a tap does nothing, no toast, no input. (4) The only 'Add section' button is at lines 574-582 inside the deskto…

### [low] Calendar cannot navigate further back than 4 weeks; older tasks are silently absent with no way to reach them
FILE: src/components/views/calendar-view.tsx:114 (area calendar)
SUMMARY: windowStart is fixed at 4 weeks before this week's Monday (calendar-view.tsx:107-116) and only weekCount grows (downward, via the bottom sentinel at :440-452). There are no prev-month controls (toolbar has only 'Today', :516-527), and segments whose due date falls before allDays[0] are skipped entirely (:318-323). The 'No date (N)' pill only covers tasks with no dates at all (:97-100), not tasks with old dates.
SCENARIO: A project has an overdue task from two months ago. On the Calendar tab the user scrolls up, hits the hard top at ~4 weeks back, and the task is simply nowhere on the calendar — no indicator that older items exist.
FIX: Prepend weeks when the user reaches the top (mirror the bottom sentinel with scroll-position compensation), or add prev/next month controls that extend windowStart backwards.
VERIFIER: CONFIRMED. Chain reproduced in src/components/views/calendar-view.tsx: (1) windowStart is created with `const [windowStart] = useState(...)` at :107 with no setter, seeded exactly 4 weeks before this week's Monday at :114 — it can never move earlier. (2) allDays is generated forward-only from windowStart (:258-266) and the only window mutation is setWeekCount(c => c + 8) in the bottom-sentinel IntersectionObserver (:440-452); there is no top sentinel (only bottomSentinelRef, rendered at :862) and no prepend path. (3) Toolbar (:516-527) has only a 'Today' button; comment at :510-511 explicitly says 'No prev/next buttons'. (4) Tasks with due < allDays[0] are skipped with no indicator at :318-3…

### [low] GET /api/projects/:id/members uses workspace membership only: leaks private-project rosters and breaks the Team tab for PUBLIC-project visitors
FILE: src/app/api/projects/[projectId]/members/route.ts:84 (area cross-permissions)
SUMMARY: The members GET gate is verifyWorkspaceAccess(userId, project.workspaceId) (members/route.ts:84) — pure workspace membership. Two divergences from the page check: (1) any L1-L3 workspace member can list the full member roster (id, name, email, image, jobTitle at :86-100) of a PRIVATE project the page 404s them on, also confirming the project exists; (2) inversely, for a PUBLIC project the page grants any authenticated user access (page.tsx:134-136), but a user from another workspace gets AuthorizationError -> 403 (:104-107), so project-team-view.tsx and the messages mention-picker (messages-view.tsx:144) fail to load members.
SCENARIO: (1) A restricted engineer calls GET /api/projects/P/members for a private project and reads everyone's emails/titles. (2) A user outside the workspace opens a PUBLIC project: page renders but the Team tab shows no members and @-mentions in Messages have an empty picker.
FIX: Replace verifyWorkspaceAccess with the shared project-access helper (owner/member/PUBLIC/leadership) so the members list is visible exactly when the page is.
VERIFIER: Core divergence CONFIRMED, but two of the claimed impacts are wrong/overstated. Corrected severity — SEVERITY: low. Reproduced chain: (1) PRIVATE-roster divergence is real. GET /api/projects/[projectId]/members gates only on `verifyWorkspaceAccess(userId, project.workspaceId)` (src/app/api/projects/[projectId]/members/route.ts:84; auth-guards.ts:7-15 = bare workspaceMember lookup, ignores project.visibility) and returns id/name/email/image/jobTitle for every ProjectMember (route.ts:86-100). The page, by contrast, 404s a workspace member who is not owner/member unless the project is PUBLIC or they hold workspace OWNER/ADMIN or position level>=4 (src/app/(dashboard)/projects/[projectId]/page.t…

### [low] Custom-field value PATCH returns 500 for unauthorized or missing tasks instead of 403/404
FILE: src/app/api/tasks/[taskId]/custom-fields/[fieldId]/route.ts:314 (area cross-permissions)
SUMMARY: verifyTaskAccess at :46 throws AuthorizationError (no workspace membership) or NotFoundError (bad taskId), but the catch block at :314-320 has no instanceof mapping — every such failure is logged and returned as 500 'Failed to set custom field value'. Other routes in the same family (e.g. members/route.ts:104-107, tasks/route.ts:189-192) map these to 403/404.
SCENARIO: A user whose session belongs to a different workspace (or a request with a stale/deleted taskId) edits a custom-field cell; the client receives a 500 server error rather than a 403/404, surfacing as a generic 'something went wrong' and polluting error logs with expected authorization denials.
FIX: Add `if (err instanceof AuthorizationError || err instanceof NotFoundError) { const { status, message } = getErrorStatus(err); return NextResponse.json({ error: message }, { status }); }` before the generic 500 in the catch block.
VERIFIER: CONFIRMED. Evidence chain reproduced from source: (1) src/app/api/tasks/[taskId]/custom-fields/[fieldId]/route.ts:46 calls `await verifyTaskAccess(userId, taskId)` inside the try block (imported from @/lib/auth-guards at :22). (2) src/lib/auth-guards.ts:143-177 — verifyTaskAccess throws NotFoundError("Task not found") at :163 for a bad taskId, AuthorizationError at :169 for a projectless task the user doesn't own, and via verifyWorkspaceAccess (:175 -> :12) throws AuthorizationError("You don't have access to this workspace") when the caller has no membership in the task's workspace. (3) The route's catch block at route.ts:314-320 contains only console.error + a blanket `NextResponse.json({ e…

### [low] "View all" button on every chart card is a silent no-op
FILE: src/components/views/dashboard-view.tsx:465 (area dashboard)
SUMMARY: The ChartCard footer renders a <button> "View all" with an ExternalLink icon (lines 465-468) that has no onClick handler and is not wrapped in any link. It appears on all three chart cards ("Incomplete tasks by section", "Tasks by completion status", "Tasks by assignee"). Unlike the other placeholder menu items in this file, which at least fire toast.info("...coming soon") (e.g. lines 198-201, 451-453), this button gives no feedback whatsoever.
SCENARIO: User clicks "View all" under the "Incomplete tasks by section" chart expecting to see the underlying task list; nothing happens — no navigation, no toast, no error.
FIX: Wire it to the corresponding drill-down, e.g. router.push(`/projects/${projectId}?view=list`) (the projectId prop is already passed to DashboardView but currently unused), or remove the button / give it the same "coming soon" toast as the other placeholders.
VERIFIER: CONFIRMED. Evidence chain: (1) src/components/views/dashboard-view.tsx:465-468 — ChartCard footer renders `<button className="text-xs text-black hover:text-black flex items-center gap-1">View all <ExternalLink .../></button>` with no onClick, no href, and not wrapped in any Link/anchor. (2) ChartCardProps (lines 433-437) accepts only title/children/filterCount — there is no way for a caller to inject a handler, so no usage can wire it up. (3) The three chart cards named in the finding all use ChartCard: line 240 "Incomplete tasks by section", line 284 "Tasks by completion status", line 322 "Tasks by assignee" — the dead button appears on all three. (4) Contrast holds: sibling placeholders gi…

### [low] Hardcoded fake "N filters" badges on KPI cards and charts presented as real widget metadata
FILE: src/components/views/dashboard-view.tsx:215 (area dashboard)
SUMMARY: The KPI cards pass literal filterCount={1} (lines 215, 221, 227) and the chart cards pass filterCount={2}/{1}/{2} (lines 240, 284, 322). KPICard (lines 419-424) and ChartCard (lines 460-464) render these as "1 filter" / "2 filters" labels with a Filter icon, mimicking Asana's widget-filter indicator. No such per-widget filters exist anywhere in the component — the numbers are invented constants and never change, including when real filters from the List tab ARE silently applied (see the filteredSections finding) or when none are.
SCENARIO: User sees "2 filters" on the "Incomplete tasks by section" chart, assumes the chart is scoped by two configurable filters, and looks for a way to view/edit them; there is none, and the label stays "2 filters" regardless of any actual filtering state.
FIX: Remove the filterCount props (render "No filters" or nothing), or derive the count from the genuinely applied filter state passed down from ProjectContent.
VERIFIER: CONFIRMED. Chain reproduced: (1) src/components/views/dashboard-view.tsx passes literal filterCount={1} at lines 215/221/227 (KPI cards) and filterCount={2}/{1}/{2} at lines 240/284/322 (ChartCards) — pure constants, no filter state exists in the component (props are only {sections, projectId}, lines 70-73). (2) KPICard lines 419-424 and ChartCard lines 460-464 render these as "N filter(s)" badges with a Filter icon; ChartCard menu actions (451-453) are all "coming soon" toasts, so there is no way to view/edit the supposed filters. (3) Sole caller src/components/projects/project-content.tsx:964-967 passes sections={filteredSections} — real filters DO change the chart data while the badge sta…

### [low] Attachments GET returns 500 for authorization/not-found errors; getErrorStatus imported but never used
FILE: src/app/api/projects/[projectId]/attachments/route.ts:64 (area files)
SUMMARY: verifyProjectAccess (route.ts:20) throws NotFoundError for a missing project and AuthorizationError for a non-member (src/lib/auth-guards.ts:182-199). The route's catch block (route.ts:64-70) does not check `error instanceof AuthorizationError/NotFoundError` — it logs and returns a blanket 500 'Failed to fetch attachments'. The file even imports getErrorStatus (line 4) but never calls it, unlike the sibling task-attachments routes (src/app/api/tasks/[taskId]/attachments/route.ts:30-33) which map these errors to 403/404 correctly.
SCENARIO: A user without workspace access (or with a stale link to a deleted project) hits GET /api/projects/:id/attachments. They receive HTTP 500 instead of 403/404; server logs and any error monitoring record a spurious internal error for every such request, and the client cannot distinguish 'no access' from 'server broken'.
FIX: Mirror the sibling routes: import AuthorizationError and NotFoundError, and in the catch block do `if (error instanceof AuthorizationError || error instanceof NotFoundError) { const { status, message } = getErrorStatus(error); return NextResponse.json({ error: message }, { status }); }` before the 500 fallback.
VERIFIER: CONFIRMED. Full chain reproduced from source: (1) src/app/api/projects/[projectId]/attachments/route.ts:20 calls `await verifyProjectAccess(userId, projectId)` inside the try block. (2) verifyProjectAccess (src/lib/auth-guards.ts:182-199) throws `new NotFoundError("Project not found")` at line 194 when the project is missing, and delegates to verifyWorkspaceAccess (auth-guards.ts:7-15) which throws `new AuthorizationError("You don't have access to this workspace")` at line 12 for non-members. (3) The route's catch block (route.ts:64-70) has no instanceof checks — it unconditionally logs `console.error("Error fetching project attachments:", error)` and returns `{ error: "Failed to fetch attac…

### [low] No pagination and no lazy loading: grid renders every image attachment at full original resolution
FILE: src/components/views/files-view.tsx:351 (area files)
SUMMARY: GET /api/projects/[projectId]/attachments has no `take`/cursor (route.ts:23-41), returning every attachment row for the project, and the grid view renders each image attachment with `<img src={file.url}>` (files-view.tsx:351) — file.url is the original Vercel Blob object (src/lib/storage.ts:59-63, up to 10MB each), with no `loading="lazy"`, no thumbnail variant, and no virtualization. All images start downloading the moment the tab opens.
SCENARIO: A civil/structural project accumulates 300 site photos at ~5MB each across its RFI tasks. Opening the Files tab (grid is the default view, line 134) kicks off ~1.5GB of image downloads in parallel; the tab is unusable on site Wi-Fi/cellular and can hang the browser tab.
FIX: At minimum add loading="lazy" and decoding="async" to the grid <img>. Better: add a `take`/cursor to the API and infinite scroll, or serve resized thumbnails (Vercel Blob supports image variants via next/image with a loader).
VERIFIER: CONFIRMED. Evidence chain: (1) API returns all rows — src/app/api/projects/[projectId]/attachments/route.ts:23-41 `prisma.attachment.findMany` has `where`, `include`, `orderBy` but no `take`/`skip`/`cursor`; result mapped 1:1 at lines 51-63, so the client receives every attachment for the project. (2) Client fetches it all on mount — src/components/views/files-view.tsx:140-155 `fetch(/api/projects/${projectId}/attachments)` in useEffect; default view is "grid" (line 134: `useState<ViewMode>("grid")`). (3) Grid renders raw full-res images — files-view.tsx:350-351 `{isImage && file.url ? <img src={file.url} alt={file.name} className="w-full h-full object-cover" />}` — plain <img>, not next/ima…

### [low] Type filter cannot select audio/other files, and legacy Office mime types are misclassified as 'other'
FILE: src/components/views/files-view.tsx:219 (area files)
SUMMARY: getFileType (files-view.tsx:86-93) buckets files into image/video/audio/spreadsheet/document/other, but the filter <select> (lines 219-229) only offers image/video/document/spreadsheet — there is no way to filter to audio or 'other' files even though uploads of audio/mpeg, audio/wav, and application/zip are explicitly allowed (src/lib/storage.ts:19-24). Additionally, 'application/msword' and 'application/vnd.ms-powerpoint' are in ALLOWED_MIME_TYPES (storage.ts:11,15) but match none of the substring checks ('pdf', 'document', 'text/'), so legacy .doc/.ppt files land in 'other': they get the generic gray icon and are excluded from the 'Documents' filter.
SCENARIO: An engineer uploads a legacy .doc RFI response to a task. In the Files tab they select 'Documents' in the type filter — the .doc disappears from the list. Similarly a user looking for a recorded site call (.mp3) has no filter option that matches it.
FIX: Add 'Audio' and 'Other' options to the select, and extend getFileType's document branch to include mimeType.includes('msword') || mimeType.includes('powerpoint') || mimeType.includes('presentation') (or map presentations to their own bucket — the Presentation icon is already imported at line 17 but never used).
VERIFIER: CONFIRMED. Chain reproduced: (1) files-view.tsx:86-93 getFileType returns "audio" for audio/* (line 89) and "other" fallback (line 92); document branch (line 91) only matches "pdf"/"document"/"text/". (2) files-view.tsx:219-229 filter select offers only All/image/video/document/spreadsheet — no audio or other option; filtering (lines 157-164) is strict equality, so those buckets are unreachable via filter. (3) storage.ts:23-24 allows audio/mpeg and audio/wav; storage.ts:19-20 allows zip types, so such files legitimately exist. (4) storage.ts:11 "application/msword" and storage.ts:15 "application/vnd.ms-powerpoint" contain none of the document-branch substrings (nor spreadsheet/excel/csv), so…

### [low] Blank dropdown option passes builder validation but the server rejects the save with a cryptic error
FILE: src/components/views/form-builder-dialog.tsx:380 (area forms)
SUMMARY: The builder's validation only checks `!f.options || f.options.length === 0` (lines 380-382), so an option whose text was cleared ("") passes. The server schemas require `options: z.array(z.string().min(1))` (api/forms/route.ts:24, api/forms/[formId]/route.ts:36). On edit, PATCH returns `{error: "Invalid payload"}` ([formId]/route.ts:196-200) which handleSave toasts verbatim; on create, POST /api/forms surfaces the raw zod message with no field context (route.ts:148-155). The user gets no indication which field or option is the problem.
SCENARIO: User clears the text of "Option 2" in a dropdown intending to retype it, gets distracted, hits Save changes. The toast says only "Invalid payload" and the form won't save; nothing highlights the empty option.
FIX: Add `if (f.options?.some((o) => !o.trim())) return { ok: false, msg: `"${f.label}" has an empty option` }` to the builder's validation memo (and/or trim+drop empty options before sending).
VERIFIER: CONFIRMED — full chain reproduced from source. (1) The builder's OptionsEditor (src/components/views/form-builder-dialog.tsx:1400-1406) writes the raw input value into the options array with no trim/filter, so clearing an option's text leaves "" in the array; nothing removes it later. (2) The validation memo (form-builder-dialog.tsx:380-382) only rejects `!f.options || f.options.length === 0` — an array like ["Option 1", ""] has length 2 and passes; no other check in the memo (372-393) touches option contents. (3) handleSave (form-builder-dialog.tsx:403-425) sends `fields` verbatim in the payload to PATCH /api/forms/{id} (edit) or POST /api/forms (create). (4) Both server schemas require non…

### [low] Submissions inbox silently truncates at 100 submissions with no indicator or pagination
FILE: src/app/api/forms/[formId]/submissions/route.ts:68 (area forms)
SUMMARY: GET /api/forms/:formId/submissions hard-caps at `take: 100` (line 68) ordered newest-first, returns no total count, and the FormSubmissionsDialog renders whatever it receives with no "showing 100 of N" hint or load-more (form-submissions-dialog.tsx:150-253). Older submissions become unreachable in the UI (only the CSV export, which has no take limit, includes them).
SCENARIO: A busy RFI intake form collects 140 submissions. The inbox dialog shows the newest 100; the oldest 40 look like they never happened — a user scrolling to find an early submission concludes it was lost, when it exists and appears in the CSV.
FIX: Return a total count and add pagination (cursor or offset) to the endpoint and a "Load more" / count indicator in FormSubmissionsDialog — or at minimum render "Showing latest 100; export CSV for all" when 100 rows come back.
VERIFIER: CONFIRMED — full chain reproduced. (1) API: src/app/api/forms/[formId]/submissions/route.ts:65-69 — `prisma.formSubmission.findMany({ where: { formId }, orderBy: { createdAt: "desc" }, take: 100 })`; response at lines 71-78 is a bare array (id/data/taskId/createdAt) with no total count or pagination cursor, and the route reads no query params (`_req` unused), so there is no way to request older pages. (2) Client: src/components/views/form-submissions-dialog.tsx:83-89 fetches `/api/forms/${form.id}/submissions` once and setSubmissions with whatever arrives; render loop at lines 151-252 maps all rows with no "showing N of M" indicator and no load-more control — nothing anywhere in the dialog r…

### [low] Inline 'Add task' in the List tab silently auto-assigns the new task to its creator
FILE: src/components/views/list-view.tsx:515 (area list)
SUMMARY: handleAddTaskSubmit POSTs { name, projectId, sectionId } with no assigneeId (list-view.tsx:515-523). POST /api/tasks defaults `const assigneeId = data.assigneeId ?? userId` (tasks/route.ts:226) - a fallback commented as intended "for My Tasks" - so every task added via the project List tab is assigned to whoever typed it. Asana-parity behavior is to leave project tasks unassigned.
SCENARIO: A PM quick-adds 15 tasks into a project section for the team to pick up; every one shows the PM as assignee in the Assignee column and lands in the PM's My Tasks, and each must be manually unassigned.
FIX: Send assigneeId: null explicitly from the List add path, or restrict the server-side auto-assign fallback to requests without a projectId.
VERIFIER: Evidence chain fully reproduced. (1) Client: src/components/views/list-view.tsx:507-523 — handleAddTaskSubmit POSTs to /api/tasks with body { name, projectId, sectionId } only; no assigneeId key at all. (2) Validation: src/app/api/tasks/route.ts:15 — createTaskSchema has assigneeId: z.string().optional().nullable(), so an omitted field parses to undefined (no server-side default of null). (3) Server: src/app/api/tasks/route.ts:226 — `const assigneeId = data.assigneeId ?? userId;` (comment on line 225: "Auto-assign to current user if no assigneeId provided (for My Tasks)"); undefined ?? userId yields the creator's userId, which is written at route.ts:254 (assigneeId: assigneeId in tx.task.cre…

### [low] Column headers look sortable (cursor-pointer + chevron) but have no click handler
FILE: src/components/views/list-view.tsx:699 (area list)
SUMMARY: The Name/Assignee/Due date/Priority/Status header cells (list-view.tsx:699-718) each render with `cursor-pointer hover:text-slate-700` and a ChevronDown icon, signaling click-to-sort, but none has an onClick. Sorting only exists in the toolbar dropdown in project-content.tsx (handleSort, lines 219-231), which supports fewer fields (no assignee/status).
SCENARIO: User clicks the 'Due date' column header expecting the list to sort by due date (standard Asana behavior the styling advertises); nothing happens.
FIX: Wire the header cells to project-content's sort state (lift handleSort down as a prop) or remove the cursor-pointer styling and chevrons until sorting is implemented.
VERIFIER: CONFIRMED. Evidence chain: (1) src/components/views/list-view.tsx:699-718 — the five header cells (Name 699-702, Assignee 703-706, Due date 707-710, Priority 711-714, Status 715-718) each render `className="flex items-center gap-1 cursor-pointer hover:text-slate-700"` plus a `<ChevronDown className="w-3 h-3" />`, and none has an onClick or any other handler (the only interactive header element is the select-all Checkbox at 692-697). (2) A whole-file grep of list-view.tsx for "sort" (case-insensitive) matches only dnd-kit imports/usage (SortableContext, useSortable, SortableTaskRow at lines 85-89, 858, 1264, 1292) — there is no column-sort state, no sort prop received, and no delegated click …

### [low] Bulk-action selection silently includes tasks hidden by search/filters, so bulk Delete can destroy rows the user cannot see
FILE: src/components/views/list-view.tsx:450 (area list)
SUMMARY: selectedTasks (list-view.tsx:192) is never pruned when the sections prop changes. ListView renders filteredSections, so after selecting tasks and then typing a search or toggling a filter in the toolbar, selected-but-now-hidden ids remain in the set; the floating bar count (1037) still includes them and handleBulkAction (450-470) sends all of them to /api/tasks/bulk, including destructive delete.
SCENARIO: User selects 8 tasks, then searches to narrow the list to 2 of them, confirms 'Delete 8 tasks?' assuming the count is wrong or clicks fast - all 8 are deleted including 6 rows not visible on screen.
FIX: Prune selectedTasks to ids present in the current visible task set whenever sections/filters change (useEffect intersecting selectedTasks with allTaskIds).
VERIFIER: CONFIRMED, full chain reproduced. (1) Filters live in the parent: src/components/projects/project-content.tsx:244-290 computes filteredSections from searchQuery/activeFilters/showCompleted, and :933-939 renders <ListView sections={filteredSections}/> with NO key prop, so ListView does not remount when filters change and its internal state persists. (2) src/components/views/list-view.tsx:192 holds selectedTasks; every setSelectedTasks call site is 434 (row toggle), 444/446 (select all/none), 465 (clear after successful bulk action), 1098 (manual X) — none prunes on sections change; the only sections-driven effect (list-view.tsx:284-287) syncs localSections only. So ids hidden by a subsequent …

### [low] Pressing Enter twice in the inline add-task input creates duplicate tasks
FILE: src/components/views/list-view.tsx:903 (area list)
SUMMARY: The add-task input's Enter handler (list-view.tsx:902-905) calls handleAddTaskSubmit with no in-flight guard; newTaskName is only cleared after the POST resolves (531), so a second Enter (or Enter followed by an app-level re-trigger) during the network round-trip fires a second POST /api/tasks with the same name, creating two rows.
SCENARIO: On a slow connection the user types 'Order rebar' and presses Enter; nothing visibly changes for a second so they press Enter again - two identical 'Order rebar' tasks appear after refresh.
FIX: Track an isSubmitting flag (or clear newTaskName/close the input synchronously before awaiting the fetch, restoring it on failure) so a second Enter is a no-op while the create is in flight.
VERIFIER: CONFIRMED. Evidence chain: (1) src/components/views/list-view.tsx:902-905 — the inline add-task input's onKeyDown fires handleAddTaskSubmit(section.id) on every Enter keydown with no guard (no e.repeat check either, so even holding Enter auto-repeats submissions); grep for isSubmitting/inFlight/disabled in the file returns zero matches. (2) list-view.tsx:507-536 — handleAddTaskSubmit is async; the only early-return is the empty-name check at 508, and newTaskName is cleared only at line 531 AFTER `await fetch("/api/tasks", {method:"POST"...})` (515) resolves OK. During the network round-trip the state still holds the typed name, so a second Enter passes the trim check and fires an identical s…

### [low] Delete menu item is shown to all users but the server rejects non-author/non-admin with 403
FILE: src/components/views/messages-view.tsx:1696 (area messages)
SUMMARY: In the message dropdown, Edit is gated by `m.mine` (line 1687) but Delete is rendered unconditionally (lines 1696-1699). The DELETE endpoint allows only the author or a scope admin (src/app/api/messages/[messageId]/route.ts:142-147). For a regular EDITOR/COMMENTER member the flow is: confirm dialog → optimistic removal of the message → 403 → snapshot restore + error toast.
SCENARIO: A regular project member opens the ⋯ menu on a teammate's message, clicks Delete, confirms the scary 'can't be undone' prompt, watches the message disappear, then sees it reappear with 'Only the author or a scope admin can delete'.
FIX: Pass an isAdmin flag (project owner/ADMIN) down to MessagesView and render the Delete item only when `m.mine || isAdmin`, mirroring the server gate.
VERIFIER: CONFIRMED. Chain reproduced: (1) messages-view.tsx dropdown toolbar (1671-1701) renders for every message; Edit is gated by `m.mine` (1687) but the Delete DropdownMenuItem (1696-1699) is unconditional, and no `isAdmin` symbol exists anywhere in the file (grep verified). Replies use the same ungated path (1931 → onReplyDelete → handleDelete, 1311). (2) handleDelete (1101-1145): confirm() "This can't be undone" at 1102, snapshot at 1103, optimistic removeAnyMessage at 1113, fetch DELETE at 1134; on !res.ok it throws body.error (1137-1139) and the catch restores the snapshot and toasts the error (1143-1144). (3) src/app/api/messages/[messageId]/route.ts:142-147: DELETE returns 403 "Only the aut…

### [low] Editing a message cannot add or remove @mentions even though the API supports it
FILE: src/components/views/messages-view.tsx:1081 (area messages)
SUMMARY: commitEdit PATCHes only `{ content }` (lines 1081-1085) and the edit UI is a plain <textarea> (lines 1709-1725) without the MentionTextarea typeahead. The PATCH route fully supports mention re-sync — syncMentionsForEditedMessage diffs rows and notifies newly added users — but only when `mentionUserIds !== undefined` (messages/[messageId]/route.ts:74-91), so the client's omission means: typing '@Jane Doe' into an edit never creates a mention row or notification (renders as plain text, no chip), and deleting a mention handle during an edit leaves the stale MessageMention row in the DB.
SCENARIO: User posts a message, realizes they forgot to tag the reviewer, edits it to add '@Jane Doe'. The text saves but Jane never gets a mention notification and no gold chip renders — with no indication anything is missing.
FIX: Use MentionTextarea for the edit composer, track staged mention ids the same way the send path does (filter by handles still present in the text), and include `mentionUserIds` in the PATCH body.
VERIFIER: Confirmed end-to-end. (1) Client: commitEdit in src/components/views/messages-view.tsx:1081-1085 PATCHes endpoints.message(messageId) with body JSON.stringify({ content }) only — no mentionUserIds. This is the sole PATCH caller of that endpoint in src (the other hit at line 1134 is the DELETE). (2) Edit UI: lines 1709-1725 render a plain <textarea> with no mention typeahead, while the send composers use MentionTextarea (lines 1394, 1986) and the send path stages and POSTs mentionUserIds (state at line 304, effectiveMentions filtered by handles still in text at line 931, sent at lines 814/973). (3) API: src/app/api/messages/[messageId]/route.ts:17-20 accepts optional mentionUserIds, and lines…

### [low] 10MB attachment limit exceeds Vercel's 4.5MB route-handler body cap — 4.5-10MB uploads fail with an unhandled 413
FILE: src/components/views/messages-view.tsx:1026 (area messages)
SUMMARY: The client accepts files up to 10MB (line 1026-1030) and the attachments route also enforces 10MB (attachments/route.ts:51-56), but the app is deployed on Vercel (uses @vercel/blob in src/lib/storage.ts, live at ttcivilstructural.com) where serverless route handlers reject request bodies over ~4.5MB with a platform-level 413 before the route code runs. The client's error path parses the body as JSON (`res.json().catch(() => null)` at 884) and falls back to a bare 'Upload failed' toast.
SCENARIO: User attaches a 7MB PDF — well under the advertised 10MB ceiling — the chip passes the client check, the message posts, then the upload dies with 'file.pdf: Upload failed' every time, with no hint that the real limit is 4.5MB.
FIX: Either lower both client and server limits to 4MB with matching copy, or switch message attachments to @vercel/blob client uploads (upload() with a token route) which bypass the function body limit and legitimately support 10MB.
VERIFIER: CONFIRMED. Evidence chain: (1) Client accepts files up to 10MB — src/components/views/messages-view.tsx:1026-1030 filters only files > 10*1024*1024 and adds the rest to pendingFiles, toast copy says "exceeds 10MB". (2) Upload path is a plain multipart POST through a Next.js route handler, not a client-side blob upload — uploadFileToMessage (messages-view.tsx:867-887) does fetch(endpoints.attachments(messageId), { method: "POST", body: FormData }). (3) Server route enforces the same 10MB limit — src/app/api/messages/[messageId]/attachments/route.ts:51-56 — and uploads server-side via uploadFile() which calls @vercel/blob put() (src/lib/storage.ts:3, 40-63, MAX_FILE_SIZE = 10MB at line 3). (4)…

### [low] Status update creation and project-status sync are not transactional; a sync failure reports error for an update that was actually saved
FILE: src/app/api/projects/[projectId]/status-updates/route.ts:262 (area overview)
SUMMARY: POST creates the StatusUpdate row (status-updates/route.ts:247-260) and then separately updates project.status (262-267) outside a transaction. If the project.update throws, the catch at 324 returns 500 — but the status update is already committed. The client (project-overview.tsx:671-703) treats the 500 as total failure: it keeps the composer open with the text and shows an error toast, inviting a retry that creates a duplicate update.
SCENARIO: The project.update fails transiently (connection blip after the create). The PM sees 'Failed to post status update', clicks Post again, and the history now shows the same update twice.
FIX: Wrap the statusUpdate.create and project.update in prisma.$transaction so the endpoint either fully succeeds or fully fails.
VERIFIER: CONFIRMED. Chain: route.ts POST creates StatusUpdate at src/app/api/projects/[projectId]/status-updates/route.ts:247-260, then separately runs prisma.project.update at 262-267 when wantsSync (defaults true per schema line 42; client always sends syncProjectStatus:true at project-overview.tsx:667). No $transaction anywhere in the file. If project.update throws, the outer catch at 324-330 returns 500, but the create is already committed — the route's own comment at 277-278 ("never rolls back the already-committed status update") confirms commit semantics at that point; the notification fan-out got a protective try/catch (279-311) but project.update did not. Client (project-overview.tsx:671-681…

### [low] 'Add member' / 'Manage all' buttons on the Overview are silent no-ops when the project owner was deleted
FILE: src/components/projects/project-content.tsx:1026 (area overview)
SUMMARY: ProjectContent only mounts ProjectMembersDialog when project.owner is truthy (project-content.tsx:1026: {project.owner && <ProjectMembersDialog .../>}), but Project.ownerId is nullable with onDelete: SetNull (prisma/schema.prisma:421,425). onManageMembers is still passed unconditionally (project-content.tsx:930), so the Overview's 'Add member' button (project-overview.tsx:889-897) and 'Manage all →' link (879-886) set membersDialogOpen=true, yet no dialog exists to open — the click does nothing, with no error.
SCENARIO: The user account that owned a project is deleted (ownerId set to null). A remaining ADMIN member opens the Overview tab and clicks 'Add member' to bring a colleague in; nothing happens at all, and members can no longer be managed from the UI for that project.
FIX: Render ProjectMembersDialog regardless of owner (make the owner prop optional in the dialog), or disable the buttons with an explanatory tooltip when project.owner is null.
VERIFIER: Reproduced full chain from source: (1) prisma/schema.prisma:421 `ownerId String?` and :425 `owner User? @relation("ProjectOwner", ..., onDelete: SetNull)` — owner is nullable and is nulled on user deletion; user deletion actually exists (src/app/api/users/account/route.ts:23 `prisma.user.delete`). (2) Server page src/app/(dashboard)/projects/[projectId]/page.tsx:36-43 includes owner (null when ownerId is null) and passes it through at :189; client type confirms nullability at src/components/projects/project-content.tsx:106-111 (`owner: {...} | null`). (3) project-content.tsx:928-931 passes `onManageMembers={() => setMembersDialogOpen(true)}` to ProjectOverview unconditionally. (4) project-ov…

### [low] Rename/Delete/Archive header actions swallow non-ok API responses with no user feedback
FILE: src/components/projects/project-content.tsx:392 (area shell)
SUMMARY: All four project-name-dropdown actions use the pattern `.then(res => { if (res.ok) { ...success... } }).catch(() => toast.error(...))` (rename 388-395, duplicate 401-407, archive 414-420, delete 427-429). The .catch only fires on network failures; a 400/403/500 JSON response (e.g. a COMMENTER/VIEWER member hitting the PATCH permission check at src/app/api/projects/[projectId]/route.ts:151-161, or the DELETE check at 228-236) results in nothing happening at all.
SCENARIO: A project member with COMMENTER role clicks Rename, enters a new name, and presses OK. The server returns 403 'You don't have permission to edit this project', but the UI shows no toast and the name stays unchanged with no explanation.
FIX: Add an else branch to each handler: parse the error JSON and call toast.error(err.error || 'Failed to …').
VERIFIER: CONFIRMED. Evidence chain: (1) src/components/projects/project-content.tsx — Rename 388-395, Duplicate 401-407, Archive 414-420, Delete 427-429 all use `.then(res => { if (res.ok) {...} }).catch(() => toast.error("Operation failed"))` with no else branch; fetch resolves (does not reject) on 4xx/5xx, so .catch never fires for HTTP error responses and a non-ok response produces zero UI feedback. (2) Menu items are rendered unconditionally — no canEdit/role gating in the file (only a `role: string` type at line 114) and no global fetch interceptor in src/. (3) src/app/api/projects/[projectId]/route.ts returns 403 JSON for PATCH at 156-161 (non owner/ADMIN/EDITOR) and for DELETE at 231-236 (non …

### [low] 'Manage members' count double-counts the owner
FILE: src/components/projects/project-content.tsx:519 (area shell)
SUMMARY: The mobile overflow menu renders 'Manage members ({project.members.length + 1})', assuming the owner is not in members. But POST /api/projects creates the owner as a ProjectMember with role ADMIN (src/app/api/projects/route.ts:411-415), so for every project created through the app the owner is already inside project.members and the +1 double-counts them.
SCENARIO: A brand-new project with only its creator shows 'Manage members (2)' in the mobile menu even though exactly one person is on the project.
FIX: Count distinct people: `new Set([project.owner?.id, ...project.members.map(m => m.userId)].filter(Boolean)).size`, or drop the +1 since the owner is a member row.
VERIFIER: CONFIRMED. Chain: (1) POST /api/projects creates the owner as a ProjectMember role ADMIN — src/app/api/projects/route.ts:400 (ownerId: userId) and :411-416 (members: { create: { userId, role: "ADMIN" } }). (2) The project page includes members with no owner exclusion — src/app/(dashboard)/projects/[projectId]/page.tsx:44-55, serialized :172-175, passed to ProjectContent :189. (3) src/components/projects/project-content.tsx:519 renders "Manage members ({project.members.length + 1})", so a brand-new project with only its creator shows "(2)". No guard/dedupe exists anywhere in the chain. Internal inconsistency proves the wrong assumption: the desktop avatar row in the same file (project-content…

### [low] Template gallery ignores failed workflow-template application (non-ok response never warned)
FILE: src/components/projects/create-project-gallery.tsx:359 (area shell)
SUMMARY: After creating a project from a template, the gallery POSTs /api/projects/[id]/workflow/templates inside try/catch, but never checks res.ok (create-project-gallery.tsx:357-374). The catch (which shows the 'couldn't seed workflow rules' warning) only fires on network errors; a 400/403/404/500 from the route (src/app/api/projects/[projectId]/workflow/templates/route.ts:107-137, 232-238) resolves normally and the user gets an unconditional success toast.
SCENARIO: The workflow route hits its 500 path (or a permission edge) while applying 'permit-cycle' rules; the user sees only 'Project "X" created' and lands on a project silently missing the advertised automation rules ('Workflow rules … will be applied automatically').
FIX: Capture the response and trigger the existing toast.warning when !res.ok, not just on thrown fetch errors.
VERIFIER: CONFIRMED. Chain reproduced: (1) create-project-gallery.tsx:357-374 — inner POST to /api/projects/[id]/workflow/templates awaits fetch but never checks res.ok; the catch at :369 only fires on rejected promises (network errors), and fetch resolves normally on HTTP 4xx/5xx, so the toast.warning at :370-372 is unreachable for HTTP errors and execution falls through to the unconditional toast.success at :376. The outer project-create fetch at :348-351 DOES check res.ok, proving the pattern was known and simply omitted here. (2) Route src/app/api/projects/[projectId]/workflow/templates/route.ts POST returns non-ok on many paths: 401 (:101), 404/403 access (:106-111), 403 no-edit-permission (:112-…

### [low] /projects/new default start date uses UTC, defaulting to tomorrow for US evening users
FILE: src/app/(dashboard)/projects/new/page.tsx:170 (area shell)
SUMMARY: The start-date input is initialized with `new Date().toISOString().split('T')[0]` (new/page.tsx:170), which is the UTC calendar date. For a US user after ~7-8 PM local, that is tomorrow's date. This start date seeds all template task due dates server-side ('Task due dates will be calculated from this date', and POST route.ts:532-542 offsets relativeDueDate from it).
SCENARIO: A Miami user creates a template project at 9 PM EDT on July 3; the form silently defaults to July 4 and every generated task due date is shifted one day later than intended.
FIX: Build the default from local date parts, e.g. `format(new Date(), 'yyyy-MM-dd')` from date-fns or manual getFullYear/getMonth/getDate padding.
VERIFIER: Evidence chain reproduced end-to-end. (1) src/app/(dashboard)/projects/new/page.tsx:170 — `const [startDate, setStartDate] = useState(new Date().toISOString().split("T")[0])`; `toISOString()` is always UTC, so for a user in EDT (UTC-4) after 8 PM local (or 7 PM EST), the string is tomorrow's calendar date. (2) The value is rendered into the visible date input (page.tsx:294-303, id="startDate", value={startDate}) with helper text "Task due dates will be calculated from this date" (page.tsx:308), and posted unchanged as `startDate` in the create-project body (page.tsx:191-198). (3) Server side, src/app/api/projects/route.ts:532 does `const projectStartDate = startDate ? new Date(startDate) : n…

### [low] Members dialog search offers users from all of the caller's workspaces; adding one from another workspace fails
FILE: src/app/api/users/search/route.ts:22 (area team)
SUMMARY: ProjectMembersDialog searches via /api/users/search (project-members-dialog.tsx:119), which matches users across ALL workspaces the caller belongs to (users/search/route.ts:17-41), not the project's workspace. The members POST then rejects users outside project.workspaceId with 404 'User is not a member of this workspace' (members/route.ts:148-161). The dialog's exclusion filter (project-members-dialog.tsx:124-128) only removes current members/owner, not wrong-workspace users.
SCENARIO: A user in two workspaces opens the members dialog on a workspace-A project, searches a colleague who only exists in workspace B, sees them listed as addable, clicks — and gets 'User is not a member of this workspace'.
FIX: Accept a workspaceId/projectId param on /api/users/search and filter workspaceMembers.some to the project's workspace; pass projectId from ProjectMembersDialog.
VERIFIER: CONFIRMED — full chain reproduced. (1) src/components/projects/project-members-dialog.tsx:119-120 fetches `/api/users/search?q=...` with no workspace/project scoping param. (2) src/app/api/users/search/route.ts:17-22 collects ALL workspaceIds the caller belongs to, and lines 34-40 match any user with `workspaceMembers.some.workspaceId IN workspaceIds` — so a caller in workspaces A and B searching from a workspace-A project gets workspace-B-only users back. (3) The dialog's exclusion filter (project-members-dialog.tsx:124-128) only removes owner.id and current members' userIds; wrong-workspace users remain listed as addable. (4) addMember (project-members-dialog.tsx:139-146) POSTs to /api/pro…

### [low] Members dialog exposes full manage controls (add, role change, remove) to non-admin members; every action fails with a 403
FILE: src/components/projects/project-content.tsx:463 (area team)
SUMMARY: The header avatar stack button and the 'Manage members' menu item open ProjectMembersDialog for anyone who can see the page (project-content.tsx:463-467, 517-519) with no permission gating, and the dialog itself renders the search/add list, role dropdowns and Remove for every viewer. The API correctly gates all mutations to project Owner/ADMIN via requireProjectAdmin (members/route.ts:141, 276, 337), so an EDITOR/COMMENTER/VIEWER's clicks always return 403; because removeMember/changeRole discard the error body (project-members-dialog.tsx:170, 186), they just see generic 'Failed to...' toasts. The Team tab, by contrast, correctly hides its controls behind canWrite.
SCENARIO: An EDITOR member clicks the avatar stack, searches a colleague and clicks to add them (or changes someone's role): the UI looks fully enabled, then fails with an unexplained 'Failed to add member' toast every time.
FIX: Fetch/derive the caller's write permission (owner or ADMIN member — e.g. reuse the canWrite flag from /companies or compare session user against project.ownerId/members) and render the dialog read-only otherwise; also propagate server error messages in removeMember/changeRole.
VERIFIER: CONFIRMED. Evidence chain reproduced: (1) Client entry points — src/components/projects/project-content.tsx:463-467 (header avatar-stack <button> with onClick={() => setMembersDialogOpen(true)}, title="Manage members") and :517-519 (mobile overflow "Manage members" DropdownMenuItem), plus :930 (onManageMembers) — none wrapped in any permission check; the surrounding JSX (lines 440-540) has no canWrite/isAdmin/ownerId gating. (2) Dialog — src/components/projects/project-members-dialog.tsx: Props interface (lines 49-55) accepts no permission/read-only prop; it unconditionally renders the search+add list (lines 206-262, addMember POST at 139-161), a role dropdown with all four roles (lines 317-…

### [low] Two concurrent 'host firm' writes can leave a project with two isOwn companies (demote+create not transactional)
FILE: src/app/api/projects/[projectId]/companies/route.ts:185 (area team)
SUMMARY: Both the companies POST (route.ts:185-200) and the company PATCH (companies/[companyId]/route.ts:100-121) enforce the 'only one isOwn=true' invariant with a separate updateMany(demote) followed by a create/update, outside any transaction. Two overlapping requests marking different companies as own can interleave (both demotes run before either write), ending with two isOwn=true rows; the UI then shows two gold 'YOU' badges and the isOwn-desc ordering (route.ts:98) becomes ambiguous.
SCENARIO: Two project admins simultaneously edit different firms and check 'This is our firm' (or one adds a new own firm while the other edits): after both saves, the Team tab shows two firms starred as the host firm.
FIX: Wrap the demote updateMany and the create/update in prisma.$transaction in both routes.
VERIFIER: CONFIRMED. Evidence chain: (1) POST src/app/api/projects/[projectId]/companies/route.ts:185-190 runs `prisma.projectCompany.updateMany({where:{projectId,isOwn:true},data:{isOwn:false}})` then a separate awaited `create` at :192-200 with `isOwn: parsed.data.isOwn ?? false` — two independent Prisma calls, no $transaction. (2) PATCH src/app/api/projects/[projectId]/companies/[companyId]/route.ts:100-105 does the same demote updateMany, then a separate `update` at :107-121 setting isOwn=true — also not transactional. Interleaving (A-demote, B-demote, A-write own=true, B-write own=true) leaves two isOwn=true rows. (3) No DB/schema guard: prisma/schema.prisma:481-506 ProjectCompany has only `@@uni…

### [low] Right-edge resize on a task without startDate moves the whole bar instead of extending it
FILE: src/components/views/timeline-view.tsx:519 (area timeline)
SUMMARY: A right-handle drag PATCHes only `dueDate` (timeline-view.tsx:519-521). But for tasks with no startDate, the bar is rendered with an implied start of dueDate − 7 days (lines 352-354). After the mutation and router.refresh(), the implied start recomputes from the NEW due date, so the bar keeps its fixed 7-day width and translates right instead of growing — diverging from the drag preview, which showed the width increasing (lines 1190-1196 grow width for handle "right").
SCENARIO: A task has only a due date. The user grabs the right edge of its 7-day bar and drags +5 days to extend the duration. On release the bar jumps: same 7-day width, shifted 5 days right — the left edge silently moved even though the user never touched it.
FIX: When resizing the right edge of a task without a persisted startDate, also send the current implied startDate (originalDue − 7d) in the PATCH body so the left edge is pinned, mirroring what the left-handle branch already does.
VERIFIER: Chain reproduced end-to-end. (1) Drag start: timeline-view.tsx:475-487 — handleResizeStart stores originalStart = task.startDate || null and originalDue. (2) Right-handle mouseup: timeline-view.tsx:519-521 — body gets only dueDate = originalDue + deltaDays; startDate is never sent (contrast left handle at 513-518 and "move" at 522-531, which both compute the implied start `addDays(parseISO(originalDue), -7)` and persist it). (3) API: src/app/api/tasks/[taskId]/route.ts:333-351 — PATCH updates only the fields present in the body; no server-side backfill of startDate, so it remains null. (4) Render after router.refresh (timeline-view.tsx:541): getTaskPosition at 352-354 computes taskStart = st…

### [low] Dependencies endpoint access rules diverge from page access: admins on private projects silently lose dependency arrows; workspace members can read deps for projects whose page 404s them
FILE: src/app/api/projects/[projectId]/dependencies/route.ts:31 (area timeline)
SUMMARY: assertProjectAccess (dependencies/route.ts:15-45) allows owner/member/PUBLIC, plus any workspace member for WORKSPACE visibility, else 403. The project page (src/app/(dashboard)/projects/[projectId]/page.tsx:136-158) instead allows owner/member/PUBLIC plus workspace OWNER/ADMIN role or level>=4 for ANY visibility (including PRIVATE). Two mismatches: (1) a workspace admin viewing a private project's Timeline gets 403 from this endpoint — the client swallows it (`r.ok ? r.json() : []`, timeline-view.tsx:151) so dependencies silently render as none and the Dependencies button is disabled (line 760); (2) a plain workspace member who cannot open a WORKSPACE-visibility project page (page requires admin/level>=4 in that branch) can still fetch its dependency rows.
SCENARIO: A workspace ADMIN opens a private project's Timeline tab: the dependency count shows nothing and the toggle is greyed out even though the project has FS links, with no error surfaced.
FIX: Extract one shared project-access helper and use it in both the page and this route so read visibility is identical.
VERIFIER: CONFIRMED — full chain reproduced. (A) Route access: src/app/api/projects/[projectId]/dependencies/route.ts:29-44 — assertProjectAccess grants access only to project owner (line 29), project member (line 30), PUBLIC visibility (line 31), or any workspaceMember when visibility === "WORKSPACE" (lines 35-42); everything else 403 (line 44). There is no role/level check anywhere in the route. (B) Page access: src/app/(dashboard)/projects/[projectId]/page.tsx:136-155 — hasAccess = owner || member || PUBLIC (line 136), else workspace membership with role OWNER/ADMIN or getLevel(position) >= 4 grants access for ANY visibility including PRIVATE (lines 148-154); otherwise notFound() (line 158). Mismat…

### [low] Dependency arrows/count go stale: dependencies are fetched once on mount and never refetched after mutations
FILE: src/components/views/timeline-view.tsx:148 (area timeline)
SUMMARY: The dependencies list is loaded in a useEffect keyed only on projectId (timeline-view.tsx:148-161). Adding or removing a dependency from the task detail panel (which posts to /api/tasks/[taskId]/dependencies) followed by router.refresh() re-renders the server components but does not re-run this client effect, so the SVG arrows and the count badge next to the Dependencies toggle (lines 764-768) keep showing the pre-edit graph until the tab is remounted or the page is hard-reloaded.
SCENARIO: On the Timeline tab, the user opens a task panel and adds "blocked by" on another task in the same project. Back on the chart, no new arrow appears and the Dependencies counter is unchanged until they switch tabs and return.
FIX: Refetch dependencies after task-panel mutations — e.g. key the effect on a refresh counter bumped when the detail panel closes, or expose a refetch callback and call it alongside router.refresh().
VERIFIER: CONFIRMED. Evidence chain: (1) timeline-view.tsx:145-161 — `dependencies` is client state loaded by a useEffect keyed only on [projectId], fetching /api/projects/${projectId}/dependencies once; no other code path in the component ever refetches or mutates it (only setDependencies inside this effect). (2) The count badge and disabled state read dependencies.length at timeline-view.tsx:760-768, and the same state drives the arrow rendering (comment at 136-138). (3) project-content.tsx:949-955 renders TimelineView when currentView === "timeline", and project-content.tsx:1009-1014 renders TaskDetailPanel as a sibling side panel — TimelineView remains mounted while the panel is open, so the effec…

### [low] Portfolio Gantt (projects/all Timeline) renders project bars and milestone diamonds one day early for US timezones
FILE: src/components/projects/gantt-timeline.tsx:519 (area timeline)
SUMMARY: GanttBar computes `left = Math.floor((visibleStart - rangeStart)/86400000) * dayPx` (gantt-timeline.tsx:518-521) where visibleStart comes from `new Date(project.startDate!)` — a UTC-midnight instant — while rangeStart is a local-time Date (lines 143-148). For users west of UTC the UTC-midnight instant falls on the previous local day, so floor() lands the bar one day-cell early; the same happens for endDate (521-529), the overdue check in LaneLabel (455-458, flags a project overdue during its whole final day), and milestone diamonds (554-563).
SCENARIO: On /projects/all in Timeline view, a project ending July 31 draws its right edge under July 30, and appears with the gold overdue ring on July 31 itself even though it is due that day.
FIX: Parse project/task dates from their yyyy-MM-dd component (or shift by getTimezoneOffset) before differencing against the local rangeStart, matching the fix needed in timeline-view.tsx.
VERIFIER: CONFIRMED — full chain reproduced. (1) Dates are stored as UTC-midnight instants: create-project-dialog.tsx:440/449 uses <Input type="date"> so the client sends plain "yyyy-MM-dd" strings (payload built at lines 233-234); the server does `new Date(data.startDate/endDate)` in src/app/api/projects/[projectId]/route.ts:167-168 (and route.ts:401 on create), which for a yyyy-MM-dd string is UTC midnight; Prisma fields are plain DateTime (schema.prisma:312-313), and GET /api/projects returns rows raw via NextResponse.json (src/app/api/projects/route.ts:222), serializing to e.g. "2026-07-31T00:00:00.000Z". /projects/all fetches this JSON (page.tsx:152) and passes filtered projects to GanttTimeline …

### [low] Re-applying a template duplicates all of its rules, doubling comments/subtasks on every future section move
FILE: src/app/api/projects/[projectId]/workflow/templates/route.ts:201 (area workflow)
SUMMARY: POST /workflow/templates dedupes sections by name (templates/route.ts:160-171) but creates rules unconditionally in the loop at 201-219 — there is no check for an existing rule with the same trigger+actions. The dialog copy encourages layering templates and nothing marks a template as already applied, so applying the same one twice yields two identical rules; ADD_COMMENT is not idempotent in the engine, so each trigger then posts duplicate comments (subtask/complete/priority actions are idempotent, comments are not).
SCENARIO: User applies 'RFI lifecycle', later forgets and applies it again from the gallery (the card shows no 'applied' state). From then on every task moved to 'RFI Open' receives the same guidance comment twice and fires SET_PRIORITY twice; the section cards show every chip duplicated.
FIX: Skip creating a rule when one with a deep-equal trigger and actions already exists on the workflow (report it in `skipped`), or mark applied templates in the gallery.
VERIFIER: CONFIRMED — full chain reproduced from source. (1) POST route: src/app/api/projects/[projectId]/workflow/templates/route.ts dedupes sections by lowercase name (lines 149-171, `byName.get(name.toLowerCase())` skips creation) but the rule loop at lines 201-219 calls `tx.workflowRule.create` unconditionally for every template rule — there is no query for an existing rule with an equal trigger/actions, and `skipped` (line 200) is only populated for unresolvable sections. Re-POSTing the same templateId therefore inserts a second identical WorkflowRule row per rule. (2) Engine: src/lib/workflow-engine.ts `executeRulesMatching` (lines 80-101) loads ALL rules on all active workflows and runs every m…

### [low] When the workflow fetch fails the header renders 'undefined rules configured'
FILE: src/components/views/workflow-view.tsx:421 (area workflow)
SUMMARY: If GET /workflow fails, the catch only toasts and `workflow` stays null while loading is set false (workflow-view.tsx:198-206). The render then evaluates `workflow?.rules.length === 0` as false, choosing the non-empty branch, and interpolates `${workflow?.rules.length} rule...` (lines 421-423) which prints the literal string 'undefined rules configured.' with no retry affordance; the section cards all claim 'No automations configured' even though rules may exist.
SCENARIO: A transient network error or 500 during tab load: the user sees the heading 'Workflow — undefined rules configured.' and empty cards, and may start re-adding rules that already exist (creating duplicates once connectivity returns).
FIX: Track an error state; when workflow is null render an error panel with a Retry button instead of the normal layout.
VERIFIER: CONFIRMED. Evidence chain (src/components/views/workflow-view.tsx): (1) line 131 `useState<WorkflowRow | null>(null)` — workflow starts null; (2) lines 184-206 — on `!wfRes.ok` the code throws (line 188), catch at 198-203 only toasts, never sets workflow or any error state, finally at 205 sets loading=false; (3) render has exactly one early return, the loading spinner at 398-404 — no `if (!workflow)` guard exists anywhere in the file (grep for setWorkflow/useState/!workflow confirms); (4) lines 419-423: `workflow?.rules.length === 0` is `undefined === 0` → false, so the else branch interpolates `${workflow?.rules.length} rule${...}` → literal text "undefined rules configured." with heading "…

### [polish] Quick-add input overlaps the '+N more' pill and spills below the week row when the day's lanes are full
FILE: src/components/views/calendar-view.tsx:820 (area calendar)
SUMMARY: The inline add input is placed at gridRow addingLane+1 where addingLane = max visible lane in that column + 1 (calendar-view.tsx:612-624, 820-826). When the column has hidden tasks, the '+N more' pill occupies gridRow columnMaxLane+2 (:754) — the exact same grid cell, so the input renders on top of the pill. When the column already uses all MAX_LANES (6) lanes with no overflow, the input lands in a row the weekHeights calc (:392-425) never accounted for (heights only count segments), so it renders past the week's fixed height and is painted over by the following week's row (later positioned sibling), leaving it partially hidden while focused.
SCENARIO: User clicks a crowded day (6+ tasks): the type-a-task input either covers the '+3 more' pill or appears clipped/overlapped by the next week's row, looking broken while they type.
FIX: Include the pending add-row in the weekHeights computation (treat addingForDate's column as one extra lane), and place the input at max(addingLane, columnMaxLane+1)+1 when an overflow pill exists.
VERIFIER: CONFIRMED. Chain reproduced in src/components/views/calendar-view.tsx: (1) Pill overlap — addingLane = max visible lane in column + 1, computed from visibleSegments only (:612-624); input placed at gridRow addingLane+1 (:824), same gridColumn as the '+N more' pill (:823 vs :753); pill sits at gridRow columnMaxLane+2 (:754) where columnMaxLane is the same visible-lane max (:739-748), so input row = columnMaxLane+2 = pill's row — identical grid cell. Explicitly co-placed grid items stack, and the input block (:819-849) is later in DOM, so it paints over and pointer-blocks the pill. (2) Height spill — weekHeights memo (:392-425) deps are [weeks, segmentsByWeek] only; addingForDate never contrib…

### [polish] Section-chart empty state message never matches its condition; zero-task sections render as blank bars
FILE: src/components/views/dashboard-view.tsx:241 (area dashboard)
SUMMARY: tasksBySection (lines 126-134) maps over ALL sections, producing an entry (with incomplete: 0) even for sections that contain no tasks. The guard at line 241 is tasksBySection.length > 0, so the EmptyChartState with message "No sections with tasks" (line 279) only appears when the project has zero sections at all. A project whose sections exist but hold no tasks (or only completed tasks) instead shows an axes-only chart of invisible zero-height bars.
SCENARIO: User opens the Dashboard tab of a freshly created project that has the default "To do / Doing / Done" sections but no tasks yet; instead of the intended "No sections with tasks" empty state, they see an empty-looking grid with section names along the X axis and no explanation.
FIX: Change the guard to check whether any section has tasks (e.g. tasksBySection.some(s => s.total > 0)) — or, to match the chart's title, whether any has incomplete > 0 — before rendering the BarChart, falling back to EmptyChartState otherwise.
VERIFIER: CONFIRMED. Evidence chain: (1) New projects are created with default sections "To do / In progress / Done" and no tasks — src/app/api/projects/route.ts:352-367. (2) The parent passes sections unfiltered: project-content.tsx:244-248 builds filteredSections by mapping over ALL project.sections (search/filter logic at lines 250-274 only filters the tasks array inside each section, never removes an empty section), and passes it to DashboardView at project-content.tsx:963-967. (3) In src/components/views/dashboard-view.tsx:126-134, tasksBySection = sections.map(...) produces one entry per section with incomplete: 0 / total: 0 for taskless sections, so its length equals the section count regardles…

### [polish] Dashboard grid is not responsive even though the Dashboard tab is exposed on mobile
FILE: src/components/views/dashboard-view.tsx:210 (area dashboard)
SUMMARY: The Dashboard tab button is one of the tabs visible on mobile (project-content.tsx:684-694 — it lacks the hidden md:flex used for Workflow/Messages/Files/Team). But the view uses fixed grid-cols-4 for the KPI row (line 210) and grid-cols-2 for both chart rows (lines 238, 320) with no responsive breakpoints, plus a fixed left margin of 80px on the assignee chart (line 329). On a ~375px phone each KPI card gets ~80px and each chart ~170px, clipping the 4-digit KPI values' padding and squeezing the recharts plots into unreadable slivers.
SCENARIO: User opens a project on their phone and taps the Dashboard tab: four crushed KPI cards share one row and the charts render a few pixels wide next to each other instead of stacking.
FIX: Use responsive classes: grid-cols-2 md:grid-cols-4 for the KPI row and grid-cols-1 md:grid-cols-2 for the chart rows, mirroring the responsive treatment used elsewhere in project-content.tsx.
VERIFIER: CONFIRMED. Full chain reproduced: (1) Dashboard tab is visible on mobile — src/components/projects/project-content.tsx:684-694 renders the Dashboard button with `flex items-center ...` (no hidden prefix), while Workflow (line 708) and later tabs use `hidden md:flex`, so on <md viewports the Dashboard tab remains tappable and handleViewChange("dashboard") renders <DashboardView> (project-content.tsx:963-968) with no responsive wrapper. (2) src/components/views/dashboard-view.tsx:210 is exactly `grid grid-cols-4 gap-4 mb-6` for the KPI row; lines 238 and 320 are `grid grid-cols-2 gap-4` for both chart rows; line 329 is `margin={{ top: 20, right: 30, left: 80, bottom: 5 }}` on the vertical assi…

### [polish] 'Download' action never downloads — it opens the file in a new tab, previewing images/PDFs instead
FILE: src/components/views/files-view.tsx:166 (area files)
SUMMARY: handleDownload (files-view.tsx:166-170) is just window.open(file.url, '_blank'). Vercel Blob serves files with inline content-disposition for browser-renderable types, so for images, PDFs, text, video and audio the 'Download' button/menu item (lines 370, 433) opens a preview tab rather than saving the file. There is also no rel=noopener equivalent and no filename preservation (blob URLs contain a UUID prefix, storage.ts:57, so even a manual save gets 'uuid-name.pdf').
SCENARIO: User clicks the explicit Download icon on a photo card expecting the file in their Downloads folder; instead a new tab opens showing the image, and a manual Ctrl+S saves it under the UUID-prefixed blob name.
FIX: Use Vercel Blob's download URL (append `?download=1` to blob URLs, or store/use blob.downloadUrl) or create a temporary <a href={url} download={file.name}> and click it, so the button actually downloads with the original filename.
VERIFIER: CONFIRMED. Full chain reproduced: (1) handleDownload is exactly `window.open(file.url, "_blank")` with no download attribute or downloadUrl — src/components/views/files-view.tsx:166-170. (2) It is wired to the explicit Download icon button (files-view.tsx:368-373), the card body click (line 348), the list-row click (line 405), and the dropdown "Download" menu item (lines 433-436). (3) file.url comes from GET /api/projects/[projectId]/attachments which returns the stored Attachment.url verbatim (src/app/api/projects/[projectId]/attachments/route.ts:54). (4) That url is blob.url from Vercel Blob `put` with access:"public" (src/lib/storage.ts:59-63, called from src/app/api/tasks/[taskId]/attach…

### [polish] Bulk bar Delete button uses text-black on the near-black slate-900 bar, making it illegible
FILE: src/components/views/list-view.tsx:1086 (area list)
SUMMARY: The floating bulk-actions bar is `bg-slate-900 text-white` (list-view.tsx:1036) but the Delete button overrides with `text-black hover:bg-black/30 hover:text-gray-300` (1086), so its label and trash icon render black on #0f172a - effectively invisible until hovered. All sibling buttons use text-white.
SCENARIO: User selects tasks and looks at the dark bulk bar: Complete, Move to and Priority are readable but the Delete action appears as an empty gap; users may not discover bulk delete at all.
FIX: Change the Delete button classes to text-white (e.g. `text-white hover:bg-slate-700`) to match the other bulk bar buttons.
VERIFIER: Reproduced the full chain. (1) src/components/views/list-view.tsx:1036 — bulk bar container is `bg-slate-900 text-white` (slate-900 = #0f172a). (2) Sibling buttons Complete (line 1042), Move to (1050), Priority (1068) all pass `text-white hover:bg-slate-700`. (3) Delete button at line 1086 passes `text-black hover:bg-black/30 hover:text-gray-300` on a ghost-variant Button. (4) src/components/ui/button.tsx:19-20 — ghost variant is `text-foreground hover:bg-accent hover:text-accent-foreground`; line 56 merges via `cn(buttonVariants({variant,size,className}))`, so the caller's `text-black` wins the tailwind-merge over `text-foreground` (both are text-color utilities, caller class comes last). N…

### [polish] Quarter zoom columns are anchored to the current month, not calendar quarters, so Q labels cover the wrong months
FILE: src/components/views/timeline-view.tsx:253 (area timeline)
SUMMARY: At quarter zoom the columns start at startOfMonth(currentDate) and step +3 months (timeline-view.tsx:253-261), while the label is derived from the column's first month (`Q${Math.floor(date.getMonth()/3)+1}`, lines 293-295). If today is February, the first column spans Feb–Apr but is labeled "Q1", the next spans May–Jul labeled "Q2", etc. Also, monthGroups shows a single year label for the whole strip even when the 24-month quarter range spans 2-3 calendar years (lines 312-316).
SCENARIO: In February a user zooms to Quarter: the header shows Q1 over a Feb-Apr window and the year row shows only the starting year across a range that extends into the next year.
FIX: Snap the quarter columns to startOfQuarter(currentDate) (date-fns startOfQuarter) and group header years per column-year rather than using only columns[0].
VERIFIER: CONFIRMED. Chain reproduced in src/components/views/timeline-view.tsx: (1) currentDate = new Date(), never quarter-snapped (line 121; quarter nav adds amount*6 months at line 568, preserving month offset). (2) Quarter columns anchor at startOfMonth(currentDate) (line 274) and step +3 months (lines 253-261); startOfQuarter is never used. (3) Label = Q${floor(date.getMonth()/3)+1} (lines 293-294), so with currentDate in Feb the first column spans Feb-Apr yet is labeled Q1, next May-Jul labeled Q2, etc.; labels are only correct in Jan/Apr/Jul/Oct (coincidentally correct today, Jul 3). (4) monthGroups returns one group labeled with columns[0]'s year spanning all 8 quarter columns (lines 313-315,…

### [polish] Dead '⋯' button on every section card and a no-op 'Mark complete' option on the On-completion card
FILE: src/components/views/workflow-view.tsx:761 (area workflow)
SUMMARY: Each SectionCard header renders a MoreHorizontal button with hover styling but no onClick (workflow-view.tsx:761-763, styled `cursor-default`), so it looks interactive and does nothing. Separately, the CompletionCard's action dropdown reuses PICKABLE_ACTIONS including MARK_COMPLETE (lines 914-923): a 'when task is completed → mark complete' rule is a pure no-op yet can be created and counts toward the rule total.
SCENARIO: User clicks the ⋯ menu on a section card expecting rename/delete options — nothing happens. Another user adds 'Mark complete' to the On-completion card and a useless rule chip appears that only inflates the configured-rules count.
FIX: Remove the ⋯ button (or wire it to real section actions), and exclude MARK_COMPLETE from the CompletionCard's pickable actions.
VERIFIER: CONFIRMED, both halves. (1) Dead ⋯ button: src/components/views/workflow-view.tsx:761-763 renders `<button className="p-1 hover:bg-slate-100 rounded text-slate-300 cursor-default"><MoreHorizontal/></button>` inside SectionCard's header — no onClick, not wrapped in any DropdownMenu/Trigger (unlike the real "Add action" dropdown at 811-831). It has hover:bg-slate-100 feedback so it reads as interactive but does nothing; no other handler (keyboard, parent delegation) exists in the component. (2) No-op MARK_COMPLETE on the On-completion card: CompletionCard maps PICKABLE_ACTIONS at workflow-view.tsx:914, and PICKABLE_ACTIONS (defined line 85) includes "MARK_COMPLETE" (line 89) — the same unfilte…

---

# HALLAZGOS REFUTADOS (no son bugs reales)

### [refutado] Clicking a Gantt bar fails to open the task detail panel due to a stale isResizing closure
FILE: src/components/views/timeline-view.tsx:1282 (area timeline)
REFUTACION: REFUTED — the claimed timing (setDragState(null) not flushed before the click event) is wrong for this codebase's React version (react 19.2.3 / react-dom 19.2.3, package.json:58,60; concurrent root via Next 16). Chain reproduced from source: mousedown on the bar calls handleResizeStart(e, task.id, "move", task) (timeline-view.tsx:1268-1274) which sets dragState (475-487); the effect at 489-554 attaches a native document mouseup listener; on a plain click deltaDays === 0 so it runs setDragState(null) and returns synchronously (504-510); onClick gates on the render-captured isResizing (1182-1183, 1275-1285). All as the finding says — but the final inference fails: 1. A setState called inside a…

### [refutado] Task PATCH response is built before workflow rules run, so views show pre-rule state (wrong priority/assignee/completed) after a drop
FILE: src/app/api/tasks/[taskId]/route.ts:507 (area workflow)
REFUTACION: The server-side fact is accurate but the claimed impact chain does not reproduce. VERIFIED FACTS: PATCH /api/tasks/:id captures `task` inside the transaction (route.ts:372-410), awaits executeRulesOnSectionChange/executeRulesOnTaskCompleted at route.ts:446-462 (which mutate the row via prisma.task.update in workflow-engine.ts:122-165, 184-192), and returns the pre-rule snapshot `{...task}` at route.ts:507. REFUTATION OF THE SCENARIO: (1) The cited drop scenario never touches this endpoint. List and Board drags call POST /api/tasks/reorder (list-view.tsx:414-421, board-view.tsx:164-168), not PATCH. No client anywhere sends sectionId through PATCH /api/tasks/:id (grep of all JSON bodies: secti…


---

# ESTADO DE CORRECCIONES — aplicadas por Fable 5 (3 Jul 2026)

Se corrigieron los 2 críticos, los 26 altos y la mayoría de medios/bajos en una sola pasada (build de producción `next build` ✓ compiled successfully, 129/129 páginas). Cambios: 46 archivos modificados + 2 nuevos (`src/lib/project-access.ts`, `src/app/api/projects/[projectId]/duplicate/route.ts`), ~1600 líneas.

## Fundaciones (fix-once, apply-everywhere)
- **`src/lib/project-access.ts` (nuevo)**: helper canónico de acceso a proyecto (owner | miembro | PUBLIC | OWNER/ADMIN de workspace | nivel≥4), con `canWrite`/`canManage`. Reemplaza los `assertProjectAccess` inline que filtraban `visibility=WORKSPACE`.
- **`src/lib/auth-guards.ts`**: `verifyTaskAccess`/`verifyProjectAccess`/`verifyBulkTaskAccess` ahora enforcean acceso a nivel de proyecto (cierra el crítico de seguridad y el timeline-drag). `requireWrite` bloquea a VIEWER/COMMENTER.
- **`src/lib/date-only.ts`** (`dueDateToLocalMidnight`): aplicado en List, Board, Timeline, Calendar, Dashboard, Overview, Gantt — cierra toda la familia de bugs de día-corrido para US.
- **Columna aditiva `Project.isArchived`** aplicada a prod vía `prisma db execute` (Archive real; filtrada del listado).

## Diferido (baja severidad, documentado)
Quedan ~15 hallazgos medium/low/polish de menor impacto: paginación de mensajes/submissions/attachments (cursor), reglas de workflow huérfanas, efectos secundarios del engine (SET_ASSIGNEE/MARK_COMPLETE sin goal-recalc — riesgo de recursión, requiere diseño), scoping de `/api/team/directory` y `/api/users/search` al workspace del proyecto, transacción en el invariante isOwn de companies, edición de menciones en mensajes, cap de 4.5MB de Vercel, y 3 polish (solape de input en Calendar, snapping de trimestre en Timeline, margen del chart de asignados en Dashboard).

## Auto-auditoría adversarial del diff (Fable 5)
Tras aplicar las correcciones, un workflow de revisión adversarial sobre el propio diff (patrón de la revisión de Home) encontró **3 regresiones reales**, todas corregidas:
1. **[alto]** El endurecimiento de `verifyTaskAccess` dejó fuera a los **colaboradores/seguidores** de tarea que no son miembros del proyecto → 404 al abrir la tarea que siguen. Fix: `verifyTaskAccess` ahora incluye `TaskCollaborator` como lazo personal de LECTURA (no de escritura).
2. **[alto]** Añadí `requireWrite` a PATCH/DELETE de tarea individual pero `verifyBulkTaskAccess` seguía chequeando solo LECTURA → un VIEWER/COMMENTER podía borrar/completar/reasignar en masa. Fix: `verifyBulkTaskAccess` ahora exige `canWrite` (o ser creator/assignee).
3. **[bajo]** `executeRulesOnTaskCompleted` en el loop de bulk-complete sin try/catch propio. Fix: envuelto.

Re-verificado: `tsc --noEmit` limpio y `next build` ✓ tras los 3 fixes.

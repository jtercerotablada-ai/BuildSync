# Revisión profunda del Home (dashboard de widgets) — 3 Jul 2026

Revisión multi-agente (93 agentes, verificación adversarial) + pruebas en vivo sobre el dev server.
Resultado: 69 hallazgos confirmados (1 crítico, 6 altos, 42 medios, 19 bajos, 1 pulido), 6 sin verificar (límite de sesión), 96 detalles de pulido.

---

# HALLAZGOS CONFIRMADOS (verificados adversarialmente)

### [critical] Saved note is never displayed after reload — and typing then overwrites it on the server
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:132 (area widget:private-notepad)
SUMMARY: The load effect writes the fetched note into the editor via editorRef.current.innerHTML, but while !isLoaded the component early-returns a skeleton (line 735) so the editor div is not mounted and editorRef.current is null; the innerHTML assignment is silently skipped, setIsLoaded(true) then mounts an empty editor (the contentEditable div has no children and no dangerouslySetInnerHTML), and nothing ever syncs content state into it. The same null-ref skip happens on the localStorage fallback paths
SCENARIO: Write a note, reload the Home page: the editor is blank, and because content state IS populated, isEmpty is false so even the placeholder is hidden — user sees an unexplained empty box. Type anything: handleContentChange sets content to only the new keystrokes and 2 seconds later the debounced PUT (lines 182-191) overwrites the saved server note wi
FIX: Set the editor content after mount, e.g. render the editor always (skeleton as overlay), or add an effect that runs once isLoaded is true: `useEffect(() => { if (isLoaded && editorRef.current && !editorRef.current.innerHTML) editorRef.current.innerHTML = DOMPurify.sanitize(content); }, [isLoaded])`.
VERIFIER: Reproduced from source. In src\components\dashboard\widgets\private-notepad-widget.tsx, isLoaded starts false and the component early-returns a skeleton (lines 735-744) until the load effect finishes, so the contentEditable div (line 761) is unmounted and editorRef.current is null for the entire asy

### [high] UTC-midnight due dates parsed with local-time getters: today's tasks land in Overdue as 'Yesterday' for US users
FILE: src/components/dashboard/widgets/assigned-tasks-widget.tsx:57 (area widget:assigned-tasks)
SUMMARY: Due dates are stored at UTC midnight (composer sends fmtIso 'YYYY-MM-DD', POST does new Date(data.dueDate)), but isOverdue/isThisWeek/formatDueDate compare against local-midnight Date objects and read local d.getDate(). For any user west of UTC (the firm is US-based), every due date renders one day early and buckets shift.
SCENARIO: User in America/New_York (UTC-4) assigns a task due today, stored 2026-07-03T00:00:00Z. Widget computes local today-midnight = 04:00Z, so 00:00Z < 04:00Z → task appears under Overdue labeled 'Yesterday' the moment it's created; a task due tomorrow shows as 'Today'. Same-day parity with Asana is broken for every US user.
FIX: Parse date-only due dates with UTC getters (getUTCFullYear/getUTCMonth/getUTCDate) or a shared parseDateOnly helper before comparing to local today; apply to isOverdue, isThisWeek, and formatDueDate.
VERIFIER: Reproduced the full chain from real code. (1) quick-create-task-modal.tsx:188-206 sends date-only "YYYY-MM-DD" strings; tasks/route.ts:247 parses with new Date(), which JS spec-parses date-only strings as UTC midnight; Prisma dueDate is DateTime? (schema.prisma:661) and /api/tasks/assigned returns r

### [high] Stats and member list scoped to the caller's oldest workspace, which the codebase itself documents as the wrong (personal signup) workspace
FILE: src/app/api/users/route.ts:36 (area widget:people)
SUMMARY: The route resolves the caller's workspace with findFirst ordered by joinedAt asc. For any user who signed up (getting an auto-generated single-member personal workspace) and was later invited to the firm workspace, this picks the personal singleton. The 'all' filter then returns zero members (nobody else is in that workspace) and, worse, the 'frequent' path finds real collaborators via shared projects but scopes all three stat groupBy queries to the wrong workspaceId, so every pill reads '0 over
SCENARIO: User signs up (personal workspace auto-created), later joins the firm workspace via invite. Opening Home: the People widget's frequent list shows collaborators from shared projects but every stat pill is 0 (tasks live in firm-workspace projects, counts scoped to the personal workspace); switching the filter to 'All' shows the 'Invite your teammates
FIX: Reuse the multi-workspace heuristic from getEffectiveAccess (prefer first workspace with >1 member) instead of findFirst joinedAt asc, or scope stats to all workspaces the caller shares with each member.
VERIFIER: Reproduced from real code. src/app/api/users/route.ts:36-42 resolves the caller's workspace via findFirst ordered by joinedAt asc; lines 105/141/151/164 scope the member list and all three stat groupBy queries to that workspaceId. The personal-workspace-first scenario is concrete: src/app/api/users/

### [high] status-overview endpoint leaks WORKSPACE-visibility projects to restricted users, producing rows that 404 on click
FILE: src/app/api/projects/status-overview/route.ts:54 (area widget:status-updates)
SUMMARY: The widget's endpoint includes `{ visibility: "WORKSPACE" }` (the Prisma schema default for every project) in its OR clause, but /api/projects GET and the /projects/[projectId] page deliberately removed WORKSPACE auto-grant for L1-L3 non-leadership users. Restricted users (including invited guests the projects route comment explicitly calls out) see every workspace project's name, latest status summary, and author in the widget — and clicking any of those rows hits the project page's access chec
SCENARIO: An L2 engineer invited to one project opens the home dashboard: the widget lists all 8 workspace projects with their confidential status summaries and authors (info disclosure). Clicking any project they are not a member of navigates to /projects/{id} and lands on a 404 page — a dead link straight from the dashboard.
FIX: Reuse the same per-workspace visibility clause builder as /api/projects GET (role OWNER/ADMIN or level >= 4 sees all; otherwise ownerId/member/PUBLIC only) instead of the flat OR with WORKSPACE.
VERIFIER: Reproduced from real code. src\app\api\projects\status-overview\route.ts:50-55 includes `{ visibility: "WORKSPACE" }` in its OR with no role/level check, while its own doc comment (lines 21-23) claims scope is "owns, is a member of, or PUBLIC" — the clause contradicts the endpoint's stated contract.

### [high] Creating a form with a blank description always fails (zod schema rejects null)
FILE: src/app/api/forms/route.ts:38 (area widget:forms)
SUMMARY: The widget's '+ New form' / 'Create your first form' flow opens FormBuilderDialog in create mode, which POSTs description: null when the description is left blank (the default), but createFormSchema declares description as z.string().max(2000).optional() — optional accepts undefined, not null — so the POST returns 400 and the form is never created.
SCENARIO: User clicks '+ New form' in the Forms widget, picks a project, types a name, keeps the default Name/Email fields, leaves description empty, clicks 'Create form' → 400 with toast 'Invalid input: expected string, received null'; the form is not created. Works only if the user happens to type a description.
FIX: Change createFormSchema to `description: z.string().max(2000).nullable().optional()` (matching the PATCH schema), or have the dialog send undefined instead of null.
VERIFIER: Reproduced end-to-end from real code. (1) forms-widget.tsx:367-374 opens FormBuilderDialog with initial={null} (create mode) from '+ New form'/'Create your first form'. (2) form-builder-dialog.tsx:405 sends description: description.trim() || null — description defaults to "" (line 173) and client va

### [high] AI has zero app context — the widget's own suggestions and placeholder promise answers it cannot give
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:37 (area widget:ai-assistant)
SUMMARY: The default suggestion 'Find my recently overdue tasks' (line 37) and the placeholder 'Ask about @projects or @people' (line 400) promise data-aware answers, but handleSubmit sends only the raw question text to /api/ai/assist, and the route (src\app\api\ai\assist\route.ts lines 30-39) relays it to Claude with no task/project/user context whatsoever. @mentions are inserted as plain text '@Name' (line 123) and nothing ever resolves them.
SCENARIO: User clicks the built-in suggestion 'Find my recently overdue tasks' and hits Enter. Claude receives only that sentence with no access to BuildSync data, so it either hallucinates a task list or replies that it cannot see the user's tasks. The widget's flagship interaction (and its @project/@people mention feature) visibly does not do what the UI a
FIX: Either enrich the endpoint with real context (fetch the caller's overdue tasks / mentioned project summaries server-side and inject into the prompt, like /api/ai/coach does), or rewrite the suggestions/placeholder to only promise contextless help ('Draft a status update', 'Summarize this text').
VERIFIER: Reproduced from source. Widget (src\components\dashboard\widgets\ai-assistant-widget.tsx lines 195-201) posts only {prompt: 'Answer the following question helpfully and concisely:', text: currentQuestion} to /api/ai/assist. The route (src\app\api\ai\assist\route.ts lines 30-39) performs zero DB quer

### [high] Half-size drag overlay renders at ~25% of the grid width and 40px too short — the exact distortion the user noticed
FILE: src/components/dashboard/widget-container.tsx:53 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: dnd-kit's DragOverlay wraps children in a PositionedOverlay whose style is set to the MEASURED rect of the active node (verified in installed @dnd-kit/core 6.3.1, node_modules/@dnd-kit/core/dist/core.cjs.development.js:3658-3662: `width: rect.width, height: rect.height`). For a half-size card that wrapper is already ~half the grid width, so `w-[calc(50%-12px)]` inside it renders the floating preview at roughly a QUARTER of the page width — the card visibly shrinks to half its size the instant yo
SCENARIO: User grabs any half-size widget header: the floating preview instantly shrinks to ~25% of the page width and 320px tall while the placeholder slot stays half-width/360px — obvious size distortion during the entire drag and a mismatched snap on release. On mobile (grid-cols-1) a 'half' widget is measured full-width, so its overlay renders at ~50% wi
FIX: In WidgetOverlay use `w-full h-full` unconditionally (drop the size prop for width, drop h-[320px]); keep the rotate/scale flourish if desired.
VERIFIER: Reproduced from real code. Installed @dnd-kit/core 6.3.1 (node_modules\@dnd-kit\core\dist\core.esm.js:3661-3665) sizes the DragOverlay wrapper div to the measured active node rect (width: rect.width, height: rect.height); page.tsx:369-373 passes no overrides. The measured node is the half-width, 360

### [medium] Overdue tab badge shows the capped (sliced) count, not the real overdue total
FILE: src/components/dashboard/widgets/my-tasks-widget.tsx:208 (area widget:my-tasks)
SUMMARY: The Overdue tab count uses overdueTasks (the list already sliced to VISIBLE_LIMIT=5) instead of overdueTasksAll, so any user with more than 5 overdue tasks sees "Overdue (5)" regardless of the true count — the one number this widget exists to surface is wrong.
SCENARIO: User has 23 overdue tasks. The tab reads "Overdue (5)". Only after clicking "Show more" inside the tab does the badge jump to (23). On the flagship dashboard card this understates urgency for exactly the users who most need the signal.
FIX: Use `count: overdueTasksAll.length` on line 208.
VERIFIER: Reproduced from the real code in "C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\my-tasks-widget.tsx". Line 121 slices overdueTasksAll to VISIBLE_LIMIT=5 when showAll is false (default), and line 208 builds the Overdue tab badge from overdueTasks.length (the s

### [medium] Non-OK API responses fail silently: 401/500 on fetch shows a fake empty state; failed create shows nothing
FILE: src/components/dashboard/widgets/my-tasks-widget.tsx:76 (area widget:my-tasks)
SUMMARY: fetchTasks only handles the res.ok branch — a 401 (expired session) or 500 sets no error and renders "No upcoming tasks", and handleCreateTask has no else for a non-OK POST (e.g. zod 400), so the task silently isn't created while the composer sits there.
SCENARIO: Session expires while Home is open; widget refetches after a toggle, gets 401, and the user sees "No upcoming tasks" — their task list appears wiped. Or: POST returns 400/500; user typed a task, blurred, saw no toast and no new row, and the input keeps the text with no explanation.
FIX: In fetchTasks add `else { setError('Failed to load data'); }` (and `setError(null)` on success); in handleCreateTask add an else that toasts 'Failed to create task'.
VERIFIER: The core defect is real and reproducible from the code. In "C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\my-tasks-widget.tsx": (1) fetchTasks (lines 73-86) has `if (res.ok) { ... setAllTasks(data); }` with no else; `setError` fires only in the catch for thro

### [medium] Severe overfetch: widget pulls up to 1000 tasks with 9 heavy includes to render 5 rows of 5 fields, and refetches it all after every toggle
FILE: src/app/api/tasks/route.ts:81 (area widget:my-tasks)
SUMMARY: GET /api/tasks?myTasks=true (default take=1000) includes assignee, creator, project, section, subtasks, dependencies, dependents, customFieldValues (with field definitions), taskTags, and _count for every task — the widget consumes only id/name/completed/dueDate/project (interface lines 13-24 of the widget) — and the full payload is re-downloaded after every checkbox toggle, undo, create, and modal edit.
SCENARIO: A user with a few hundred assigned tasks (completed history included, since no completed filter is sent) triggers a multi-hundred-KB JSON response with N joined relations on every Home load and again after each checkbox click — visible latency on the flagship widget and unnecessary DB load, since the Completed tab is unbounded history.
FIX: Add a lightweight mode (e.g. ?fields=summary or a dedicated /api/my-tasks/summary) selecting id,name,completed,dueDate,project{id,name,color}, pass a sensible limit, and after a toggle update local state optimistically instead of refetching the world.
VERIFIER: Reproduced from real code. src/app/api/tasks/route.ts lines 41-44 default take=1000, lines 83-173 include 9 relations (assignee, creator, project, section, subtasks, dependencies, dependents, customFieldValues with nested field.options, taskTags, plus _count) — comments explicitly say these serve th

### [medium] Sort dropdown is dead — API ignores the sort param and there is no client-side fallback
FILE: src/components/dashboard/widgets/projects-widget.tsx:42 (area widget:projects)
SUMMARY: The widget offers Recent / Alphabetical / Status sorting and refetches on change, but GET /api/projects never reads the `sort` query param — it only reads `workspaceId` and `q` (route.ts:107-109) and always orders by `updatedAt: "desc"` (route.ts:203-205). The component does no client-side sorting either, so picking 'A-Z' or 'Status' triggers a network round-trip that returns the identical order.
SCENARIO: User clicks the sort dropdown and selects 'Alphabetical'. The button label changes to 'A-Z' and a refetch fires, but the list order stays recently-updated-first. A visible, primary control of the widget does nothing.
FIX: Either honor `sort` in the route (map recent→updatedAt desc, alphabetical→name asc, status→status) or sort client-side in the widget after fetch; the param is already being sent.
VERIFIER: Reproduced from real code. Widget (src\components\dashboard\widgets\projects-widget.tsx line 42) fetches `/api/projects?sort=${sortBy}&limit=4` and re-runs on sortBy change (line 55), but stores the response verbatim (line 45) and renders in received order — no client-side sort exists. The API route

### [medium] Non-OK API responses render the 'No projects yet' empty state instead of an error
FILE: src/components/dashboard/widgets/projects-widget.tsx:43 (area widget:projects)
SUMMARY: The fetch handler only assigns data when `res.ok`; there is no else branch, so a 401 (expired session) or 500 leaves projects=[] and loading=false, and the render falls into the empty state that tells the user they have no projects and should create their first one. setError only fires on a network-level throw.
SCENARIO: The API returns 500 (or the session expired → 401) for a user with 30 projects. The widget confidently shows 'No projects yet — Create your first project', a factually wrong state that invites the user to create a duplicate project.
FIX: Add `else { setError('Failed to load projects'); }` (or throw on !res.ok) and render a dedicated error state with a retry action, distinct from the empty state.
VERIFIER: Reproduced from source. src\components\dashboard\widgets\projects-widget.tsx lines 43-46: `if (res.ok) { const data = await res.json(); setProjects(data); }` has no else branch; setError (line 49) only fires in the catch block, which fetch() only enters on network-level failures, never on 401/500 HT

### [medium] limit=4 is ignored — widget downloads every visible project with members and all root tasks
FILE: src/app/api/projects/route.ts:163 (area widget:projects)
SUMMARY: The widget requests `limit=4` but the route never reads it and always returns ALL projects the user can see, each including the full owner object, every ProjectMember with nested user objects, and every root-level task (id/completed/taskType/dueDate). The widget only needs id, name, color, icon, status, and _count.tasks for a handful of rows, and it renders the entire unbounded array (projects.map with no slice). DashboardShell independently fetches the same heavy endpoint on every mount (dashbo
SCENARIO: A workspace with 60 projects averaging 80 tasks each: the home dashboard fetches ~4,800 task stubs plus all member/user objects twice (widget + shell) just to paint a few list rows, slowing first paint on the dashboard and hammering the DB with unnecessary joins.
FIX: Support `limit`/`fields` in the route (e.g. `take: Number(searchParams.get('limit'))` and a slim select when requested), or add a lightweight /api/projects/recent endpoint for the widget. At minimum, slice client-side to match the 'Recent projects' intent.
VERIFIER: Reproduced from source. src/app/api/projects/route.ts GET reads only workspaceId and q (lines 107-109); limit=4 and sort from the widget are silently ignored. The findMany (line 155) has no take and includes full owner, every ProjectMember with nested user objects, and ALL root-level tasks (lines 16

### [medium] /goals?new=1 does not open the create flow — the Create goal CTA is a broken promise
FILE: src/app/(dashboard)/goals/page.tsx:130 (area widget:goals)
SUMMARY: Home wires the widget with onCreateGoal={() => router.push("/goals?new=1")} (home/page.tsx:258), and the widget exposes 'Create goal' in two places (empty-state button, goals-widget.tsx:241-249, and the dashed button at the top of the list, lines 254-260). But the goals page never reads searchParams: grep for useSearchParams/searchParams/new=1 across src/app/(dashboard)/goals returns zero matches, and the create dialog is gated solely by local state `const [createOpen, setCreateOpen] = useState(
SCENARIO: User clicks 'Create goal' in the Goals widget → lands on the /goals list page with nothing open; the ?new=1 param is silently discarded. They must find and click the 'New goal' split button a second time. The CTA visibly does not do what it says.
FIX: In goals/page.tsx, read the param and open the dialog: `const searchParams = useSearchParams(); useEffect(() => { if (searchParams.get('new') === '1') setCreateOpen(true); }, [searchParams]);` (wrap the page in <Suspense> as Next 15 requires for useSearchParams), and optionally router.replace('/goals') after opening so refresh doesn't re-trigger.
VERIFIER: The logic flaw is reproducible from the real code. (1) "C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\(dashboard)\home\page.tsx" line 258 wires the widget as `<GoalsWidget onCreateGoal={() => router.push("/goals?new=1")} />`. (2) "src\components\dashboard\widgets\goals-widget.tsx" ex

### [medium] Non-OK API responses are silently swallowed — server errors render the misleading 'You haven't added any goals yet' empty state
FILE: src/components/dashboard/widgets/goals-widget.tsx:121 (area widget:goals)
SUMMARY: fetchGoals only handles res.ok: there is no else branch, so a 401 (expired session) or 500 sets loading=false with goals still [], and the widget renders the empty state telling the user they have no goals. setError only fires on thrown network exceptions, and error is never reset to null on retry, so a single transient failure leaves 'Failed to load data' rendered forever alongside later successful content. The teams fetch (lines 90-97) has the same silent non-OK swallow.
SCENARIO: Session expires while Home is open → /api/objectives returns 401 → widget confidently shows 'You haven't added any goals yet. Add a goal so your team knows what you plan to achieve.' to a user who has 12 goals. Conversely, one flaky request permanently pins 'Failed to load data' above healthy content because the error state is never cleared.
FIX: Add `else { setError('Failed to load goals'); setGoals([]); }` for non-OK responses, call setError(null) at the top of fetchGoals, and render the error as a distinct state (with a retry affordance) instead of a stray black <p> above normal content.
VERIFIER: Reproduced from source. In src\components\dashboard\widgets\goals-widget.tsx, fetchGoals (lines 120-128) only handles res.ok with no else branch: a 401/404/500 response leaves goals=[] and error=null while finally sets loading=false, so line 231 renders the 'You haven't added any goals yet' empty st

### [medium] limit=4 is ignored by /api/objectives — widget overfetches the entire goal graph on every tab switch
FILE: src/app/api/objectives/route.ts:39 (area widget:goals)
SUMMARY: The widget appends limit=4 (goals-widget.tsx:118), but the route only reads period/teamId/ownerId/parentId (route.ts:39-43) and its findMany has no `take` (lines 80-116). It returns every accessible objective, each with full keyResults array, children, owner, team, and three _count aggregates — the widget then discards everything past index 4 client-side (line 127). The widget also omits parentId=null (which the /goals page sends), so sub-objectives occupy the 4 visible slots alongside their par
SCENARIO: Company tab in a workspace with 200 objectives: every tab switch transfers the entire objective tree (KRs, children, owners) to render 4 rows, and the per-objective progress recomputation (lines 119-139) runs for all 200. Widget also double-fetches on initial mount once teams resolve.
FIX: Support `take` in the route (parse limit, cap it), have the widget pass parentId=null and status filters server-side, or add a slim `?summary=1` shape (id, name, progress, status, period, team) for widget consumers.
VERIFIER: Every element of the finding reproduces from the actual code. goals-widget.tsx:118 sends limit=4, but src/app/api/objectives/route.ts reads only period/teamId/ownerId/parentId (lines 39-43); the string "limit" never appears in the route, and the findMany at lines 80-116 has no `take` while eagerly i

### [medium] Row-click deep-link ?task= is consumed nowhere — clicking a task never opens it
FILE: src/components/dashboard/widgets/assigned-tasks-widget.tsx:234 (area widget:assigned-tasks)
SUMMARY: Clicking a task row pushes /projects/{id}?task={taskId} or /my-tasks?task={taskId}, but no code in the repo reads a 'task' query param: project-content.tsx imports useSearchParams (line 4) but never calls it, and my-tasks/page.tsx has no searchParams usage at all. A canonical task page exists at src/app/(dashboard)/tasks/[taskId]/page.tsx but is not used.
SCENARIO: User clicks 'Review structural calcs' assigned to Maria → lands on the project's generic list view with the task param silently dropped and must hunt for the task manually. Worse, for a task with no project the user lands on /my-tasks, which lists tasks assigned TO them — the delegated task isn't even in that list, so the click appears completely b
FIX: Navigate to the existing /tasks/{taskId} route, or implement ?task= handling in ProjectContent (call useSearchParams, seed selectedTaskId) and in my-tasks page.
VERIFIER: Reproduced from source. The widget (src\components\dashboard\widgets\assigned-tasks-widget.tsx:232-237) pushes /projects/{id}?task={taskId} or /my-tasks?task={taskId}, and an exhaustive grep of src for searchParams.get("task"), all useSearchParams() call sites, and all URLSearchParams/window.locatio

### [medium] Task created via the widget's own 'Assign task' CTA never appears until full page reload
FILE: src/components/tasks/quick-create-task-modal.tsx:209 (area widget:assigned-tasks)
SUMMARY: The POST succeeds and the toast fires, but the modal has no onCreated callback and dispatches no event; AssignedTasksWidget fetches only on mount (useEffect([fetchTasks]) with a stable useCallback) and the widget never unmounts when the composer closes, so the freshly assigned task does not show up in any tab.
SCENARIO: Empty 'Upcoming' tab says 'Assign tasks to your teammates and track them here.' User clicks Assign task, creates a task for a teammate, composer closes with a success toast — the widget still shows the same empty state, which reads as the save having failed.
FIX: Add an onCreated callback prop (or a window CustomEvent like 'buildsync:task-created') fired after res.ok, and have AssignedTasksWidget re-run fetchTasks on it.
VERIFIER: Reproduced from the actual code, all three links of the chain hold: 1. Modal success path does nothing but toast + close. "src\components\tasks\quick-create-task-modal.tsx" lines 209-215: `if (res.ok) { toast.success(...); handleClose(); }`. `handleClose` only resets local form state and calls `onOp

### [medium] Fetch failure renders a misleading happy empty state — no error UI, no retry
FILE: src/components/dashboard/widgets/assigned-tasks-widget.tsx:82 (area widget:assigned-tasks)
SUMMARY: When /api/tasks/assigned returns non-ok or throws, the catch only console.errors and loading flips off with allTasks still []; the widget then shows the normal empty-state copy, including 'No overdue assignments. Way to go, team!' on the Overdue tab. The sibling MyTasksWidget tracks an error state; this widget has none.
SCENARIO: Session expires or the API 500s → the manager's widget cheerfully reports zero overdue delegated tasks while five are actually overdue; there is no signal anything failed and no way to retry short of a full page reload.
FIX: Add error state on !res.ok/catch and render an error message with a Retry button, mirroring MyTasksWidget.
VERIFIER: Reproduced from the real code. In src\components\dashboard\widgets\assigned-tasks-widget.tsx, fetchTasks (lines 80-92) does `if (res.ok) { setAllTasks(data) }` — a 401/500 is silently ignored (not even thrown), the catch only console.errors, and no error state exists. The render branch (lines 214-22

### [medium] getInitials crashes on assignees with null name, taking down the Home page
FILE: src/components/dashboard/widgets/assigned-tasks-widget.tsx:39 (area widget:assigned-tasks)
SUMMARY: Prisma User.name is String? (schema.prisma:52) and /api/tasks/assigned selects it unguarded, but the widget's interface declares name: string and getInitials calls .split on it; task.assignee.name === null throws during render, and home/page.tsx has no error boundary around widgets.
SCENARIO: A task is assigned to a user whose name is null (e.g. account provisioned via OAuth or data imported without a name) → TypeError: Cannot read properties of null (reading 'split') during render → the entire /home route errors out, not just the widget.
FIX: Type assignee.name as string | null and guard: getInitials(task.assignee.name ?? '?') plus render fallback text; ideally also fall back to email in the API select.
VERIFIER: Reproduced from real code: assigned-tasks-widget.tsx:38-40 calls name.split(' ') in getInitials, invoked at line 269 with task.assignee.name; the line-264 guard only checks assignee, not name. The API (src/app/api/tasks/assigned/route.ts) returns Prisma results unmodified with assignee.name selected

### [medium] Period tabs are nearly a no-op: 'This week / This month' only changes the 'done' count; overdue and upcoming ignore the period
FILE: src/app/api/users/route.ts:158 (area widget:people)
SUMMARY: periodStart is only applied to the completed query (completedAt gte periodStart). The overdue query is dueDate < today with no period bound (reasonable), but the upcoming query is `dueDate: { gte: today }` with NO upper bound, despite the route's own comment at lines 154-157 claiming 'Upcoming = ... due within the selected period window'. So a task due in 8 months counts as 'upcoming' under 'This week', and switching tabs in the widget changes only the third pill — two of three stats are identic
SCENARIO: A collaborator has 1 task due tomorrow and 20 tasks due next quarter. The widget shows '21 upcoming' on the 'This week' tab, and toggling to 'This month' changes nothing except the 'done' pill — the tabs appear broken to the user.
FIX: Compute a periodEnd (end of week / end of month) and add `dueDate: { gte: today, lt: periodEnd }` to the upcoming query.
VERIFIER: Verified against real code. In src/app/api/users/route.ts, periodStart (lines 126-132) is applied only to the completed query (completedAt: { gte: periodStart }, line 149). The upcoming query (lines 158-167) is `completedAt: null, dueDate: { gte: today }` with no `lte` period-end bound, directly con

### [medium] Error state is never cleared after a failure, and errors render alongside a misleading 'Invite your teammates' empty state
FILE: src/components/dashboard/widgets/people-widget.tsx:79 (area widget:people)
SUMMARY: setError(null) is never called: fetchPeople sets loading true but does not reset error, so after one transient failure the 'Failed to load data' banner persists above the list forever, even after subsequent successful tab/filter refetches. Additionally, when the first fetch fails, people stays [] so the widget renders the error line AND the 'Invite your teammates to collaborate' empty state simultaneously — telling the user their team is empty when the request merely failed. There is also no ret
SCENARIO: API returns 500 once (e.g., DB hiccup). User switches to 'This month', the fetch succeeds and the list populates — but 'Failed to load data' remains pinned above it for the rest of the session. On a hard failure, a brand-new user sees 'Invite your teammates to collaborate' instead of an error with retry.
FIX: Call setError(null) at the top of fetchPeople; render a dedicated error state (red text + Retry button) instead of falling through to the empty state; use text-red-600/destructive for the message.
VERIFIER: Verified directly in src/components/dashboard/widgets/people-widget.tsx. (1) The only setError call in the file is setError('Failed to load data') in the catch (line 79); fetchPeople (lines 66-84) sets loading true but never resets error, and the success path only calls setPeople. Since filter/perio

### [medium] Status pill prefers the last update's historical status over the project's current status
FILE: src/components/dashboard/widgets/status-updates-widget.tsx:230 (area widget:status-updates)
SUMMARY: `const pillStatus = row.lastUpdate?.status || row.status` displays the status of the most recent posted update, but the API documents `row.status` as the authoritative header pill and they diverge whenever status is changed via the project header quick-dropdown (PATCH /api/projects/:id, no StatusUpdate created) or when a viewer posts an update without sync permission. The widget then shows an outdated status while its own sort order uses the current `row.status` — pill and ranking disagree.
SCENARIO: A PM flips a project to OFF_TRACK via the overview header dropdown. On the home widget the row still shows the gold 'On track' dot/label from last week's update (while sorting it as off-track), so leadership scanning the dashboard misses the escalation until someone posts a full update.
FIX: Use `row.status` for the dot/label (matching the project header pill), keeping `lastUpdate.status` only if you want to show 'was On track' historical context.
VERIFIER: Reproduced from the real code. Widget line 230 (src/components/dashboard/widgets/status-updates-widget.tsx) is exactly `const pillStatus = row.lastUpdate?.status || row.status;`, preferring the historical update's status — while the adjacent comment (lines 251-252) claims the dot shows "current proj

### [medium] Every row renders "undefined projects" — API response shape does not match component interface
FILE: src/components/dashboard/widgets/portfolios-widget.tsx:88 (area widget:portfolios)
SUMMARY: The widget reads portfolio.projectCount, but GET /api/portfolios never returns a projectCount field — it returns Prisma's _count: { projects } (route.ts:74-78) spread into the withStats objects (route.ts:147-165). projectCount is undefined for every portfolio.
SCENARIO: Any user with at least one portfolio opens Home with the Portfolios widget enabled: every row displays the literal text "undefined projects" next to the portfolio name.
FIX: Render {portfolio._count?.projects ?? portfolio.projects?.length ?? 0} (and update the interface), or have the widget map data to projectCount after fetch. Also handle singular: "1 project".
VERIFIER: The core shape mismatch is real. The widget ("C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\portfolios-widget.tsx" line 26) sets state directly from `await res.json()` of GET /api/portfolios, and line 88 renders `{portfolio.projectCount} projects`. The API ("

### [medium] Empty-state "Create portfolio" CTA navigates to /portfolios/new, which does not exist — lands on "Portfolio not found"
FILE: src/components/dashboard/widgets/portfolios-widget.tsx:64 (area widget:portfolios)
SUMMARY: There is no /portfolios/new route; the URL is captured by the [portfolioId] dynamic segment, whose page fetches /api/portfolios/new, gets a 404 (api/portfolios/[portfolioId]/route.ts:93-95), and renders "Portfolio not found / Go back to portfolios". Actual creation lives in a Dialog on /portfolios (portfolios/page.tsx:177 createOpen state, Dialog at line 334) with no ?create= deep link.
SCENARIO: A new user with zero portfolios clicks the only CTA in the widget's empty state and lands on an error page saying "Portfolio not found" instead of a create flow — the widget is a complete dead end for first-time users.
FIX: Navigate to /portfolios and open the create dialog (e.g. support /portfolios?create=1 on the list page), or open the create dialog directly from the widget.
VERIFIER: Reproduced from real code: portfolios-widget.tsx:64 pushes '/portfolios/new'; no such route exists (only (dashboard)/portfolios/page.tsx and [portfolioId]/page.tsx), so Next.js captures it as portfolioId="new". The detail page fetches /api/portfolios/new, whose GET does findUnique({id:"new"}) -> nul

### [medium] ?limit=5 is ignored by the API and the widget never slices — rows silently clipped by overflow-hidden in the 360px card
FILE: src/components/dashboard/widgets/portfolios-widget.tsx:23 (area widget:portfolios)
SUMMARY: The widget requests /api/portfolios?limit=5 but the route handler is `export async function GET()` with no request parameter (route.ts:14) so the limit is never applied; the widget then renders ALL portfolios (no .slice(0,5)), and the widget body is `overflow-hidden` (widget-container.tsx:239), so anything past ~5 rows is invisibly cut off with no scroll and no "+N more" affordance — at both half and full width, since the card height is fixed at 360px (home/page.tsx:351 auto-rows-[360px]).
SCENARIO: A user with 8 portfolios sees roughly 4.5 rows; the remaining portfolios are cut off mid-row with no scrollbar or indicator, and there is no way inside the widget to know more exist beyond clicking "View all".
FIX: Support limit in the route (parse req.nextUrl.searchParams) or slice client-side to 4-5 rows, and add a "+N more" row or make the list area overflow-y-auto.
VERIFIER: Reproduced from real code. portfolios-widget.tsx:23 fetches '/api/portfolios?limit=5' but src/app/api/portfolios/route.ts:14 declares `export async function GET()` with no request argument — the handler never reads searchParams and the prisma findMany (lines 37-81) has no `take`, so all visible port

### [medium] No error state — API failures render the misleading "No portfolios yet" empty state with a Create CTA
FILE: src/components/dashboard/widgets/portfolios-widget.tsx:24 (area widget:portfolios)
SUMMARY: When fetch resolves non-ok (401 expired session, 404 "No workspace found", 500) or throws, the widget leaves portfolios=[] and loading=false, so the empty state branch at line 57 renders "No portfolios yet" — telling a user who HAS portfolios that they have none, and pointing them at the (also broken) create CTA.
SCENARIO: The portfolios API returns 500 (or the session expires mid-visit); a user with 12 portfolios sees "No portfolios yet — Create portfolios to organize related projects" instead of an error/retry affordance.
FIX: Track an error state; on !res.ok or catch, render a small error message with a Retry button instead of the empty state.
VERIFIER: Reproduced from real code. In src\components\dashboard\widgets\portfolios-widget.tsx lines 20-35, the fetch effect only calls setPortfolios inside `if (res.ok)`; there is no else branch, no error state, and the catch only console.errors — so on any non-ok response or thrown fetch, portfolios stays [

### [medium] First-save race can create duplicate notepad notes on the server
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:195 (area widget:private-notepad)
SUMMARY: saveNote POSTs when noteIdRef.current is null and only sets the ref after the response resolves; blur (line 772) and the 2s debounce timer (line 217) can both invoke saveNote while the first POST is still in flight (the blur save does not clear the pending timer), producing two WorkspaceNote rows titled '__home_private_notepad__'. The GET-failure fallback path also POSTs a duplicate even when a server note already exists (GET 500 -> localStorage fallback -> noteIdRef stays null -> POST at line 1
SCENARIO: First-ever note: user types, clicks outside at t=1.7s (blur POST starts), debounce timer fires at t=2.0s while the POST is still resolving — second POST creates a second note. From then on the widget binds to an arbitrary one of the two and edits diverge.
FIX: Track an in-flight promise (savingRef) and await/reuse it, or make the server upsert by (authorId, title) for the notepad note.
VERIFIER: Reproduced from real code. In src\components\dashboard\widgets\private-notepad-widget.tsx: saveNote (lines 168-212) POSTs when noteIdRef.current is null and sets the ref only after the response resolves (line 206); there is no in-flight guard. onBlur={saveNote} (line 772) and the 2s debounce timer (

### [medium] Save failures are completely silent — no saving/saved indicator and PUT response never checked
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:184 (area widget:private-notepad)
SUMMARY: There is no 'Saving…'/'Saved' indicator anywhere in the widget, the PUT result is never inspected (res.ok unchecked), and network errors are swallowed by an empty catch. If the session expires (401), the note was deleted elsewhere (404), or the server 500s, every subsequent save fails invisibly while the user keeps writing; only localStorage retains the text, and the user gets zero feedback.
SCENARIO: User leaves the Home tab open overnight, session cookie expires, they jot down site-visit notes in the morning: every PUT returns 401, nothing warns them, and on the next device/browser the note is stale.
FIX: Check res.ok, set a saveStatus state ('saving'|'saved'|'error'), and render a small indicator (Asana shows 'Saved' ticks); on 401/403/404 surface a visible warning.
VERIFIER: Reproduced from real code. In src/components/dashboard/widgets/private-notepad-widget.tsx, the PUT at lines 184-191 discards the response entirely (no res.ok check — fetch does not throw on HTTP 401/403/404/500), and the catch at lines 209-211 is empty, swallowing network errors. No save-status stat

### [medium] Multi-tab last-write-wins plus an unconditional PUT fired 2s after every page load
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:215 (area widget:private-notepad)
SUMMARY: The auto-save effect depends on [content, saveNote, isLoaded], so when the load completes (content set + isLoaded true) it schedules a saveNote even though the user typed nothing — every visit to Home (or the portal dashboard) issues a redundant PUT of the just-loaded snapshot. Combined with full-content PUTs and no version/updatedAt guard in the route (route lines 182-192), merely opening the widget in a second tab can overwrite newer edits made in the first tab in the intervening seconds, and 
SCENARIO: Tab A open on Home; user opens Home in tab B (loads snapshot S), types in tab A (saves S+edit at t+1s), tab B's load-triggered timer fires at t+2s and PUTs stale S — tab A's edit is gone from the server.
FIX: Only schedule saves when content differs from the last-loaded/last-saved value (dirty flag), and consider an updatedAt precondition on PUT.
VERIFIER: Reproduced from the actual code. In private-notepad-widget.tsx the load effect (lines 121-165) sets content, noteIdRef, and isLoaded; the auto-save effect (lines 215-219) depends on [content, saveNote, isLoaded] with no dirty flag, so completion of the load itself schedules saveNote after 2s. saveNo

### [medium] Toolbar overflows and clips at half width — trailing buttons (code block, AI assist) become unreachable
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:781 (area widget:private-notepad)
SUMMARY: The toolbar renders 14 icon buttons (28px each) plus 6 separators and gaps — roughly 480px — in a single non-wrapping flex row (no flex-wrap, no overflow-x-auto, no shrink). At half size the card's inner width (card width minus WidgetContainer's px-4) drops below that on common laptop layouts (e.g. ~1280px viewport with the sidebar), and WidgetContainer's content wrapper is overflow-hidden, so the rightmost buttons (Link, inline code, code block, AI Sparkles) are clipped and cannot be clicked.
SCENARIO: On a 13-inch laptop with the widget at the default half size, the user can never open AI assist or insert a code block from the toolbar — the buttons are cut off with no scroll affordance.
FIX: Add flex-wrap or overflow-x-auto to the toolbar row, or collapse the tail buttons into an overflow '⋯' menu below a width threshold.
VERIFIER: Reproduced from real code. Toolbar at private-notepad-widget.tsx:781 is a single non-wrapping flex row (`flex items-center gap-0.5`, no flex-wrap/overflow-x-auto) containing 14 fixed 28px icon buttons (p-1.5 + h-4 w-4, min-content so they cannot shrink) and 5 `w-px mx-1` separators + 18 gaps ≈ 473px

### [medium] AI 'Replace text' does not replace when invoked without a selection — it duplicates the content
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:547 (area widget:private-notepad)
SUMMARY: openAIAssist falls back to the entire document text when nothing is selected but only calls saveSelection() in the has-selection branch; applyAIResult then restoreSelection()s a stale collapsed range (from the last focus/mouseup) and insertText()s the AI result at that caret — the original text is not removed, so 'Replace text' appends a full rewritten copy next to the original.
SCENARIO: User clicks the Sparkles button with no selection, runs 'Improve writing', clicks 'Replace text' — the note now contains the old text plus the improved text jammed in at the old cursor position.
FIX: In the no-selection path, select the editor's full contents (range.selectNodeContents(editorRef.current)) into savedSelectionRef, or clear innerHTML before inserting.
VERIFIER: Reproduced from source. In private-notepad-widget.tsx, openAIAssist (lines 547-560) falls back to the entire document (editorRef.current.textContent) when nothing is selected but only calls saveSelection() in the has-selection branch, so savedSelectionRef keeps a stale collapsed caret from the last 

### [medium] Widget title links to /portal/admin/forms — inaccessible to default dashboard users (wrong-audience link)
FILE: src/app/(dashboard)/home/page.tsx:190 (area widget:forms)
SUMMARY: The Forms widget header is a Link to /portal/admin/forms, but that route lives in the (portal) route group whose layout only admits WORKER/ADMIN/OWNER workspace roles and redirects everyone else to the public marketing page; the page itself further restricts to OWNER/ADMIN. A dashboard user with the default MEMBER role is dumped onto the marketing site.
SCENARIO: A workspace MEMBER (the schema default for dashboard users) clicks the 'Forms' title on the Home dashboard → server redirect to '/' → they land on the public TTC marketing homepage, losing the app entirely. A WORKER gets bounced to /portal/dashboard (the TTC portal shell, different chrome). Only OWNER/ADMIN ever see the target page — and even they 
FIX: Point the title at a dashboard-side surface (or remove titleHref for the forms widget until one exists), or role-gate the href so only OWNER/ADMIN get the portal-admin link.
VERIFIER: Every step of the cited chain exists in the real code. (1) home/page.tsx:190 returns "/portal/admin/forms" as the Forms widget titleHref; (2) widget-container.tsx:221-229 renders titleHref as an ungated <Link>; (3) the (dashboard) layout only checks for a session — no role gate — so MEMBER users (th

### [medium] Clicking a mention never opens the task/comment — ?task= deep-link is consumed nowhere
FILE: src/components/dashboard/widgets/mentions-widget.tsx:89 (area widget:mentions)
SUMMARY: The row click pushes /projects/{projectId}?task={taskId} (or /my-tasks?task={taskId}), but no code in the app reads a 'task' search param, so the user lands on the project list view (or My Tasks) with nothing opened, highlighted, or scrolled to — the mentioned comment is never shown.
SCENARIO: User is @-mentioned in a comment on task X in project P. They click the row in the mentions widget. The app navigates to /projects/P?task=X, which renders the default list view of the whole project — no TaskDetailPanel opens, no comment is visible. For a project-less task the user lands on /my-tasks with nothing selected. The widget's core purpose 
FIX: In ProjectContent (which already has useSearchParams imported and a selectedTask/TaskDetailPanel), read searchParams.get('task') on mount and open the TaskDetailPanel for that id; do the equivalent in my-tasks/page.tsx. This also fixes the same dead param pushed by assigned-tasks-widget, inbox, team calendar, and the email links in src/lib/email.ts:272-273.
VERIFIER: Reproduced from real code. mentions-widget.tsx:87-93 pushes /projects/{id}?task={taskId} (or /my-tasks?task={taskId}), but a repo-wide search finds zero consumers of a 'task' search param: projects/[projectId]/page.tsx types searchParams as Promise<{ view?: string }> and destructures only `view` (li

### [medium] Non-OK API responses fail silently into the empty state
FILE: src/components/dashboard/widgets/mentions-widget.tsx:32 (area widget:mentions)
SUMMARY: Only `res.ok` is handled; a 401 (expired session) or 500 from /api/mentions sets no error, so the widget shows the misleading 'When you are @-mentioned in a comment, it will appear here.' empty state instead of an error. The catch block only fires on network exceptions, and even then the error renders in plain black text above the still-shown empty state.
SCENARIO: The mentions API throws (e.g. getUserWorkspaceId AuthorizationError → 500, or session expired → 401). The user sees the friendly 'no mentions yet' empty state even though they have mentions, with no error and no way to retry. On a network exception they see black 'Failed to load data' text stacked on top of the same 'no mentions' copy — two contrad
FIX: Add `else { setError('Failed to load mentions'); }` for !res.ok, render the error state INSTEAD of the empty state (early-return branch), style it as an error (text-red-600) and add a Retry button.
VERIFIER: Reproduced from real code. mentions-widget.tsx lines 31-35 handle only res.ok with no else branch: a 401 or 500 from /api/mentions leaves mentions=[] and error=null, and the finally block ends loading, so the render falls into the mentions.length===0 empty state ("When you are @-mentioned in a comme

### [medium] 'Create your first project' tutorial navigates to the public marketing site, and ?create=true is read by nothing
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:35 (area widget:learning)
SUMMARY: The tutorial URL '/projects?create=true' resolves to src\app\(public)\projects\page.tsx — the TTC marketing portfolio page (hero, category filters, stock project photos) whitelisted as public in src\proxy.ts (publicExactRoutes includes "/projects", line 22). There is no (dashboard)/projects/page.tsx; the dashboard pages are /projects/all, /projects/new, and /projects/[projectId]. A repo-wide grep for 'create=true' matches only this widget line — no page or component consumes the param.
SCENARIO: User clicks 'Create your first project' in the Learn TT widget → router.push('/projects?create=true') → lands on the public marketing portfolio page with the marketing layout, outside the app shell, with no project-creation UI. The onboarding tutorial actively ejects a new user from the product.
FIX: Point the tutorial at the real creation flow: url: '/projects/new' (page exists), or '/projects/all' if the intent is the project list. Delete the dead create=true param or implement a handler for it.
VERIFIER: Reproduced from source: learning-widget.tsx:35 pushes '/projects?create=true'; the only page at URL /projects is src\app\(public)\projects\page.tsx (the TTC marketing portfolio with stock images and category filters) since no (dashboard)/projects/page.tsx exists (only /projects/all, /projects/new, /

### [medium] 'Show tutorials again' does not persist — the DB dismissed flag is never cleared, so the widget re-dismisses on every reload
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:158 (area widget:learning)
SUMMARY: dismissWidget() (lines 132-144) writes uiState.learningDismissed:true to the DB via PATCH. But the un-dismiss button only calls setDismissed(false) and localStorage.removeItem — it never PATCHes learningDismissed:false. On next mount the load effect (line 71: if (ui?.learningDismissed === true) setDismissed(true)) reads the stale DB flag and re-dismisses the widget. The user's un-dismiss survives only the current page view.
SCENARIO: User dismisses the widget (DB now has learningDismissed:true), later clicks 'Show tutorials again', uses the tutorials, then reloads or opens Home on another device → widget is dismissed again. Repeats forever; there is no way to permanently un-dismiss.
FIX: In the un-dismiss onClick, also send fetch('/api/users/preferences', { method:'PATCH', ..., body: JSON.stringify({ uiState: { learningDismissed: false } }) }).
VERIFIER: Reproduced from real code. In src\components\dashboard\widgets\learning-widget.tsx, dismissWidget (lines 132-144) persists uiState.learningDismissed:true to the DB via PATCH /api/users/preferences, but the "Show tutorials again" button (lines 158-161) only calls setDismissed(false) and localStorage.

### [medium] Explicit "Half size" menu choice is silently reverted by auto-recalc after any drag or widget toggle
FILE: src/hooks/use-widget-preferences.ts:93 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: calculateAutoSizes treats only 'full' as a manual choice (line 71-81); every other widget is forced to 'half' (line 84), and any lone widget in a row is force-promoted to 'full' (lines 74-76 and 93-95). recalculateWidgetSizes runs 300ms after EVERY drag (page.tsx:165) and toggleWidget also runs calculateAutoSizes (line 243). So a user who opens the "..." menu and picks "Half size" on a widget that ends up alone in its row (e.g. the last widget, or one followed by a full card) sees it flip back t
SCENARIO: With 5 visible widgets all half: last one is auto-full. User sets it to "Half size" via the menu (setWidgetSize, sticks visually). User then drags any card one slot, or hides/shows any widget in Customize — recalculateWidgetSizes/calculateAutoSizes forces that widget back to 'full', overwriting and persisting over the user's explicit choice with no
FIX: Track explicit user sizes separately (e.g. widgetSizes = user intent, computed layout derived at render) or have calculateAutoSizes respect an explicit 'half' the same way it respects 'full', only auto-sizing widgets with no recorded choice.
VERIFIER: Reproduced from source. widget-container.tsx (lines 152-162) offers an explicit "Half size" menu item wired to setWidgetSize, which stores widgetSizes[id]='half' (use-widget-preferences.ts:277-285). But calculateAutoSizes (use-widget-preferences.ts:59-98) treats only manualSize === 'full' as a user 

### [medium] setWidgetSize never recalculates layout — grid gets visible holes until an unrelated drag 'fixes' it
FILE: src/hooks/use-widget-preferences.ts:277 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: setWidgetSize (lines 277-285) only writes the one size and returns; unlike toggleWidget and handleDragEnd it never runs calculateAutoSizes. The grid container has no `grid-auto-flow: dense` (page.tsx:351), so when the user sets a widget to 'Full size' while the previous widget sits alone in the left column, the span-2 card cannot fit in the remaining column and wraps to the next row, leaving an empty 360px cell. The hole persists until the next drag or toggle triggers recalc — which then also mu
SCENARIO: Layout [A(half)][B(half)] / [C(half)][D(half)]: user sets B to Full size. A stays alone in row 1 col 1, B (span 2) wraps to row 2, leaving a blank 360px cell next to A. The hole remains until the user happens to drag or toggle something, at which point recalc promotes A to full — a size change the user never requested.
FIX: Either run the (fixed, intent-respecting) recalc inside setWidgetSize, or add grid-auto-flow dense — but note dense reorders visually, so recalc is the better fit here.
VERIFIER: All cited evidence matches the actual code. setWidgetSize (use-widget-preferences.ts:277-285) only spreads the single size into widgetSizes and never calls calculateAutoSizes, unlike toggleWidget (line 243), recalculateWidgetSizes (263), and resetToDefaults (273). The user-facing path exists: the "F

### [medium] Layout save silently lost if user navigates away within ~800ms of dropping a card
FILE: src/hooks/use-widget-preferences.ts:205 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: After a drop, reorderWidgets fires at t=0 (arms the 500ms debounced PATCH), then recalculateWidgetSizes fires at t=300ms, which changes `preferences` again and — via the effect cleanup at lines 215-217 — cancels the first timer and re-arms it. The PATCH therefore lands ~800ms after drop. If the user clicks any sidebar link before that, the component unmounts, the cleanup clears the pending timeout, and the PATCH never fires. localStorage does hold the new layout (written synchronously at line 19
SCENARIO: User drags 'Projects' above 'My tasks', immediately clicks 'My tasks' in the sidebar (~within 0.8s). PATCH is cancelled by unmount cleanup. User returns to /home: GET returns the old server layout, the drag is undone. No error shown anywhere.
FIX: Flush the pending save on unmount (fire the fetch in the cleanup with keepalive:true / navigator.sendBeacon instead of only clearing the timer), and reconcile server data with a newer localStorage copy (timestamp the payload).
VERIFIER: Reproduced from real code. (1) src\hooks\use-widget-preferences.ts:191-218 — persist effect arms a 500ms debounced PATCH and its cleanup clears the pending timer on every `preferences` change and on unmount. (2) src\app\(dashboard)\home\page.tsx:165 — `setTimeout(() => recalculateWidgetSizes(), 300)

### [medium] rectSortingStrategy assumes uniform item sizes — mixed half/full spans produce wrong drop previews and a settle jump
FILE: src/app/(dashboard)/home/page.tsx:349 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: rectSortingStrategy predicts positions by array-moving the initially measured rects, assuming items are interchangeable in size (documented dnd-kit limitation: 'the rect sorting strategy is not compatible with virtualized lists and assumes all items are the same size' for accurate previews). Here items span 1 or 2 columns (widget-container.tsx:196), so when a half card is dragged past a full card (or vice versa) the strategy's predicted rects don't match how CSS grid will actually reflow span-2 
SCENARIO: Grid: [A half][B half] / [C full] / [D half][E half]. Drag D up over A: while dragging, B/C/E preview-shift into slots computed from swapped rects that ignore C's 2-column span; on release the real CSS grid reflow (plus the 300ms recalc possibly flipping a lone half to full) lands cards in different spots than the preview, producing a visible jump/
FIX: Constrain the sortable preview to same-size accuracy: either keep all drag previews position-only and accept the settle animation, or model rows explicitly (Asana-style slot grid) / use a custom strategy aware of col-spans; at minimum remove the post-drop recalc size flips so the drop target matches the preview.
VERIFIER: Every element of the finding reproduces from the real code. page.tsx:349 applies rectSortingStrategy to a 2-column CSS grid whose items span 1 or 2 columns (widget-container.tsx:196), and mixed spans are the normal state: the widget menu offers Half/Full size, and calculateAutoSizes (use-widget-pref

### [medium] Unmount cleanup cancels the pending layout PATCH — drag then navigate loses the layout server-side
FILE: src/hooks/use-widget-preferences.ts:215 (area Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model))
SUMMARY: The persist effect's cleanup clearTimeout runs on unmount and cancels the debounced PATCH with no flush (no keepalive/sendBeacon). Reordering/resizing a widget and navigating away within 500ms (e.g. clicking a widget title link — the header links are right next to the drag handle) silently discards the server save. localStorage keeps the new layout, but on the next load the server branch wins whenever widgetPreferences is non-null (line 127), so localStorage is never consulted — the layout visib
SCENARIO: User drags 'My tasks' to a new position, then within 500ms clicks the 'Goals' widget title (router navigation unmounts home). PATCH is canceled. Returning to /home: GET returns the old server layout and the widget snaps back to its old slot; on the user's other device the reorder never existed.
FIX: On unmount, flush instead of cancel: keep the latest preferences in a ref and in the cleanup send fetch('/api/users/preferences', { method:'PATCH', keepalive: true, ... }) if a save is pending. Alternatively move the debounce to module level like use-ui-state.ts does.
VERIFIER: Reproduced from real code. (1) src\hooks\use-widget-preferences.ts lines 204-217: the debounced 500ms PATCH is scheduled in the persist effect and the effect cleanup unconditionally clearTimeouts it; on unmount (client navigation) the save is cancelled. Grep of src for sendBeacon/keepalive/beforeunl

### [medium] isInitialLoadRef guard is defeated — persist effect PATCHes on every load, and can overwrite the saved server layout with defaults/stale localStorage when GET fails
FILE: src/hooks/use-widget-preferences.ts:183 (area Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model))
SUMMARY: The finally block sets isInitialLoadRef.current = false synchronously BEFORE the post-load re-render, so the 100ms-delayed guard effect (lines 221-226) is dead code — by the time the persist effect first runs with isLoaded=true, the guard is already false and it schedules a PATCH. Consequences: (a) every home mount echoes the just-loaded layout back to the server (wasted write + widened multi-tab clobber window); (b) if the initial GET throws (transient network blip), preferences are still the D
SCENARIO: User's saved layout is on the server. They open /home on flaky wifi: the GET rejects (catch at line 178 leaves defaults), finally sets isLoaded=true, and 500ms later the network recovers — PATCH { widgetPreferences: <defaults> } succeeds. The customized layout is permanently replaced by defaults on all devices.
FIX: Remove the isInitialLoadRef assignment from finally (let only the delayed effect clear it), or better: track a loadSucceeded flag and skip persisting until the user actually mutates preferences (dirty flag set by toggleWidget/reorderWidgets/etc.). Never PATCH when the initial GET did not succeed.
VERIFIER: Reproduced from real code. In src/hooks/use-widget-preferences.ts the finally block (lines 180-185) sets isInitialLoadRef.current = false synchronously in the same batch as setIsLoaded(true), so when the persist effect (lines 191-218) first runs after the load re-render, its guard at line 192 alread

### [medium] migratePreferences does not repair widgetOrder missing visible IDs — visible widgets can render nowhere
FILE: src/hooks/use-widget-preferences.ts:36 (area Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model))
SUMMARY: migratePreferences filters visibleWidgets and widgetOrder independently and only falls back to defaults when visible.length === 0 (line 52). A payload with non-empty visibleWidgets but an empty widgetOrder (or one missing some visible IDs) passes through untouched, and the page renders widgetOrder.filter(w => visibleWidgets.includes(w)) (page.tsx:352-353) — widgets marked visible but absent from order render NOWHERE. The API accepts widgetPreferences with zero shape validation (route.ts:61-63), 
SCENARIO: A PATCH from any buggy/old client (or manual DB edit — prod DB already has schema drift per project memory) stores { visibleWidgets: ['my-tasks','goals'], widgetOrder: [] }. Home loads with isLoaded=true, no error, and an empty grid; the write-back at lines 133-142 re-persists the same broken payload, so the blank home survives every reload.
FIX: In migratePreferences, append any visible ID missing from order: const repairedOrder = [...order, ...visible.filter(id => !order.includes(id))]. Optionally validate widgetPreferences shape server-side with zod.
VERIFIER: Reproduced from real code. migratePreferences (src\hooks\use-widget-preferences.ts:33-53) filters visibleWidgets and widgetOrder independently and only falls back to defaults when visible.length === 0; it never repairs an order that is missing visible IDs. The renderer (src\app\(dashboard)\home\page

### [medium] useUiState drops edits made during the initial fetch window — comment claims a merge that doesn't exist
FILE: src/hooks/use-ui-state.ts:75 (area Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model))
SUMMARY: fetchUiState replaces the whole module cache with the server payload (cachedUiState = state, line 75), discarding any key the user changed while the fetch was in flight (setCachedKey wrote into the old cache object reference... which is then thrown away). The subscriber sync then calls setValueState with the stale server value, visibly reverting the user's click — while the still-pending schedulePatch sends the NEW value to the server 600ms later. Result: UI shows the old value, the server store
SCENARIO: User opens /home and within ~300ms (before GET /api/users/preferences resolves) switches the header period from 'week' to 'month'. When the fetch lands, the toggle visibly snaps back to 'week'; 600ms later the PATCH stores 'month'. UI and server now disagree until the next reload, where the toggle shows 'month' — confusing double-flip on a control 
FIX: When the server response lands, re-apply locally dirty keys: track keys touched by setCachedKey since mount (or the keys in pendingPatches) and overlay them onto the fetched state before assigning cachedUiState and notifying subscribers.
VERIFIER: Reproduced from the real code. In src\hooks\use-ui-state.ts, fetchUiState (lines 69-91) does `cachedUiState = state` (line 75) — wholesale replacement of the module cache with the server payload, plus writeCachedToStorage(state) overwriting the localStorage mirror. Any edit made during the fetch win

### [medium] Header-chips fetch failure blanks the entire Home page
FILE: src/app/(dashboard)/home/page.tsx:300 (area Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts))
SUMMARY: The /api/dashboard/ceo fetch only feeds the two summary chips ('X tasks completed', 'Y collaborators'), but if it fails the whole page renders the 'Couldn't load home' error screen — hiding all 13 widgets, which self-fetch and would work fine.
SCENARIO: The portfolio-wide /api/dashboard/ceo endpoint 500s or times out (it is the heaviest dashboard query) → user's entire Home, including customize/background and every working widget, is replaced by an error screen with a full-page Retry.
FIX: On error, render the page normally and degrade the chips (hide them or show '—') instead of returning the full-page error branch.
VERIFIER: Reproduced from the real code in src\app\(dashboard)\home\page.tsx. The only setError in the file is in the /api/dashboard/ceo fetch (lines 132-149), which the file's own comment (93-95) says exists solely for the two HomeHeader summary chips. Yet `if (error)` at lines 300-315 early-returns a full-p

### [medium] Period selector is purely cosmetic — changes nothing but its own label
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\(dashboard)\home\page.tsx:173 (area Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts)
SUMMARY: The header's period dropdown (today/week/next14/lookahead3w/quarter, persisted to the DB as home.period) is not consumed by any computation. The 'tasks completed' chip is hardcoded to a 7-day window and 'collaborators' ignores period entirely; no widget reads the period either. The only thing that changes when the user picks a period is the button's own label.
SCENARIO: User selects 'This quarter' expecting the completed-tasks count to cover the quarter; the chip still shows the last-7-days number (itself capped at 12 — see next finding). The choice is even persisted to the DB and restored across devices, reinforcing the illusion that it does something.
FIX: Either wire it: pass `period` into the summary memo, map each HomePeriod to a date range, and have the API return per-period counts (or return raw completedAt timestamps to filter client-side); or remove the dropdown until it drives real data. Do not ship a persisted no-op control.
VERIFIER: Reproduced from the actual code. (1) In "src\app\(dashboard)\home\page.tsx" the period state (lines 101-104, key "home.period", DB-persisted via useUiState per "src\hooks\use-ui-state.ts" — GET/PATCH /api/users/preferences, follows the user across devices) is passed only to HomeHeader (lines 322-325

### [medium] 'Tasks completed' chip is mathematically capped at 12 and undercounts
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\dashboard\ceo\route.ts:212 (area Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts)
SUMMARY: The chip counts completed-in-last-7-days items within data.activity, but activity is only the 12 most-recently-UPDATED tasks (completed or not). The chip can never exceed 12, and completed tasks displaced from the top-12 by newer edits to other tasks are silently dropped.
SCENARIO: A team completes 30 tasks this week and also edits 10 open tasks today. The 12 most-recently-updated rows are mostly the open edits, so the header proclaims '3 tasks completed' — off by 10x. Any moderately active workspace shows a wrong headline stat every day.
FIX: Add a dedicated count to the API: `prisma.task.count({ where: { ...taskWhere, completedAt: { gte: periodStart } } })` (ideally parameterized by the selected period) and return it as e.g. `summary.tasksCompleted`, instead of deriving the stat from a truncated activity sample.
VERIFIER: The full chain is verified in the real code. src\app\api\dashboard\ceo\route.ts lines 198-212: the activity query is prisma.task.findMany({ where: taskWhere, ..., orderBy: { updatedAt: "desc" }, take: 12 }) with no completedAt filter, so it returns the 12 most-recently-updated tasks of any status. s

### [medium] Full-screen error nukes every self-fetching widget when the chips fetch fails
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\(dashboard)\home\page.tsx:300 (area Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts)
SUMMARY: The /api/dashboard/ceo fetch exists solely to feed two small header chips (the page's own comment says so), yet its failure replaces the ENTIRE home page — header, widget grid, notepad, everything — with a full-screen 'Couldn't load home' + Retry. All widgets self-fetch and would render fine.
SCENARIO: /api/dashboard/ceo returns a transient 500 (or the viewer's getEffectiveAccess query hiccups). The user loses the whole dashboard — task list, projects, notepad — even though every one of those widgets has its own healthy endpoint. Retry is a full window.location.reload().
FIX: On chips-fetch failure, render the page normally and degrade only the header: hide the two chips (or show an em-dash) instead of early-returning. Reserve full-screen errors for failures that actually block the whole page (e.g. preferences load).
VERIFIER: Reproduced from real code in src\app\(dashboard)\home\page.tsx: error state (line 97) is set only by the /api/dashboard/ceo fetch (lines 132-149) whose sole purpose is the two HomeHeader summary chips (per the file's own comments at lines 18-23 and 93-95). The `if (error)` block at lines 300-315 ear

### [low] Composer "Open task details" button never opens the task details — /my-tasks has no auto-open
FILE: src/components/dashboard/widgets/my-tasks-widget.tsx:306 (area widget:my-tasks)
SUMMARY: The ChevronRight button (title="Open task details") creates the task then does router.push('/my-tasks') with no task identifier; the code comment claims "the saved task opens automatically" but src/app/(dashboard)/my-tasks/page.tsx has no useSearchParams/auto-open logic (grep confirms zero searchParams usage; openTaskDetail is only called from list row clicks), so the user lands on the full page with nothing opened.
SCENARIO: User drafts a task in the composer and clicks the details chevron expecting Asana's behavior (task pane opens pre-filled). Instead they are dumped on /my-tasks and must hunt for the just-created task in the list to open it.
FIX: Have handleCreateTask return the created task (POST already responds with it), then push `/my-tasks?task=<id>` and add searchParams handling in the my-tasks page to call openTaskDetail — or open the local TaskDetailModal with the returned id instead of navigating.
VERIFIER: Reproduced from real code. In src\components\dashboard\widgets\my-tasks-widget.tsx (lines 302-315), the "Open task details" chevron runs `await handleCreateTask(); router.push('/my-tasks')` with no task identifier, while the comment (lines 298-301) claims the saved task "opens automatically" on /my-

### [low] Team tab with no selected team fetches ALL accessible goals and presents them as team goals
FILE: src/components/dashboard/widgets/goals-widget.tsx:113 (area widget:goals)
SUMMARY: The teamId filter is only appended when `activeTab === 'team' && selectedTeam`. If the user belongs to no teams (teams=[] so selectedTeam stays null), or clicks the Team tab before /api/teams/list resolves, fetchGoals runs with no filter — identical to the Company tab query — and every goal the user can access is displayed under 'Team'. The selector is also hidden when teams.length===0 (line 189), so there is no cue that no team is selected.
SCENARIO: A user in no teams opens the Team tab: instead of the 'You haven't added team goals yet' empty state, they see up to 4 personal/company goals mislabeled as team goals. Users in teams see the same flash of wrong data between clicking 'Team' and the teams fetch resolving (then a second fetch corrects it).
FIX: When activeTab==='team' and selectedTeam is null, skip the fetch and render the team empty state (or a 'no teams' message); gate the initial fetch on the teams request having settled.
VERIFIER: Reproduced from real code. In "src/components/dashboard/widgets/goals-widget.tsx" lines 111-116, the teamId param is only appended when `activeTab === 'team' && selectedTeam`; otherwise the fetch is unfiltered, identical to the Company tab. The API ("src/app/api/objectives/route.ts" lines 62-78) app

### [low] Invite CTA navigates to /team?invite=true but the team page never reads the invite param — modal does not open
FILE: src/components/dashboard/widgets/people-widget.tsx:95 (area widget:people)
SUMMARY: Both invite buttons (empty-state CTA and the '+ Invite teammate' footer) call handleInvite, which falls back to router.push('/team?invite=true') because home/page.tsx:270 renders <PeopleWidget /> without the onInvite prop. The team page's invite modal is controlled solely by local state initialized to false (team/page.tsx:160 `const [showInviteModal, setShowInviteModal] = useState(false);`) and the file contains no useSearchParams/searchParams usage at all (grep over the file returns zero matche
SCENARIO: User clicks 'Invite teammate' in the People widget expecting the invite dialog; they are dropped on the Team page with nothing open and no indication of what to do next — the promised invite flow silently degrades to bare navigation.
FIX: In team/page.tsx, read useSearchParams() and setShowInviteModal(true) when invite=true (then strip the param), or pass onInvite from the home page.
VERIFIER: Reproduced from source: people-widget.tsx:95 pushes '/team?invite=true' because home/page.tsx:270 (and portal/dashboard/page.tsx:157) render <PeopleWidget /> without onInvite. team/page.tsx initializes showInviteModal to false (line 160) and contains no useSearchParams or any read of the 'invite' pa

### [low] Fetch failure shows the 'no projects' empty state instead of an error state
FILE: src/components/dashboard/widgets/status-updates-widget.tsx:180 (area widget:status-updates)
SUMMARY: There is no error state. A non-ok response (500/401) is silently ignored (rows stays []) and a network throw only console.errors; in both cases `!loading && rows.length === 0` renders the marketing empty state 'Keep your projects visible', falsely telling a user with many projects that they have none, with no retry affordance.
SCENARIO: The API returns 500 (or the session expired → 401) on home load: the PM sees 'Keep your projects visible... Go to projects' instead of an error message, concludes their status data is gone, and there is no way to retry short of a full page reload.
FIX: Track an `error` state when res.ok is false or fetch throws on the initial load; render an error message with a Retry button, distinct from the empty state.
VERIFIER: Reproduced from the actual code: status-updates-widget.tsx lines 105-111 have no handling for !res.ok (silently ignored) and the catch only console.errors, with no error state in the component; line 180 renders the 'Keep your projects visible' empty state whenever !loading && rows.length === 0, so a

### [low] Severe overfetch: widget pulls every task of every project in every portfolio to display name + count
FILE: src/app/api/portfolios/route.ts:67 (area widget:portfolios)
SUMMARY: GET /api/portfolios includes projects -> project -> tasks { id, completed, dueDate } for stats plus owner objects, for ALL of the user's portfolios (limit ignored). The widget uses only id, name, color, and a count. Payload and DB work scale O(portfolios x projects x tasks) for a 360px card.
SCENARIO: A workspace with 10 portfolios x 20 projects x 200 tasks makes the Home dashboard's portfolios tile trigger a query touching ~40,000 task rows and ship a multi-hundred-KB JSON response on every Home load.
FIX: Add a lightweight mode (e.g. ?limit=5&fields=summary) that selects id, name, color, _count.projects only, or a dedicated /api/portfolios/summary endpoint for the widget.
VERIFIER: Core flaw is real: GET /api/portfolios (src\app\api\portfolios\route.ts:14) accepts no request object, so the widget's ?limit=5 is ignored, and the Prisma query (lines 55-73) includes tasks {id, completed, dueDate} for every project of every visible portfolio — DB work genuinely scales O(portfolios 

### [low] Typing '@' leaves a stray '@' character after the inserted mention
FILE: src/components/dashboard/widgets/private-notepad-widget.tsx:726 (area widget:private-notepad)
SUMMARY: The '@' keydown handler calls saveSelection() before the browser inserts the '@' character (keydown is not prevented), so savedSelectionRef points to the pre-'@' position. insertMention then calls restoreSelection() and inserts the mention span at that stale position, leaving the literally-typed '@' dangling after the mention chip.
SCENARIO: User types 'ping @' then picks 'Juan' — the editor reads 'ping @Juan @' (mention inserted before the typed @, stray @ left behind).
FIX: preventDefault on the '@' keydown, or on insertMention delete the character before the restored caret / extend the restored range to cover the typed '@'.
VERIFIER: Reproduced from source: the '@' keydown handler (private-notepad-widget.tsx lines 725-729) calls saveSelection() and opens the picker without e.preventDefault(), so the browser then inserts the literal '@'. saveSelection (lines 222-227) clones the range at the pre-'@' caret, and DOM live-range rules

### [low] Draft list clips inside the 360px card — drafts beyond ~3 are invisible and undeletable
FILE: src/components/dashboard/widgets/draft-comments-widget.tsx:76 (area widget:draft-comments)
SUMMARY: The widget root is a plain `<div className="space-y-4">` with no h-full/flex column and no overflow-y-auto, while WidgetContainer wraps children in `flex-1 overflow-hidden px-4 pb-4` (widget-container.tsx:239). Each draft row is ~80px, so once more than ~3 drafts exist the rest are hard-clipped with no scrollbar — they can't be read or deleted. The sibling MentionsWidget solves this correctly with `h-full flex flex-col` + `flex-1 overflow-y-auto` (mentions-widget.tsx:57,65).
SCENARIO: Once the producer ships and a user accumulates 5 draft comments, only the first ~3 render inside the 360px card at half width; drafts 4-5 are clipped by overflow-hidden with no scroll affordance, so the user can neither see nor delete them, and the '5 drafts' counter contradicts the 3 visible rows.
FIX: Match the MentionsWidget pattern: root `h-full flex flex-col`, put the header row in a flex-shrink-0 block, and wrap the list in `flex-1 overflow-y-auto`.
VERIFIER: The CSS flaw is real and reproducible from the code: home page grid fixes every card at 360px (auto-rows-[360px], src/app/(dashboard)/home/page.tsx:351); WidgetContainer wraps children in `flex-1 overflow-hidden px-4 pb-4` (widget-container.tsx:239); and DraftCommentsWidget's root is a bare `<div cl

### [low] In-widget copy promises draft saving that no code implements (no producer anywhere)
FILE: src/components/dashboard/widgets/draft-comments-widget.tsx:94 (area widget:draft-comments)
SUMMARY: Grep across src confirms nothing writes uiState.draftComments or the 'buildsync-draft-comments' localStorage key — the only writer is the widget's own deleteDraft. Yet the widget's subtitle says 'Comments you haven't sent yet' and the empty state says 'Unsent comments will be saved here', which asserts an active behavior. Only the Customize modal (dashboard.ts:135) says 'Coming soon'. A user who adds the widget will type comments, navigate away, lose them, and find this widget forever empty.
SCENARIO: User enables the widget from Customize, half-writes a comment on a task, navigates away expecting it to be captured 'here', and the text is silently discarded — the widget's own copy told them the opposite of what the app does.
FIX: Until a producer exists, mirror the honest copy inside the widget itself (e.g. empty state: 'Draft saving is coming soon') or implement the producer in the task comment composer before shipping the widget in Customize.
VERIFIER: Reproduced from real code: draft-comments-widget.tsx reads uiState.draftComments (API) and localStorage key 'buildsync-draft-comments', but grep across src shows the only writer anywhere is the widget's own deleteDraft (delete-only, can never populate). In-widget copy at lines 78 and 94-96 ('Comment

### [low] 'View submissions' in the inline form editor opens the OWNER/ADMIN-only portal route
FILE: src/components/views/form-builder-dialog.tsx:538 (area widget:forms)
SUMMARY: The dialog opened by clicking a widget row exposes 'View submissions', which opens /portal/admin/forms/[id]/submissions in a new tab — a page gated to workspace OWNER/ADMIN inside the WORKER/ADMIN/OWNER-only portal layout — even though the dialog itself is available to any project EDITOR.
SCENARIO: A workspace MEMBER who is a project EDITOR (allowed to edit the form per assertFormEditAccess) opens a form from the widget, clicks More > View submissions → new tab redirects to the public marketing page; a WORKER lands on /portal/dashboard. The submissions are never shown despite the user having edit rights on the form.
FIX: Gate the menu item by workspace role, or build/point to a dashboard-side submissions view (an authorized API already exists at /api/forms/[formId]/submissions).
VERIFIER: Every cited code element checks out: form-builder-dialog.tsx:536-543 opens /portal/admin/forms/[id]/submissions in a new tab, and the "View submissions" menu item (lines 622-625) renders with no role gating whenever an existing form is open. The target page (src\app\(portal)\portal\admin\forms\[form

### [low] Deleting a form from the inline dialog leaves a dead row in the widget
FILE: src/components/dashboard/widgets/forms-widget.tsx:340 (area widget:forms)
SUMMARY: FormBuilderDialog's delete handler only closes the dialog and explicitly expects the parent to refetch on close, but the widget's onOpenChange just clears editingForm without refetching, so the deleted form stays in the list until the next 30s poll.
SCENARIO: User opens a form from the widget, chooses More > Delete form, confirms → dialog closes, toast says 'Form deleted', but the row is still listed. Clicking it fires GET /api/forms/[id]?settings=1 which 404s, showing 'Couldn't open the form. Try again.' for up to 30 seconds until the poll removes it.
FIX: Add an onDeleted callback to FormBuilderDialog (or refetch the list when the dialog closes) and filter the deleted id out of `forms`.
VERIFIER: Reproduced from the actual code. form-builder-dialog.tsx:510-531 handleDelete() only calls onOpenChange(false) after a successful DELETE, with a comment (lines 507-509) explicitly delegating list refresh to the parent on close. forms-widget.tsx:340-342 onOpenChange merely does setEditingForm(null) —

### [low] Fetch failure renders the 'no forms yet' educational empty state instead of an error state
FILE: src/components/dashboard/widgets/forms-widget.tsx:82 (area widget:forms)
SUMMARY: The widget has no error state: a non-ok response is silently ignored and a thrown fetch error only logs to console, leaving forms=[] so the widget shows the 'Turn requests into tasks' onboarding empty state to users who actually have forms.
SCENARIO: Session expires (401 from /api/forms) or a transient network/server error occurs on Home load → the widget confidently tells a user with 12 forms 'Turn requests into tasks / Create your first form', with no retry affordance and no hint anything failed.
FIX: Track an error flag when !res.ok or on catch, and render a compact error state with a Retry button (matching the Home page's own error pattern).
VERIFIER: Reproduced from the real code in src\components\dashboard\widgets\forms-widget.tsx: line 82 `if (res.ok && !cancelled)` has no else branch (non-ok responses silently dropped), the catch (line 86-87) only console.errors, the finally clears loading, and the render has exactly three branches — spinner 

### [low] Read-only project members see forms they can never open — every click errors with a misleading 'Try again'
FILE: src/components/dashboard/widgets/forms-widget.tsx:168 (area widget:forms)
SUMMARY: The list endpoint includes forms from projects where the user is a member of ANY role, but opening a row requires ?settings=1 which enforces owner/ADMIN/EDITOR, so VIEWER and COMMENTER members get a guaranteed 403 on every click, surfaced as a transient-sounding 'Couldn't open the form. Try again.'
SCENARIO: A user added to a project as VIEWER sees that project's forms in the Home widget; every click on a row spins briefly then toasts 'Couldn't open the form. Try again.' — retrying can never succeed, and there is no read-only fallback (e.g. opening the public form view).
FIX: Either have the widget fall back to the public /forms/[id] view on 403, or surface the API's distinct 403 with an honest message ('You don't have permission to edit this form'), or exclude non-editor projects from the widget query.
VERIFIER: Reproduced from the real code. (1) GET /api/forms (src/app/api/forms/route.ts:67-90) builds projectIds from projectMember.findMany({ where: { userId } }) with no role filter, so VIEWER/COMMENTER members' project forms are listed, and every row includes projectId (line 117). (2) In forms-widget.tsx o

### [low] Widget content is ~24px taller than the 360px card — the 4th tutorial row is clipped with no scroll
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:170 (area widget:learning)
SUMMARY: The grid row is 360px (home page auto-rows-[360px], line 351) and WidgetContainer wraps children in <div className="flex-1 overflow-hidden px-4 pb-4"> (widget-container.tsx line 239) — overflow-HIDDEN, no scroll. Available content height ≈ 360 − 56 (header: h-8 menu button + py-3) − 16 (pb-4) = 288px. Widget content: progress row ~24px (h-7 button, -mt-1) + 16px space-y-4 + 4 tutorial rows × 62px (p-3 + 36px two-line text + 2px border) + 3×8px space-y-2 = ~312px. The 'Setting goals and OKRs' row
SCENARIO: User enables the Learn TT widget at default half size → the last tutorial ('Setting goals and OKRs') renders with its description and bottom border visibly amputated; nothing indicates a 4th item exists in full, and there is no way to scroll to it.
FIX: Make the list scrollable (wrap in a div with overflow-y-auto / use h-full flex flex-col with min-h-0 on the list), or tighten row padding (p-2.5) and single-line-clamp descriptions (line-clamp-1) so 4 rows fit within 288px.
VERIFIER: Reproduced from the actual code. Grid row is fixed 360px (home/page.tsx:351 auto-rows-[360px]) at both half and full size. WidgetContainer classic chrome (widget-container.tsx:194-239): 1px card borders + 56px header (py-3 + h-8 button) + content wrapper 'flex-1 overflow-hidden px-4 pb-4' → ~286px u

### [low] 'Getting started with TT' links to /home — the page the widget lives on — so clicking it does nothing
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:27 (area widget:learning)
SUMMARY: The first tutorial's url is '/home'. The Learn TT widget only renders on the Home dashboard, so handleNavigate → router.push('/home') is a same-route push with no visible effect: no tutorial content, no tour overlay, no navigation. The most prominent onboarding item is a dead click.
SCENARIO: New user clicks 'Getting started with TT' (title, description, and chevron all invite a click) → nothing happens at all. User assumes the widget is broken.
FIX: Point it at real getting-started content (e.g. a docs/knowledge page — /knowledge exists), or mark it complete on click, or scroll/spotlight the dashboard — anything observable.
VERIFIER: Reproduced from the real code. src\components\dashboard\widgets\learning-widget.tsx lines 20-28 set the first tutorial's url to '/home', and handleNavigate (lines 128-130) does router.push(tutorial.url); both the content button and chevron button wire to it. The widget renders on the /home page (src

### [low] Shared /api/ai/assist prompt suffix contradicts the widget's Q&A prompt
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\ai\assist\route.ts:36 (area widget:ai-assistant)
SUMMARY: The route unconditionally appends 'Respond only with the improved/modified text, without any explanations or additional commentary.' — wording built for the text-improvement callers (private-notepad, inbox, ai-panel) — onto the widget's 'Answer the following question helpfully and concisely:' prompt, giving Claude two conflicting instructions for the Q&A use case.
SCENARIO: User asks 'How to create and manage projects'. The final prompt tells Claude to both answer the question AND respond only with 'the improved/modified text' of it — the model may return a rewritten version of the question or an oddly terse fragment instead of an answer, in the widget's primary flow.
FIX: Make the suffix caller-controlled (e.g. only append it when the request sets mode:'transform'), or have the widget call a dedicated Q&A path without the transform suffix.
VERIFIER: Reproduced from source: src\app\api\ai\assist\route.ts line 36 unconditionally appends 'Respond only with the improved/modified text, without any explanations or additional commentary.' to every request, and src\components\dashboard\widgets\ai-assistant-widget.tsx line 199 (plus src\components\ai\ai

### [low] Missing-API-key (503) and all other errors collapse into a misleading 'Please try again' message
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:208 (area widget:ai-assistant)
SUMMARY: The route returns 503 { error: 'AI features not configured' } when ANTHROPIC_API_KEY is unset (route lines 21-27), but the widget treats every non-ok status identically: setResponse('Sorry, I couldn\'t process your request. Please try again.') (line 208), rendered under a 'Response:' label as if it were an AI answer. Retrying can never fix a missing key. Additionally, if the model returns non-text content the route sends result:'' (route lines 42-44), which is falsy, so the widget (line 276 `res
SCENARIO: On a deployment without ANTHROPIC_API_KEY, every question spins, then shows 'Please try again' — the user retries indefinitely with no hint that AI is unconfigured, and each failed attempt is still logged into Past topics.
FIX: Branch on res.status: for 503 show 'AI features aren\'t configured for this workspace' (and consider hiding/disabling the widget), otherwise surface data.error; style errors distinctly from responses (e.g. red-tinted callout) and keep the typed question in the input.
VERIFIER: All cited code is verbatim accurate. The route (src\app\api\ai\assist\route.ts lines 21-27) returns 503 {error:'AI features not configured'} when ANTHROPIC_API_KEY is unset, and lines 42-44 return result:'' for non-text content. The widget (src\components\dashboard\widgets\ai-assistant-widget.tsx li

### [low] /api/projects ignores ?limit=20 and returns fully-hydrated projects for a names-only mention list
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\projects\route.ts:155 (area widget:ai-assistant)
SUMMARY: The widget fetches /api/projects?limit=20 on every mount just to build @mention entries (widget lines 91-93, using only id + name), but the GET handler never reads a limit param (only workspaceId and q, route lines 107-109) and its findMany has no take — it includes owner, every member with user objects, every root-level task, and _count for every visible project (route lines 155-206). The parallel /api/users call is fetched eagerly too, even if the @ picker is never opened.
SCENARIO: An L4+ user in a workspace with 80 projects x hundreds of tasks opens Home with this widget enabled: every page load transfers the entire project graph (members + all root tasks per project) over the wire just to populate at most 8 dropdown rows of names, slowing the dashboard and hammering the DB.
FIX: Honor take: limit in the GET (or add a lightweight ?fields=summary mode selecting id/name/color), and in the widget fetch mention data lazily on first @ click instead of on mount.
VERIFIER: Every factual claim checks out: the widget (ai-assistant-widget.tsx lines 88-114) eagerly fetches /api/projects?limit=20 on mount and uses only id+name, while the GET handler (src/app/api/projects/route.ts lines 107-109) never reads a limit param and its findMany (line 155) has no take, including ow

### [low] 'Reset to defaults' does not reset the background color
FILE: src/hooks/use-widget-preferences.ts:271 (area Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts))
SUMMARY: The sheet is titled 'Customize your Home' with Background as its first section, but the footer 'Reset to defaults' only resets widget visibility/order/sizes — the background tint (a uiState key, separate store) is untouched.
SCENARIO: User picks Burgundy, later clicks 'Reset to defaults' expecting the Asana-style factory state → widgets reset but the page stays tinted burgundy; there is no other 'remove background' affordance except manually clicking the white swatch.
FIX: In the onReset handler in page.tsx, also call setBackgroundId('default') (or accept an onResetBackground prop in the modal), or rename the button to 'Reset widgets'.
VERIFIER: Reproduced from real code. resetToDefaults (src\hooks\use-widget-preferences.ts:271-275) only rebuilds visibleWidgets/widgetOrder/widgetSizes and calls setPreferences; the background tint is stored separately via useUiState('home.background-color') (src\app\(dashboard)\home\page.tsx:105-106). A proj

### [low] 'Collaborators' chip is capped at 8 by the API's team slice
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\dashboard\ceo\route.ts:326 (area Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts)
SUMMARY: data.team is the workspace member list sorted by load and sliced to 8 entries (built for a Team Capacity tile that no longer exists), so `collaborators: data.team.length` in page.tsx shows min(actualMembers, 8). A 25-person firm reads '8 collaborators' forever. It is also 'workspace members', not people the viewer actually collaborates with.
SCENARIO: Any workspace with more than 8 members: the header permanently shows '8 collaborators' no matter how many people are in the org, and the number never changes as the team grows — an obviously frozen, dishonest stat.
FIX: Return an explicit `teamCount` (workspaceMembers.length before the slice) from the route, or better, count distinct users sharing projects/tasks with the viewer if the chip is meant to mean 'collaborators' in the Asana sense.
VERIFIER: Reproduced from real code: route.ts lines 316-326 build `team` from an uncapped workspaceMember.findMany, sort by load, and `.slice(0, 8)` (a shortlist artifact for a defunct Team Capacity tile). page.tsx line 177 sets `collaborators: data.team.length` and passes it to HomeHeader (line 327), which r

### [polish] Clicking a draft ignores draft.taskId and dumps the user on generic /my-tasks
FILE: src/components/dashboard/widgets/draft-comments-widget.tsx:108 (area widget:draft-comments)
SUMMARY: The row button pushes the bare template literal `/my-tasks` even though every DraftComment carries taskId (interface line 11) and the row is labeled 'On: {taskName}'. The established deep-link pattern already exists in the sibling MentionsWidget: `/my-tasks?task=${taskId}` or `/projects/${projectId}?task=${taskId}` (mentions-widget.tsx:87-93). The draft content is also not carried anywhere, so the click can never let the user resume/send the draft — the row's implicit promise.
SCENARIO: With drafts present, user clicks a draft labeled 'On: Foundation pour checklist' expecting to land on that task with their text restored; instead they get the generic My Tasks list and must hunt for the task manually, then retype the comment.
FIX: Push `/my-tasks?task=${draft.taskId}` (matching the mentions pattern) and, when the producer ships, prefill the comment composer with draft.content.
VERIFIER: The code matches the finding: draft-comments-widget.tsx:108 pushes bare `/my-tasks`, ignoring draft.taskId, and the draft content is not carried anywhere. However, the impact is neutralized by two facts. (1) The failure scenario is unreachable: src/types/dashboard.ts:129-139 documents that nothing i

---

# PENDIENTES DE VERIFICACIÓN (evidencia citada, verificador no corrió)

### [medium] Enter/Space on the title link or "..." menu button starts a drag instead of activating them — keyboard users can't open widget pages or the menu
FILE: src/components/dashboard/widget-container.tsx:218 (area Home grid drag & drop + resize mechanics (dnd-kit sortable grid))
SUMMARY: The drag listeners are spread on the whole header (lines 215-218), and useSortable's setActivatorNodeRef is never used, so active.activatorNode is null. In the installed @dnd-kit/core 6.3.1 KeyboardSensor activator (node_modules/@dnd-kit/core/dist/core.esm.js:1357-1364), the guard `if (activator && event.target !== activator) return false;` is skipped when activator is null, and `event.preventDefa
WHY-UNCERTAIN: verifier died
FIX: Add onKeyDown={(e) => e.stopPropagation()} to the title Link and menu trigger, or better: attach listeners to a dedicated grip element registered via setActivatorNodeRef so dnd-kit's own target check applies.

### [medium] Heavy 8-query aggregation endpoint invoked to render two chips
FILE: C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\dashboard\ceo\route.ts:304 (area Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts)
SUMMARY: Every home load runs the full former-CEO-cockpit pipeline — all projects with owner+counts, a groupBy over completed tasks, workspace members with users, 24 critical-path tasks, 12 activity rows, a distinct-assignee utilization query, and an UNBOUNDED fetch of every incomplete assigned task for per-member load — but page.tsx consumes only activity (for a capped count) and team.length. ~95% of the 
WHY-UNCERTAIN: verifier died
FIX: Add a lightweight /api/dashboard/home-summary that runs two counts (tasks completed in range, member count) and point the header at it; or trim /api/dashboard/ceo to what /home still consumes and add `take`/aggregate to the load query.

### [high] Created task never appears in MyTasksWidget or AssignedTasksWidget without a manual page reload
FILE: src/components/tasks/quick-create-task-modal.tsx:209 (area QuickCreateTaskModal (Gmail-style composer) + Home page integration)
SUMMARY: On success the modal only toasts and closes — there is no onCreated callback, no custom event, and no router.refresh(). Home mounts the modal with only open/onOpenChange (home/page.tsx:379-382), and both task widgets fetch exactly once on mount (my-tasks-widget.tsx:88-90 `useEffect(() => { fetchTasks(); }, [fetchTasks])`; assigned-tasks-widget.tsx:94-96 same pattern). No widget listens for any tas
WHY-UNCERTAIN: verifier died
FIX: Add an optional `onCreated?: (task) => void` prop to QuickCreateTaskModal, call it with the 201 response body; in home/page.tsx bump a refreshKey passed to MyTasksWidget/AssignedTasksWidget (or dispatch a `window` CustomEvent('task-created') that both widgets' fetch effects listen for, matching the 

### [medium] Close (X) silently discards a typed draft with no confirmation
FILE: src/components/tasks/quick-create-task-modal.tsx:276 (area QuickCreateTaskModal (Gmail-style composer) + Home page integration)
SUMMARY: The X button calls handleClose, which immediately wipes title, description, assignee, project, and dates. There is no 'discard draft?' confirm and no draft persistence, so a fully composed task (title + long description) is lost on one misclick — the X sits 24px from the Minimize button. Outside-click does NOT close (the composer is a plain fixed div, not a Radix Dialog), which is good, so X is th
WHY-UNCERTAIN: verifier died
FIX: In the X handler, if title.trim() or description.trim() is non-empty, show a confirm ('Discard this task?') or minimize instead of closing; alternatively keep the draft in state/localStorage so reopening restores it (Gmail keeps drafts).

### [medium] Due date displays one day early for users west of UTC (default 'Tomorrow' renders as 'Today')
FILE: src/components/tasks/quick-create-task-modal.tsx:188 (area QuickCreateTaskModal (Gmail-style composer) + Home page integration)
SUMMARY: The modal sends dueDate as a bare 'YYYY-MM-DD' string; the API stores `new Date(data.dueDate)` which JS parses as UTC midnight (api/tasks/route.ts:247). Every consumer then formats with LOCAL date components — assigned-tasks-widget.tsx:50 `new Date(d.getFullYear(), d.getMonth(), d.getDate())` and my-tasks-widget.tsx:38 `d.toDateString()`. In any UTC-negative timezone (this firm is in Florida, UTC-
WHY-UNCERTAIN: verifier died
FIX: Parse date-only strings in the API as local/date-only (e.g. `new Date(y, m-1, d)` or append 'T00:00:00' before parsing), or format all task due dates from the UTC components (getUTCFullYear/getUTCMonth/getUTCDate) in the widgets.

### [medium] Assignee picker truncated to 20 users, has no search, and excludes the current user
FILE: src/components/tasks/quick-create-task-modal.tsx:139 (area QuickCreateTaskModal (Gmail-style composer) + Home page integration)
SUMMARY: The modal fetches `/api/users` with no params, so the route applies its default `limit=20` (users/route.ts:30 `parseInt(searchParams.get("limit") || "20", 10)`) and always excludes the caller (`id: { not: userId }`, users/route.ts:107). In a workspace with more than 20 members, teammates past the 20th (alphabetical) simply never appear in the dropdown and there is no search/typeahead to reach them
WHY-UNCERTAIN: verifier died
FIX: Fetch `/api/users?limit=200` (or add a search input filtering client-side), and prepend the current user as an 'Assign to me' entry.

---

# PULIDO / CALIDAD PRO

- [low] (widget:my-tasks) Blur guard relies on e.relatedTarget, which is null in Safari — composer buttons misfire the blur-create — The composer's onBlur skips create only when relatedTarget matches [data-composer-action]; macOS Safari does not focus <button> elements on mousedown, so relatedTarget is null and handleCreateTask fires when clicking the due-date or details buttons — creating  || FIX: Guard with onMouseDown={(e) => e.preventDefault()} on the composer action buttons (prevents the input blur entirely), or track an isPointerDownOnAction ref set in onPointerDown, instead of relying on  [src/components/dashboard/widgets/my-tasks-widget.tsx:268]
- [low] (widget:my-tasks) Concurrent fetchTasks calls can resolve out of order and display stale data (no abort/sequence guard) — fetchTasks has no AbortController or request-sequence check, and it is fired from toggle, undo-toast, create, and modal-update paths that can overlap; an earlier slow GET can land after a later one and overwrite allTasks with pre-mutation data. || FIX: Keep a monotonically increasing request id (or AbortController) in a ref and ignore/abort responses that aren't the latest, as task-detail-modal.tsx already does with currentTaskIdRef. [src/components/dashboard/widgets/my-tasks-widget.tsx:73]
- [low] (widget:my-tasks) Error banner never clears, renders in black, and stacks on top of the fake empty state — setError('Failed to load data') is never reset to null on a subsequent successful fetch, the message is styled text-black (indistinguishable from body copy, no retry affordance), and because allTasks stays [] the widget simultaneously shows 'Failed to load dat || FIX: setError(null) at the start (or on success) of fetchTasks, style the message as an error (e.g. text-red-600 with a Retry button), and suppress the empty state while error is set. [src/components/dashboard/widgets/my-tasks-widget.tsx:327]
- [low] (widget:my-tasks) Overdue classification shifts by viewer timezone (timestamp compare against local midnight on datetime-stored dates) — Due dates are stored as full timestamps (composer sends local-midnight `toISOString()`, line 173; my-tasks page does the same) but the widget buckets by raw timestamp vs the viewer's local midnight, so the same task is 'Upcoming' for its creator and 'Overdue'  || FIX: Compare calendar days, not timestamps: derive year/month/day from the parsed date (or store date-only strings app-wide) and bucket with a dayOnly() comparison like due-date-picker.tsx:89-91 already do [src/components/dashboard/widgets/my-tasks-widget.tsx:109]
- [polish] (widget:my-tasks) Every button in the widget except the row itself shows the default arrow cursor (Tailwind v4 preflight) — The project uses tailwindcss ^4 (package.json:79), whose preflight sets buttons to cursor:default, and globals.css adds no global button{cursor:pointer} rule — so the three tab buttons, 'Create task', the completion checkbox, both composer action buttons, and  || FIX: Add cursor-pointer to each button's className (or add `button:not(:disabled){cursor:pointer}` to the @layer base block in globals.css to fix it app-wide). [src/components/dashboard/widgets/my-tasks-widget.tsx:224]
- [polish] (widget:my-tasks) Project badge has no truncation — long project names wrap/blow out the 360px half-width card — The task name gets truncate + min-w-0, but the project pill renders {task.project.name} raw inside an inline-flex span with no truncate/max-width/whitespace-nowrap, so realistic AEC project names ('TT-2024 Structural Retrofit — Building C Phase 2') wrap into a || FIX: Add `min-w-0 max-w-full truncate whitespace-nowrap` to the badge span (and `min-w-0` on its wrapper div) so the project name ellipsizes like the task name. [src/components/dashboard/widgets/my-tasks-widget.tsx:402]
- [polish] (widget:my-tasks) No optimistic checkbox update — the toggle feels dead for two round trips (comment even claims 'optimistic') — Clicking the completion circle awaits the PATCH and then a full list refetch before any visual change; there is no local state flip, no per-task pending state, and no disable during flight, despite the code comment describing 'optimistic fetch'. Asana strikes  || FIX: setAllTasks(prev => prev.map(t => t.id === taskId ? { ...t, completed: next } : t)) before the PATCH, revert in the catch, and drop the full refetch on the happy path. [src/components/dashboard/widgets/my-tasks-widget.tsx:140]
- [polish] (widget:my-tasks) Task rows and controls are not keyboard accessible; checkbox has no accessible name; tabs lack tab semantics — Each task row is a plain div with onClick (no role, tabIndex, or key handler) so keyboard users cannot open the detail modal; the completion toggle is an icon-only button with no aria-label (screen readers announce nothing meaningful, and its state isn't expos || FIX: Add aria-label={task.completed ? `Mark "${task.name}" incomplete` : `Complete "${task.name}"`} and aria-pressed to the checkbox; make the row a button (or add role="button" tabIndex={0} + Enter/Space  [src/components/dashboard/widgets/my-tasks-widget.tsx:363]
- [polish] (widget:my-tasks) Completed tab shows unbounded history in position order, not most-recently-completed first — The API orders by position asc / createdAt desc and the widget applies no sort, so the Completed tab's first 5 rows are whatever tasks happen to have the lowest position values — typically ancient tasks — instead of recent completions (Asana shows the last-com || FIX: Return completedAt from the GET and sort the Completed tab by completedAt desc client-side (or add orderBy completedAt when completed=true is requested), optionally windowing to recent completions. [src/app/api/tasks/route.ts:174]
- [polish] (widget:my-tasks) Dead useSession() call and cross-tab showAll leakage — useSession() is invoked with its result discarded (the comment admits the avatar it served was removed), subscribing the flagship widget to session-context re-renders for nothing; separately, the single showAll flag is shared across all three tabs, so expandin || FIX: Delete the useSession() call; either key showAll per tab (Record<TabType, boolean>) or reset it in setActiveTab. [src/components/dashboard/widgets/my-tasks-widget.tsx:57]
- [low] (widget:projects) Error banner and empty state render simultaneously, and the error is never cleared on refetch — When the fetch throws, setError fires but the list area still evaluates `projects.length === 0` and shows the full 'No projects yet / Create your first project' empty state directly under the 'Failed to load data' text — two contradictory states at once. The e || FIX: Call setError(null) at the start of each fetch, make error/empty/list mutually exclusive branches, and style the error distinctly (e.g. red text with a retry button). [src/components/dashboard/widgets/projects-widget.tsx:108]
- [low] (widget:projects) Refetch race condition and no loading feedback on sort change — The useEffect refetches on every sortBy change with no AbortController or cancelled flag, so rapid sort toggles can resolve out of order and commit stale data; it's currently masked only because the server ignores sort and all responses are identical, and it b || FIX: Add a cancelled flag or AbortController in the effect cleanup, and either set loading=true on refetch or keep a subtle in-flight indicator. [src/components/dashboard/widgets/projects-widget.tsx:39]
- [low] (widget:projects) ON_HOLD projects render no status badge — The Prisma ProjectStatus enum includes ON_HOLD (schema.prisma:568-574), but getStatusLabel has no case for it, returns '', and the render guard skips the pill entirely — so an on-hold project is the only one with no badge, indistinguishable from 'no status'. || FIX: Add `case 'ON_HOLD': return 'On hold';` plus a neutral pill class in getStatusClasses. [src/components/dashboard/widgets/projects-widget.tsx:57]
- [polish] (widget:projects) On track and Complete status pills are visually identical; At risk nearly so — getStatusClasses maps ON_TRACK and COMPLETE to the exact same classes, and AT_RISK uses the same text color with only a slightly different background/border tint — the three most important statuses are indistinguishable at a glance, and OFF_TRACK (the alarm st || FIX: Give each status a distinct hue within the brand palette (or at least distinct label weight/icon): e.g. Complete gets a check-tinted variant, At risk amber, Off track a high-contrast dark treatment. [src/components/dashboard/widgets/projects-widget.tsx:67]
- [polish] (widget:projects) Clickable project rows show the default arrow cursor (Tailwind v4, no pointer restore) — The project rows and the dashed 'Create project' row are raw <button> elements with no cursor class. The project uses Tailwind v4 (package.json: "tailwindcss": "^4"), whose preflight leaves buttons at cursor:default; globals.css has no compensating `button { c || FIX: Add `cursor-pointer` to the row buttons (or restore `button:not(:disabled){cursor:pointer}` globally in @layer base, which would fix the whole app at once). [src/components/dashboard/widgets/projects-widget.tsx:143]
- [low] (widget:goals) No request cancellation in fetchGoals — rapid tab switches can render the wrong tab's goals — fetchGoals has no AbortController and no staleness guard, unlike the /goals page which correctly aborts (goals/page.tsx:179-184). Responses that resolve out of order win last-write. Because the Company query is unfiltered (largest payload, slowest), clicking C || FIX: Mirror the goals page: create an AbortController in the effect, pass signal to fetch, abort in the cleanup, and swallow AbortError. [src/components/dashboard/widgets/goals-widget.tsx:106]
- [low] (widget:goals) PARTIAL status renders as raw enum text and is treated as an open goal; widget re-duplicates the status map goal-utils was created to centralize — The DB enum and the PATCH/check-in APIs accept PARTIAL (schema.prisma:1207; api/objectives/[objectiveId]/route.ts:10), and the goal detail UI can set it (STATUS_OPTIONS in src/lib/goal-utils.ts:64-70, label 'Partial'). The widget's formatStatusPill has no PART || FIX: Delete formatStatusPill and use getStatusOption() from @/lib/goal-utils for label + colors; add PARTIAL to the closed-status filter (or consciously keep it open, but label it properly). [src/components/dashboard/widgets/goals-widget.tsx:55]
- [polish] (widget:goals) Every clickable in the widget shows the default arrow cursor (Tailwind v4) — This project is on Tailwind v4 (package.json: "tailwindcss": "^4"), whose preflight sets buttons to cursor:default (unlike v3). There is no global override (globals.css has no `button { cursor: pointer }` rule) and the shadcn Button base (src/components/ui/but || FIX: Add cursor-pointer to the widget's buttons (or add a project-level `@layer base { button:not(:disabled) { cursor: pointer } }` since this affects every widget). [src/components/dashboard/widgets/goals-widget.tsx:267]
- [polish] (widget:goals) Tabs row + team selector cannot shrink — selector clips at half width on narrow viewports — The header row (line 170, `flex items-center justify-between mb-3`) holds three tabs (~190px incl. gap-4) plus, on the Team tab, an outline button whose only shrink affordance is max-w-[120px] on the name span (total ~170px). Nothing wraps and nothing has min- || FIX: Add flex-wrap to the header row (selector drops to its own line when tight) or give the tab group min-w-0 and shrink the selector with flex-shrink. [src/components/dashboard/widgets/goals-widget.tsx:170]
- [polish] (widget:goals) Error message rendered as plain black text with stray padding, stacked above normal content — The error line `{error && <p className="text-sm text-black px-4 py-2">{error}</p>}` uses text-black (indistinguishable from body copy), adds px-4 inside a container that already gets px-4 from WidgetContainer (double indent), and renders above the skeleton/lis || FIX: Make error a terminal state of the content area (icon + message + Retry button), use the app's muted/destructive styling, and clear it on refetch. [src/components/dashboard/widgets/goals-widget.tsx:221]
- [low] (widget:assigned-tasks) Composer assignee picker silently capped at 20 workspace members with no search — The modal fetches /api/users with no limit param; the route defaults to limit=20 ordered by name (users/route.ts:30, take: limit at line 110), so in a workspace with more than 20 members, teammates later in the alphabet can never be assigned from the quick com || FIX: Fetch with an explicit higher limit (e.g. /api/users?limit=100) and/or add a search input to the assignee dropdown. [src/components/tasks/quick-create-task-modal.tsx:139]
- [low] (widget:assigned-tasks) GET /api/tasks/assigned overfetches: unbounded, unscoped, full task rows for a 6-field widget — The query has no take limit, no workspace scoping, and no select on the task itself, so every task the user ever delegated — including the entire completed history and each task's full description and all scalar columns — is returned on every widget mount and  || FIX: Add select for the 6 fields used, a take (e.g. 200), and consider excluding completed tasks older than N days or paginating the Completed tab. [src/app/api/tasks/assigned/route.ts:15]
- [low] (widget:assigned-tasks) Tab row overflows and clips at half width on ~1024px viewports and mobile — Four flex-1 tabs with whitespace-nowrap labels plus '(N)' counts have a minimum content width of roughly 350px; inside a half-width card the tab strip gets less than that on ≤~1080px viewports (sidebar + grid px-6 + card px-4), and there is no wrap, truncation || FIX: Allow the strip to scroll (overflow-x-auto) or drop the '(N)' counts below a container query breakpoint; alternatively use shorter labels ('Week/Later/Overdue/Done') at half size. [src/components/dashboard/widgets/assigned-tasks-widget.tsx:185]
- [polish] (widget:assigned-tasks) No optimistic toggle — checkbox feels dead for two round-trips, and rapid toggles race — handleToggleComplete awaits the PATCH and then a full re-fetch before any visual change, so on slow connections the circle doesn't respond to the click at all; additionally two quick toggles fire two concurrent fetchTasks() whose responses can resolve out of o || FIX: Flip the task in local state immediately, roll back on PATCH failure, and guard fetchTasks with an incrementing request id (ignore stale responses). [src/components/dashboard/widgets/assigned-tasks-widget.tsx:112]
- [polish] (widget:assigned-tasks) Widget is mouse-only: rows, toggle, and tabs lack keyboard/AT semantics — Task rows are plain divs with onClick (no role, tabIndex, or Enter/Space handling), the complete-toggle button has no aria-label (icon-only), and the tab strip has no role=tablist/aria-selected, so keyboard and screen-reader users cannot open a task, mark it c || FIX: Make rows <button> or add role="button" tabIndex={0} + onKeyDown; add aria-label={task.completed ? 'Mark incomplete' : 'Mark complete'} to the toggle; add role=tablist/tab/aria-selected to the tab str [src/components/dashboard/widgets/assigned-tasks-widget.tsx:229]
- [polish] (widget:assigned-tasks) Arrow cursor on tabs and CTAs — Tailwind v4 dropped button cursor:pointer and no compat rule exists — The project uses tailwindcss ^4 (which removed preflight's cursor:pointer on buttons) and globals.css has no restoring base rule, so the widget's tab buttons, the 'Assign task' outline Button, and the composer's Create/minimize/close/date-pill buttons all show || FIX: Add a base layer rule `button:not(:disabled), [role="button"]:not(:disabled) { cursor: pointer }` in globals.css (the official Tailwind v4 upgrade-guide snippet), or add cursor-pointer to the Button c [src/components/dashboard/widgets/assigned-tasks-widget.tsx:187]
- [polish] (widget:assigned-tasks) Long assignee names overflow the row and collide with the due-date label — The task name gets truncate, but the assignee name span under it has no truncate/min-w-0; as a flex item with min-width:auto its text cannot shrink, so a long name ('Maria Alejandra Rodriguez Castellanos') extends past the flex-1 column and paints into/under t || FIX: Add min-w-0 to the avatar row and truncate to the name span (matching the composer's 'truncate max-w-[120px]' pattern). [src/components/dashboard/widgets/assigned-tasks-widget.tsx:272]
- [low] (widget:people) Date math for 'today' and period start uses server-local time, skewing overdue/completed buckets for users in other timezones — today = new Date(now.getFullYear(), now.getMonth(), now.getDate()) and the Monday/month periodStart (lines 127-131) are computed in the server's timezone. On a UTC-deployed server, a user in UTC-6 (e.g., Central America, matching this project's Spanish-locale  || FIX: Accept the client's timezone offset (or IANA zone) as a query param and compute today/periodStart in the user's zone, mirroring however my-tasks handles due-date bucketing. [src/app/api/users/route.ts:121]
- [polish] (widget:people) All clickables show default arrow cursor (Tailwind v4 preflight, no cursor-pointer anywhere in the widget) — The project is on Tailwind v4 (package.json: "tailwindcss": "^4"; globals.css line 1: @import "tailwindcss"), whose preflight sets buttons to cursor: default, and globals.css has no global button cursor override. None of the widget's interactive elements — the || FIX: Add cursor-pointer to each interactive className, or add a base-layer `button:not(:disabled) { cursor: pointer }` rule in globals.css (fixes the whole app). [src/components/dashboard/widgets/people-widget.tsx:190]
- [polish] (widget:people) Every tab/filter switch blanks the list to a skeleton and hides the footer CTA, causing flash and layout shift — fetchPeople unconditionally setLoading(true) on every period/filter change, replacing the already-rendered people list with the 3-row skeleton for the round-trip duration; simultaneously the footer '+ Invite teammate' is gated on `!loading && people.length > 0 || FIX: Only show the skeleton on initial load (people.length === 0); during refetches keep rows rendered with a subtle opacity/inline spinner, and gate the footer on people.length alone. [src/components/dashboard/widgets/people-widget.tsx:67]
- [polish] (widget:people) Three always-visible stat pills crush the name to ~80px at half width; tabs lack tab semantics — At half size near the md breakpoint the widget is ~360px wide (~324px usable row width). The pill cluster is flex-shrink-0 and always renders all three pills ('0 overdue' ~60px + '0 upcoming' ~68px + '0 done' ~48px + gaps ≈ 188px); with avatar+gaps (~56px) the || FIX: Hide zero-value pills (or collapse to icon+number at half width via container query), and add role="tablist"/role="tab"/aria-selected to the period tabs. [src/components/dashboard/widgets/people-widget.tsx:206]
- [low] (widget:status-updates) Project picker shows an infinite 'Loading projects...' spinner when the list is empty or the fetch fails — The dropdown uses `projects.length === 0` as its only loading indicator, so a successful empty response ([]) and a failed fetch (error swallowed by the empty catch) both render a spinner that never resolves. || FIX: Track separate `pickerLoading`/`pickerError` state; render 'No projects' and an error message as distinct dropdown states. [src/components/dashboard/widgets/status-updates-widget.tsx:336]
- [low] (widget:status-updates) Picker overfetches: /api/projects returns owners, full member rosters, and every root task just to render name + color — The dropdown needs only { id, name, color } but calls the unparameterized /api/projects, whose GET includes owner, all members with nested user objects, all root-level tasks (id/completed/taskType/dueDate per task), and _count for every accessible project — po || FIX: Add a lightweight query mode (e.g. /api/projects?fields=picker) or reuse /api/projects/status-overview's already-fetched rows (id/name/color are all present) instead of a second endpoint. [src/components/dashboard/widgets/status-updates-widget.tsx:160]
- [low] (widget:status-updates) Widget shows only one workspace's projects while the picker spans all workspaces — status-overview scopes to a single workspace via getUserWorkspaceId (heuristic: first workspace with >1 members), but the picker's /api/projects merges visibility clauses across all of the user's workspace memberships — so a multi-workspace user can post an up || FIX: Iterate memberships like /api/projects does, or accept a workspaceId param from the widget. [src/app/api/projects/status-overview/route.ts:32]
- [low] (widget:status-updates) Relative-day math is elapsed-time based, not calendar-day based in the user's timezone — daysSinceUpdate = Math.floor((now - createdAt) / 86400000) counts 24-hour blocks, so an update posted yesterday evening reads 'Updated today' the next morning, and there is no 'yesterday' calendar boundary. The client then renders this pre-bucketed integer (wi || FIX: Compute the relative label client-side from lastUpdate.createdAt using local calendar-day difference (startOfDay deltas), keeping the server integer only for the 14-day stale threshold. [src/app/api/projects/status-overview/route.ts:116]
- [polish] (widget:status-updates) Row and footer buttons show the arrow cursor (Tailwind v4 removed cursor:pointer on buttons) — The project has tailwindcss ^4, whose preflight makes <button> use cursor: default. The clickable row buttons (line 238) and the 'Post status update' trigger (line 326) have no cursor-pointer class and no global button cursor rule exists in globals.css, so the || FIX: Add cursor-pointer to the row button and footer trigger (or add a base-layer `button:not(:disabled) { cursor: pointer }` rule globally). [src/components/dashboard/widgets/status-updates-widget.tsx:238]
- [polish] (widget:status-updates) ON_HOLD dot color doesn't match ProjectOverview despite the comment claiming parity — STATUS_DOT's comment says it 'matches ProjectOverview.STATUS_VISUAL so the widget and the project page read as one product', but ON_HOLD uses #94a3b8 (slate-400) here versus bg-slate-500 (#64748b) on the project page. ON_TRACK/AT_RISK/OFF_TRACK/COMPLETE do mat || FIX: Change ON_HOLD to #64748b (slate-500) to match STATUS_VISUAL. [src/components/dashboard/widgets/status-updates-widget.tsx:77]
- [polish] (widget:status-updates) 'Updated ... by {author}' meta line has no truncation, so long author names wrap and displace the Stale badge at half width — The project name uses truncate and the summary uses line-clamp-2, but the timestamp/author span has no truncate/min-w-0 and the Clock icon lacks flex-shrink-0; with a long author name at half width (~300px of content inside the 360px card) the flex line wraps  || FIX: Add `min-w-0 truncate` to the text span, `flex-shrink-0` to the Clock icon, keep the Stale badge in a non-wrapping position; bump skeleton rows to match real row height. [src/components/dashboard/widgets/status-updates-widget.tsx:292]
- [polish] (widget:portfolios) Portfolio rows use <button> + router.push instead of <Link> — no cmd/middle-click, no href, no prefetch — Rows (line 72-75), "View all" (line 45), and the empty-state CTA (line 64) are all buttons with router.push. Asana's equivalents are real anchors: users cannot open a portfolio in a new tab via middle-click/ctrl-click, there is no status-bar href preview, and  || FIX: Replace navigation buttons with next/link <Link href> styled identically. [src/components/dashboard/widgets/portfolios-widget.tsx:72]
- [polish] (widget:portfolios) Clickable rows show the default arrow cursor (Tailwind v4 preflight, no cursor-pointer) — The project uses tailwindcss ^4 (package.json:79), whose preflight leaves buttons at cursor: default. The row button's class list has hover:bg-gray-50 but no cursor-pointer, globals.css has no button cursor override, and the shared shadcn Button base (button.t || FIX: Add cursor-pointer to the row class (or fix globally by adding cursor-pointer to the shadcn Button base and a `button { cursor: pointer }` layer rule); becomes moot if rows convert to <Link>. [src/components/dashboard/widgets/portfolios-widget.tsx:74]
- [polish] (widget:portfolios) Skeleton rows don't match real row height — layout shift when data arrives — Loading skeletons are h-14 (56px) x3 with space-y-3 (12px gaps), but real rows are ~46px (p-3 + one text line + border) with space-y-2 (8px gaps) and there can be up to 5+ of them — content jumps when loading resolves. || FIX: Match the skeleton to the real row: same height (~h-11), same space-y-2, and render 4-5 placeholders. [src/components/dashboard/widgets/portfolios-widget.tsx:54]
- [low] (widget:private-notepad) Notes typed in the last 2s are lost entirely on refresh/tab close — localStorage write is also debounced — The localStorage cache is written inside saveNote itself (lines 170-179), which only runs from the 2s debounce or blur. Refresh (F5) or tab close fires neither blur nor the timer, and there is no beforeunload/pagehide flush and no keepalive on the fetch, so ke || FIX: Write to localStorage synchronously in handleContentChange (cheap), and add a pagehide handler using fetch(..., { keepalive: true }) or navigator.sendBeacon. [src/components/dashboard/widgets/private-notepad-widget.tsx:168]
- [low] (widget:private-notepad) Inline-code button injects the raw selection into insertHTML — HTML metacharacters are parsed, not escaped — The inlineCode action interpolates selection.toString() directly into an insertHTML string without escaping, so selected text containing <, >, or & is parsed as live HTML. Selecting literal markup (e.g. '<img src=x onerror=alert(1)>') and clicking inline code  || FIX: Escape the text (replace & < >) before interpolation, or create the <code> element and set textContent, then insert its outerHTML. [src/components/dashboard/widgets/private-notepad-widget.tsx:591]
- [low] (widget:private-notepad) Overfetch: loads every accessible workspace note (with authors and collaborators) to find one, and eagerly fetches users for a picker most sessions never open — The widget calls GET /api/workspace/notes with no filter; the route returns the full content of every note the user can see, each including author and all collaborator user objects (route lines 27-78), just so the client can .find() one magic title. Separately || FIX: Support ?title= (or a dedicated /api/workspace/notes/notepad endpoint) returning only id+content, and lazy-load users the first time the mention picker opens. [src/components/dashboard/widgets/private-notepad-widget.tsx:125]
- [polish] (widget:private-notepad) Error state is misleading, styled black, and never clears — The only error UI is 'Failed to load data' rendered in text-black (not a warning color), and it is set exclusively by the mentions users fetch failing (line 111) — not by note load or save failures, which are the operations that matter. Note-load failure silen || FIX: Scope the message ('Couldn't load teammates for mentions'), style it as a warning, clear it on retry, and add a distinct offline/save-failed notice. [src/components/dashboard/widgets/private-notepad-widget.tsx:752]
- [polish] (widget:private-notepad) Icon buttons missing cursor-pointer under Tailwind v4, and overlays lack keyboard dismissal — The project uses Tailwind v4 (package.json: "tailwindcss": "^4"), whose preflight gives <button> cursor:default, and globals.css has no global button cursor override — so every notepad toolbar button, emoji button, mention row, and the AI modal close button sh || FIX: Add cursor-pointer and aria-label to the icon buttons, anchor the popovers to the trigger (Radix Popover is already in the stack), and support Escape/focus trap. Also replace the native prompt('Enter  [src/components/dashboard/widgets/private-notepad-widget.tsx:785]
- [low] (widget:draft-comments) Delete is fire-and-forget — a failed PATCH silently resurrects the draft on next load — deleteDraft optimistically removes the row and fires the PATCH with `.catch(() => {})`, never checking res.ok. But the load path prefers the API over localStorage (lines 28-37), so if the PATCH fails (offline, 500), the DB still holds the draft and it reappear || FIX: Check res.ok, and on failure restore the item with a brief error notice (or retry); use `setDrafts(prev => prev.filter(...))` and derive the PATCH body from that result. [src/components/dashboard/widgets/draft-comments-widget.tsx:64]
- [low] (widget:draft-comments) PATCH uiState merge is an unserialized read-modify-write — concurrent writers can undo a draft deletion — The PATCH handler reads uiState, merges in JS, then upserts — no transaction or row lock. The home page has multiple concurrent uiState writers (use-ui-state.ts schedulePatch for home.period/background, this widget's deleteDraft). Two overlapping PATCHes each  || FIX: Wrap read+merge+write in prisma.$transaction (or use a raw jsonb_set update) so concurrent uiState PATCHes serialize per user. [src/app/api/users/preferences/route.ts:67]
- [low] (widget:draft-comments) First-visit race in GET: three concurrent preference fetches can double-create the row and 500 — GET creates the UserPreferences row when missing. On a brand-new user's first home load, three GETs fire concurrently (useWidgetPreferences:124, use-ui-state fetchUiState:71, this widget line 28). Two can both see null and both call create; the loser throws P2 || FIX: Replace find+create with `prisma.userPreferences.upsert({ where: { userId }, update: {}, create: { userId } })` which is race-safe on the unique key. [src/app/api/users/preferences/route.ts:17]
- [polish] (widget:draft-comments) Widget re-fetches the whole preferences row instead of joining the shared uiState cache (overfetch + duplicate request) — The widget issues its own GET /api/users/preferences on every mount even though the home page already fetches that exact endpoint via useWidgetPreferences and via use-ui-state's module-level shared/deduped cache (use-ui-state.ts:57-91). It also receives the en || FIX: Rewrite the widget on useUiState<DraftComment[]>('draftComments', []) — it shares the single in-flight fetch, handles the localStorage cache, and its setValue already does the debounced key-scoped PAT [src/components/dashboard/widgets/draft-comments-widget.tsx:28]
- [polish] (widget:draft-comments) No pointer cursor on either clickable in the row (Tailwind v4 — buttons default to arrow cursor) — The project uses Tailwind v4 (package.json: "tailwindcss": "^4"), whose preflight no longer sets cursor:pointer on buttons; globals.css has no base button cursor rule (only .mobile-pill-tab at line 279), and the shadcn buttonVariants base (button.tsx:8) contai || FIX: Add `cursor-pointer` to both the row button and the delete Button (or add a base-layer `button:not(:disabled){cursor:pointer}` rule in globals.css to fix it app-wide). [src/components/dashboard/widgets/draft-comments-widget.tsx:106]
- [polish] (widget:draft-comments) Icon-only delete button has no accessible name; row button has no focus-visible styling — The Trash2 delete Button carries no aria-label or title, so screen readers announce an unnamed button, and there is no tooltip on hover. The destructive action also has no confirm or undo. The plain navigation <button> (line 106) relies on the browser default  || FIX: Add `aria-label={`Delete draft on ${draft.taskName}`}` (and a title tooltip) to the delete Button, add focus-visible ring classes to the row button, and offer an undo toast after delete. [src/components/dashboard/widgets/draft-comments-widget.tsx:114]
- [polish] (widget:draft-comments) Date formatting: hardcoded en-US, no year on old drafts, no guard against invalid dates — formatDate always renders `toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`: a draft from 11 months ago shows just 'Aug 12' with no year, an unparseable createdAt renders the literal string 'Invalid Date', and the locale is pinned rather than t || FIX: Adopt the mentions widget's relative-time helper (extract it to a shared util), include the year when the date falls outside the current year, and fall back to an empty string when `isNaN(d.getTime()) [src/components/dashboard/widgets/draft-comments-widget.tsx:71]
- [low] (widget:forms) Project picker shows 'No projects yet' while the projects request is still in flight — The New form dropdown has no loading state: projects are fetched only after the picker opens, so users with projects always see a 'No projects yet' / 'Create a project first' flash until the response lands, and if the fetch fails (error swallowed at line 143-1 || FIX: Track a projectsLoading state and render a spinner row while the fetch is pending; surface fetch failure instead of ignoring it. [src/components/dashboard/widgets/forms-widget.tsx:309]
- [low] (widget:forms) Project picker overfetches /api/projects — full member rosters, owner, and all root tasks for a name+color dropdown — The widget calls bare GET /api/projects, whose response includes each project's owner, complete members array (with user emails/images), every root-level task (id/completed/taskType/dueDate), and counts — hundreds of KB on real workspaces — when the picker onl || FIX: Add a lightweight mode (e.g. /api/projects?fields=picker or a ?q= slim path) returning id/name/color only, and use it here. [src/components/dashboard/widgets/forms-widget.tsx:138]
- [polish] (widget:forms) Clickable form rows and '+ New form' button show the default arrow cursor — This project is on Tailwind v4, whose preflight no longer applies cursor:pointer to buttons, and neither the form-row button, the footer '+ New form' button, nor the shared Button component adds cursor-pointer — so the widget's primary click targets read as in || FIX: Add cursor-pointer to the row and footer buttons (or add a global `@layer base { button:not(:disabled) { cursor: pointer } }` rule as shadcn recommends for Tailwind v4). [src/components/dashboard/widgets/forms-widget.tsx:261]
- [polish] (widget:forms) Empty-state content slightly exceeds the 360px card and gets clipped (no scroll on that branch) — The educational empty state stacks icon (56px) + heading + a ~4-line paragraph + a 4-item example list + the CTA button (~310px total with margins) inside the ~296px content area (360px row minus header and pb-4), and its container is justify-center inside the || FIX: Trim one element (e.g. drop the 4-item example list to 2), reduce vertical margins, or add overflow-y-auto to the empty-state branch like the list branch has. [src/components/dashboard/widgets/forms-widget.tsx:194]
- [low] (widget:mentions) Mentions from private projects leak comment text and 404 on click — The API scopes only by workspace (`task: { OR: [{ project: { workspaceId } }, ...] }`) with no project-visibility/membership check, while the project page denies non-members of PRIVATE projects via notFound(). A user mentioned in a private project they cannot  || FIX: Either extend the where-clause to match the project page's access rules (visibility PUBLIC OR members/owner include user), or keep showing the mention but route/behave gracefully (e.g. disable click w [src/app/api/mentions/route.ts:32]
- [low] (widget:mentions) Mentions on project-less tasks assigned to the user (but created by someone else) are excluded — For tasks without a project the filter is `{ projectId: null, creatorId: userId }`, but the app's own access rule (verifyTaskAccess) grants access when the user is creator OR assignee. A teammate creating a personal task, assigning it to you, and @-mentioning  || FIX: Change the second OR branch to `{ projectId: null, OR: [{ creatorId: userId }, { assigneeId: userId }] }`. [src/app/api/mentions/route.ts:34]
- [low] (widget:mentions) Workspace-membership failure returns 500 instead of 403 — getUserWorkspaceId throws AuthorizationError for a user with no workspace membership; the route's generic catch converts it to a 500 'Failed to fetch mentions' instead of using the codebase's getErrorStatus mapping (403). || FIX: In the catch, use `const { status, message } = getErrorStatus(error); return NextResponse.json({ error: message }, { status });` like other routes that import auth-guards. [src/app/api/mentions/route.ts:17]
- [polish] (widget:mentions) Clickable mention rows show the default arrow cursor (Tailwind v4) — The row <button> has no cursor-pointer class. The project uses Tailwind v4 (package.json: "tailwindcss": "^4"), whose preflight no longer applies cursor:pointer to buttons, and globals.css adds no button cursor override — so hovering a mention row shows the de || FIX: Add `cursor-pointer` to the row button's className. [src/components/dashboard/widgets/mentions-widget.tsx:86]
- [polish] (widget:mentions) Author name and task name are not truncated — long values wrap and break row rhythm in the 360px card — The comment body is line-clamped, but the author-name span (line 104) and the 'on {taskName}' line (lines 108-110) have no truncate/line-clamp. At half width (~300px of content) a long author name wraps and pushes the timestamp, and a long task name wraps onto || FIX: Add `truncate` to the author-name span (and `min-w-0` handling in its flex row, e.g. `flex-shrink-0` on the timestamp) and `truncate` to the task-name <p>. [src/components/dashboard/widgets/mentions-widget.tsx:104]
- [polish] (widget:mentions) formatDate shows '0m ago' for fresh mentions and omits the year for old ones — Mentions under one minute old render as '0m ago' instead of 'just now', and mentions older than a year render as e.g. 'Jul 3' with no year, indistinguishable from this year's dates. || FIX: Return 'Just now' when diff < 60000, and include `year: 'numeric'` when the date's year differs from the current year. [src/components/dashboard/widgets/mentions-widget.tsx:51]
- [polish] (widget:mentions) The 'info' title icon promised as a tooltip has no tooltip content — The widget's own comment says the container provides a 'Title + info tooltip', and dashboard.ts pairs the mentions widget with titleIcon:'info', but WidgetContainer renders a bare <Info> icon with no Tooltip wrapper, title attribute, or aria-label — hovering i || FIX: Wrap the icon in the app's Tooltip component using the widget's `description` from AVAILABLE_WIDGETS ('Comments where you were @mentioned') as the content, with an aria-label. [src/components/dashboard/widget-container.tsx:105]
- [low] (widget:learning) Widget issues its own duplicate GET of the full preferences row already fetched twice on Home load — On every Home load, /api/users/preferences is GET-fetched by use-widget-preferences.ts (line 124), use-ui-state.ts (line 71, used by home/page.tsx), dashboard-shell.tsx (line 74), and again by this widget — plus draft-comments and ai-assistant widgets if enabl || FIX: Share one preferences fetch via the existing useUiState hook (it already GETs once and exposes uiState) or a small SWR/context provider, instead of a private fetch per widget. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:64]
- [low] (widget:learning) Mount fetch can overwrite a toggle made while the request is in flight — The mount effect unconditionally setTutorials() from the server response with no cancelled/stale guard and no check against in-flight local mutations. If the user toggles a tutorial before the initial GET resolves (slow connection), the GET response then reset || FIX: Track a hasInteracted ref (skip the fetch's setTutorials once the user has toggled) or apply the server data only before first interaction; also add the cancelled-flag cleanup pattern used elsewhere i [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:75]
- [polish] (widget:learning) No loading state — saved completed/dismissed state pops in after render (flash of wrong state) — State initializes to the static TUTORIALS (all incomplete) and dismissed=false, then the async preferences fetch mutates it. A returning user briefly sees '0 of 4 completed' with an empty progress bar before checkmarks/strikethroughs pop in; a user who dismiss || FIX: Add an isLoaded flag set after the fetch settles and render a lightweight skeleton (4 gray row placeholders) until then, matching the neighboring widgets. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:57]
- [polish] (widget:learning) No cursor-pointer on any clickable in the widget (Tailwind v4 leaves buttons cursor:default), and completed rows lose hover feedback — The project uses tailwindcss ^4 (package.json line 79), whose preflight does NOT set cursor:pointer on buttons; globals.css has no button cursor rule and buttonVariants (src\components\ui\button.tsx line 8) has none either. So the tutorial-content button (line || FIX: Add cursor-pointer to the three row buttons (or to a shared base), keep a hover state on completed rows (e.g. hover:bg-slate-50 unconditionally), and consider adding cursor-pointer to buttonVariants g [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:221]
- [polish] (widget:learning) Icon-only dismiss button has no accessible name; each row exposes two duplicate tab stops for the same navigation — The X dismiss Button (lines 183-190) renders only <X className="h-4 w-4" /> with no aria-label — screen readers announce it as 'button' with no name, and it hides the entire widget when activated. Separately, each tutorial row has two adjacent buttons that bot || FIX: Add aria-label="Dismiss learning widget" to the X button; make the duration/chevron decorative (aria-hidden, tabIndex={-1} or move it inside the content button) so each row has one navigation stop. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\learning-widget.tsx:183]
- [low] (widget:ai-assistant) Failed questions are permanently recorded in Past topics, and the question is erased on failure — handleSubmit saves the topic to localStorage + DB before the request is sent (lines 185-192), so questions that fail (network error, 503, 500) still pollute Past topics; the finally block then clears the input (line 214), so after an error the user must retype || FIX: Move savePastTopics into the res.ok branch (or roll back on failure), and only clear the input on success. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:192]
- [low] (widget:ai-assistant) Race between prefs load and first save can wipe DB-stored past topics — savePastTopics writes [newTopic, ...pastTopics] where pastTopics is component state; if the user submits (e.g. click suggestion + Enter) before the mount-time GET /api/users/preferences resolves, pastTopics is still [] and the PATCH replaces uiState.aiPastTopi || FIX: Track a loaded flag and queue/skip saves until the initial GET settles (or have the server merge-append arrays for aiPastTopics). [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:164]
- [low] (widget:ai-assistant) formatRelativeDate produces '1 days ago' and miscounts calendar days — days = Math.floor(diff / 86400000) counts 24-hour blocks, not calendar days, and the label template does not pluralize: a topic from 2 calendar days ago (e.g. 38 hours) fails both the isToday and isYesterday checks and renders '1 days ago' — grammatically wron || FIX: Compute days from local-midnight boundaries (like the isToday/isYesterday checks already do) and use `${days} day${days === 1 ? '' : 's'} ago`. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:66]
- [polish] (widget:ai-assistant) No cursor-pointer on any clickable — Tailwind v4 buttons default to the arrow cursor — The project is on tailwindcss ^4 (package.json line 79), whose preflight no longer applies cursor:pointer to buttons, and globals.css has no button cursor override (grep for 'button' in globals.css: no matches). None of the widget's clickables — tab buttons (l || FIX: Add cursor-pointer to each interactive element (or add a base-layer `button:not(:disabled){cursor:pointer}` rule in globals.css to fix this app-wide). [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:302]
- [polish] (widget:ai-assistant) AI response rendered in a bare <p>: newlines collapse and long tokens overflow — Claude answers routinely contain line breaks and lists, but the response is rendered as <p className="text-sm text-gray-700">{response}</p> with no whitespace-pre-wrap, so all formatting collapses into one paragraph blob; there is also no break-words, so an un || FIX: Add 'whitespace-pre-wrap break-words' to the response <p> (or render minimal markdown). [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:281]
- [polish] (widget:ai-assistant) Mention picker lacks keyboard support, Escape close, loading state, and icon-button labels — The @ picker can only be dismissed by clicking the backdrop div (lines 349-352) — no Escape handler, and the backdrop is a plain div invisible to keyboards; arrow keys/Enter do not navigate the result list; while the mount-time users/projects fetch is still in || FIX: Add onKeyDown for Escape/ArrowUp/ArrowDown/Enter on the picker, aria-label="Insert mention" / aria-label="Send question" on the icon buttons, and a small 'Loading...' row while mentionItems is unfetch [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\dashboard\widgets\ai-assistant-widget.tsx:346]
- [low] (widget:ai-assistant) /api/ai/assist is an unmetered authenticated Claude proxy — The route accepts an arbitrary client-supplied `prompt` and `text` (line 12) and forwards them to anthropic.messages.create with no rate limiting, length cap, or prompt allow-list — any authenticated user can script unlimited 1024-token completions on the work || FIX: Add per-user rate limiting (e.g. token bucket keyed on userId), cap text/prompt length, and consider replacing the free-form prompt field with a server-side enum of allowed operations. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\ai\assist\route.ts:12]
- [low] (Home grid drag & drop + resize mechanics (dnd-kit sortable grid)) touch-none on the full-width header hijacks mobile scrolling and invites accidental drags — The drag handle is the entire header row with `touch-none` (line 218; also the invisible 56px strip in naked mode, line 249). On mobile the grid is single-column (page.tsx:351 grid-cols-1), so a header appears every 360px; any scroll gesture that starts on one || FIX: Add a TouchSensor with { delay: 250, tolerance: 8 } (keep PointerSensor distance for mouse), which also lets you drop touch-none in favor of touch-action: manipulation. [src/components/dashboard/widget-container.tsx:218]
- [polish] (Home grid drag & drop + resize mechanics (dnd-kit sortable grid)) isOver highlight morphs the card radius (rounded-2xl over rounded-lg) and draws a white ring-offset halo on tinted backgrounds — The drop-target style adds `ring-2 ring-black ring-offset-2 rounded-2xl` to a card whose base chrome is `rounded-lg` (line 202). Both classes set border-radius; rounded-2xl (16px) wins in Tailwind's output, so the hovered-over card's corners visibly pop from 8 || FIX: Use `rounded-lg ring-offset-0` (or a ring-offset color matching the page background) for the isOver state, and keep the placeholder border at 1px (border-dashed border-gray-400) to avoid the content s [src/components/dashboard/widget-container.tsx:209]
- [low] (Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model)) No concurrency control: widgetPreferences is whole-replace last-write-wins and the uiState read-merge-write is non-transactional — PATCH has no updatedAt/version guard. widgetPreferences is replaced wholesale, so two tabs each debounce-PATCHing different layouts simply race — last write wins (arguably acceptable, but the mount-echo bug in finding 2 widens this window to every page load, n || FIX: Wrap read+merge+write in prisma.$transaction, or use a raw jsonb_set / jsonb concat update (UPDATE ... SET "uiState" = coalesce("uiState",'{}') || $1) so the merge happens atomically in Postgres. [src/app/api/users/preferences/route.ts:91]
- [low] (Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model)) PATCH accepts arbitrary unbounded JSON for widgetPreferences/uiState and unvalidated theme — widgetPreferences and uiState are persisted verbatim with no schema validation or size cap, and theme accepts any string. App Router route handlers do not enforce the old 4MB pages-API body limit, so an authenticated client can store multi-megabyte JSON in its || FIX: Validate widgetPreferences against the UserWidgetPreferences shape (arrays of known widget IDs, sizes in {half,full}), whitelist theme values, and reject bodies over a few KB. [src/app/api/users/preferences/route.ts:61]
- [low] (Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model)) localStorage keys are not user-scoped — one user's layout can be migrated into another user's account on a shared machine — Both caches use fixed keys ('buildsync-widget-preferences' and 'ui-state-cache' in use-ui-state.ts:31) with no userId namespace. When user B logs in on a machine where user A used the app, and B's server widgetPreferences is null (fresh account), the localStor || FIX: Namespace the storage keys by user id (e.g. `buildsync-widget-preferences:${userId}` from the session) and clear/ignore mismatched entries. [src/hooks/use-widget-preferences.ts:148]
- [low] (Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model)) Network-error branch skips the localStorage fallback that the non-ok branch has — When fetch REJECTS (offline / DNS failure) the catch block does nothing, so the user sees the default layout even though their saved layout sits in localStorage — inconsistent with the res.ok===false branch (lines 164-177) which does read localStorage. The 'Al || FIX: Extract the localStorage-fallback into a helper and call it from both the non-ok branch and the catch block; still suppress the persist PATCH after any failed load. [src/hooks/use-widget-preferences.ts:178]
- [low] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) uiState PATCH is a non-atomic read-modify-write — concurrent key writes can be lost — Each uiState PATCH reads the current row, merges in memory, and upserts the whole JSON. Two concurrent PATCHes for different keys (each key has its own independent 600ms debounce timer in use-ui-state.ts schedulePatch) race; the last writer overwrites the firs || FIX: Use a Prisma interactive transaction (or a raw jsonb_set / json merge UPDATE) so the merge happens atomically in the DB. [src/app/api/users/preferences/route.ts:67]
- [low] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) toggleWidget can create duplicate widgetOrder entries (duplicate React keys / dnd ids) — When enabling a widget, toggleWidget appends to widgetOrder without checking it is already there. visibleWidgets and widgetOrder are filtered independently in migratePreferences and can diverge in stored payloads, so an id present in widgetOrder but not visibl || FIX: Guard the append: `newOrder = prev.widgetOrder.includes(widgetId) ? prev.widgetOrder : [...prev.widgetOrder, widgetId]`, and have migratePreferences reconcile visible ⊆ order. [src/hooks/use-widget-preferences.ts:239]
- [low] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) QuickOverviewWidget is dead code kept alive by the barrel export — quick-overview-widget.tsx is exported from the widgets barrel but imported nowhere; 'quick-overview' is not in the WidgetType union, not in AVAILABLE_WIDGETS, and not in REMOVED_WIDGET_IDS, so it can never render or be toggled. || FIX: Delete src/components/dashboard/widgets/quick-overview-widget.tsx and line 3 of index.ts (or, if it is meant to return, add 'quick-overview' to WidgetType + AVAILABLE_WIDGETS). [src/components/dashboard/widgets/index.ts:3]
- [polish] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) Re-enabled widget silently appends to the END of the grid with no feedback — Toggling a widget ON appends it to the bottom of widgetOrder. With the sheet covering the right side of the screen and no scroll-to/highlight, the user gets zero visible confirmation that anything happened unless the grid is short. || FIX: Insert new widgets at defaultOrder position (or top), or scroll the new tile into view with a brief ring highlight after the sheet closes. [src/hooks/use-widget-preferences.ts:239]
- [polish] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) Draft comments 'Coming soon' entry is a toggleable dead-end and the notice can truncate away — The only 'Coming soon' signal is the text-xs description with `truncate`, which clips on the narrow sheet; the toggle is fully enableable and yields a widget whose empty state promises 'Unsent comments will be saved here' although nothing in the app writes dra || FIX: Render a small 'Coming soon' badge next to the title and disable the Switch (or hide the entry), and use draft.taskId in the click target when the feature ships. [src/components/dashboard/customize-widgets-modal.tsx:174]
- [polish] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) Swatches and buttons lack pointer cursor and selected-state semantics; amber/teal check contrast is borderline — Under Tailwind v4 preflight (buttons get cursor:default), the swatch <button>s, the Customize trigger, and 'Reset to defaults' all show an arrow cursor; swatch selection is exposed only as a visual check icon with no aria-pressed, and the white check on the am || FIX: Add cursor-pointer to the swatch buttons (and the shared Button component), set aria-pressed={selected} (or use role=radio/radiogroup), make the whole row toggle via a <label>, and use a dark check or [src/components/dashboard/customize-widgets-modal.tsx:133]
- [polish] (Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts)) Background tint appears only after hydration — white flash on every Home mount for tinted users — The pre-load spinner branch renders outside the tinted container, and useUiState deliberately applies the cached background in a post-paint useEffect, so users with a saved background see white → tint on every navigation to Home. || FIX: Apply the same style={{ backgroundColor: background.bg ?? undefined }} to the !isLoaded branch (useUiState hydrates from cache before widget prefs finish loading, so the value is already correct there [src/app/(dashboard)/home/page.tsx:293]
- [polish] (Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts) Chips flash '0 tasks completed / 0 collaborators' while the fetch is in flight — Before /api/dashboard/ceo resolves, the summary memo returns zeros, so the header confidently renders '0 tasks completed' and '0 collaborators' and then jumps to the real numbers — a misleading transient state rather than a loading treatment. Note the widget g || FIX: Track data===null as a loading state and render a small skeleton pill (or hide the chips) until data arrives; HomeHeader could accept `tasksCompleted?: number | null` and render a shimmer for null. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\(dashboard)\home\page.tsx:171]
- [polish] (Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts) Period dropdown gives no selected-state indication — The open dropdown lists all five periods identically — no checkmark, bold, or highlight on the currently active period. Asana-quality menus always mark the active choice. (Keyboard access itself is fine via Radix DropdownMenu.) || FIX: Use DropdownMenuCheckboxItem or render a Check icon when `id === period` (and optionally font-medium on the active row). [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\home\home-header.tsx:87]
- [low] (Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts) Header date/greeting hardcode en-US locale and never refresh across midnight/hour boundaries — Date string uses a hardcoded 'en-US' locale and the greeting buckets are computed once per render; for a long-lived tab the greeting/date go stale (says 'Good morning' at 3pm after an overnight tab). Hardcoded en-US is at least CONSISTENT with every widget (al || FIX: Centralize locale in one constant/util (fed by user preference later), and if long-lived-tab freshness matters, recompute the greeting on visibilitychange or a coarse interval. [C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\components\home\home-header.tsx:61]
- [low] (QuickCreateTaskModal (Gmail-style composer) + Home page integration) Leaving Assignee empty silently self-assigns, and a picked assignee/project cannot be cleared — The modal sends `assigneeId: null` when no assignee is picked, but the API coerces null to the creator (api/tasks/route.ts:217 `const assigneeId = data.assigneeId ?? userId;`), so an 'unassigned' task is impossible from this composer and the toast just says 'T || FIX: Add a 'No assignee' item at the top of the assignee dropdown; when creating from this modal pass an explicit sentinel the API respects, or at minimum change the empty-assignee toast to 'Task created a [src/components/tasks/quick-create-task-modal.tsx:203]
- [polish] (QuickCreateTaskModal (Gmail-style composer) + Home page integration) Picker dropdowns show 'No teammates/projects available' while the lists are still loading (and on fetch failure) — users/projects start as empty arrays (line 114-115) and the fetch fires on open; if the user clicks the Assignee or Project pill before the responses land — or the fetch fails, which is swallowed by empty catch blocks (lines 144-146, 156-158) — the dropdown as || FIX: Track a loading flag per fetch and render a spinner/'Loading…' row until resolved; on error show 'Couldn't load projects — retry'. [src/components/tasks/quick-create-task-modal.tsx:323]
- [polish] (QuickCreateTaskModal (Gmail-style composer) + Home page integration) Enter in the task-name field does nothing; composer has no keyboard submit or Esc handling and no dialog semantics — The title input has no onKeyDown and the composer is a bare div (no form, no role=dialog, no aria-label, no focus trap, no Esc handler), so Enter does not create the task, Esc does not minimize/close, and screen readers get no dialog announcement. Focus is als || FIX: Add onKeyDown to submit on Enter when title+project are set; add a document-level Esc handler that minimizes (preserving the draft); add role="dialog" aria-label="New task" to the container. [src/components/tasks/quick-create-task-modal.tsx:290]
- [low] (QuickCreateTaskModal (Gmail-style composer) + Home page integration) Two QuickCreateTaskModal instances can stack pixel-for-pixel at bottom-right — DashboardShell mounts its own QuickCreateTaskModal (line 231-235) while home/page.tsx mounts a second one (line 379-382). Both render `fixed bottom-0 right-6 ... z-50 w-[480px]`. If the shell's quick-create is opened (from the global create affordance) while H || FIX: Lift the composer to a single shared instance (e.g. context or the existing custom-event pattern used by openCreateProjectGallery) so Home triggers the shell's modal instead of mounting its own; or of [src/components/layout/dashboard-shell.tsx:231]
---

# ESTADO POR ÁREA

## widget:my-tasks — minor-issues
All wiring is real and correct in shape. GET /api/tasks?myTasks=true -> src\app\api\tasks\route.ts GET (exists, auth via getCurrentUserId + workspace scoping via getUserWorkspaceId, returns an array matching the widget's Task interface superset). Checkbox toggle -> PATCH /api/tasks/[taskId] (src\app\api\tasks\[taskId]\route.ts, exists, auth + verifyTaskAccess, persists completed + completedAt, writes activity log, recalcs goal progress, fires workflow rules; widget refetches after, so the change genuinely persists). Composer -> POST /api/tasks (exists, zod-validated, auto-assigns creator so th
OK: Completion toggle truly persists: PATCH /api/tasks/[taskId] with { completed } is validated (zod), guarded (verifyTaskAc | All three API routes used (GET/POST /api/tasks, PATCH /api/tasks/[taskId]) exist, check auth (401 on no session), and wo | Loading skeleton (3 pulse rows), per-tab empty states with tab-aware copy (empty Overdue reads "No overdue tasks. You're | Task name truncation is done right: parent has flex-1 min-w-0 and the <p> has truncate, so long names ellipsize cleanly  | Composer keyboard model is thoughtful: Enter commits via blur, Escape cancels and clears draft state, focus is auto-set  | Draft due date flows through correctly: DueDatePicker props match its interface (dueDate/onChange/trigger), the ISO stri | TaskDetailModal is wired with onTaskUpdate={fetchTasks} so edits made in the modal (rename, due date, completion) refres | Show more/Show less affordance correctly reveals the tasks past the 5-row cap and reports the hidden count (my-tasks-wid | Row hover feedback is good: hover:bg-gray-50 + cursor-pointer on the row, and the checkbox previews a 50%-opacity checkm

## widget:projects — minor-issues
GET /api/projects (src/app/api/projects/route.ts) exists, checks auth via getCurrentUserId() (401 on missing), and returns an array whose objects contain every field the widget destructures (id, name, color, icon, status, _count.tasks — all confirmed in the Prisma include + schema). However the widget's query params `sort` and `limit` are NOT read by the route (it only reads workspaceId and q), so two of the widget's controls are dead server-side. Create CTA is fully wired: onCreateProject → openCreateProjectGallery() (src/lib/open-create-project.ts) dispatches "buildsync:open-create-project-g
OK: Create-project CTA path is complete end-to-end: all three entry points in the widget (header New button, dashed 'Create  | Every navigation target is real: router.push(`/projects/${project.id}`) resolves to src/app/(dashboard)/projects/[projec | API access control is careful: per-workspace visibility clauses (route.ts:121-153) prevent an OWNER role in one workspac | Truncation is handled correctly at both half and full width: the name cell uses flex-1 min-w-0 + truncate (widget lines  | First-load skeleton exists (3 pulsing rows, lines 112-117), the empty state is actionable with icon + copy + CTA button, | Rows are native <button> elements, so they are keyboard-focusable and Enter/Space-activatable; the sort control and cont

## widget:goals — minor-issues
GoalsWidget fetches GET /api/objectives (src\app\api\objectives\route.ts — exists, auth via getCurrentUserId, workspace + privacy scoping owner/member/team-member; response array shape matches the widget's Objective interface incl. progress/status/period/team) and GET /api/teams/list (src\app\api\teams\list\route.ts — exists, auth'd, returns id/name/_count.objectives which the selector uses). Navigation targets all exist: goal cards push /goals/{id} (src\app\(dashboard)\goals\[objectiveId]\page.tsx), "View all goals" and the header titleHref/menu action push /goals (src\app\(dashboard)\goals\p
OK: Both API calls hit real, existing routes with auth checks and a proper privacy gate (objectives route filters to owner O | Progress bar math is safe: bar width clamps with Math.min(goal.progress, 100) (goals-widget.tsx:280) and the server-side | Every router.push target exists: /goals, /goals/[objectiveId]; the widget header title is a working Link to /goals via t | Goal name truncation works at both half and full width: flex-1 + truncate on the name span (goals-widget.tsx:285) — over | Loading state exists (2 pulsing skeleton rows, lines 226-229), per-tab empty-state copy is differentiated (getEmptyMessa | All clickables are real <button> elements, so keyboard focus/Enter activation works out of the box. | No timezone risk: the widget renders period as a plain string (e.g. 'Q1 FY26') and does no Date parsing at all.

## widget:assigned-tasks — minor-issues
Widget → GET /api/tasks/assigned (src/app/api/tasks/assigned/route.ts — exists, auth via getCurrentUserId, returns array matching the component's AssignedTask shape). Checkbox toggle → PATCH /api/tasks/[taskId] (exists; verifyTaskAccess workspace check; persists completed + completedAt, writes activity, recalcs goals, fires workflow rules, notifies creator). Empty-state CTA + '...' menu 'Assign task' → QuickCreateTaskModal via home/page.tsx setShowQuickComposer — correctly wired. Modal → GET /api/users (exists, auth OK, array shape matches, but silently capped at 20), GET /api/projects (exists
OK: Self-assignment exclusion is correct: /api/tasks/assigned filters creatorId=userId AND assigneeId:{not:userId}; Prisma's | Auth is checked on every traced route: getCurrentUserId 401-guards GET /api/tasks/assigned, GET/PATCH /api/tasks/[taskId | Completion toggle genuinely persists: PATCH sets completed AND completedAt (tasks/[taskId]/route.ts:279-281), logs TASK_ | Task creation from the composer really saves: POST /api/tasks wraps position calc + create in a transaction, and fires n | Loading state exists (3 pulsing skeleton rows, widget lines 208-213) so there is no layout flash, and each tab has disti | Task-name truncation is done correctly (flex-1 min-w-0 + truncate on the <p>), due-date span is flex-shrink-0, and tab c | The composer guards double-submit with the `creating` flag, disables Create until title + project are set, shows a spinn

## widget:people — minor-issues
GET /api/users?limit&filter&includeStats=true&period → src/app/api/users/route.ts exists, checks auth via getCurrentUserId() (401 when absent), returns a plain array whose shape matches the widget's Person interface (id/name/email/image/jobTitle/overdueCount/completedCount/upcomingCount) — wiring is correct and res.ok is checked before parsing. Row click → router.push(`/profile/${person.id}`) and the page src/app/(dashboard)/profile/[userId]/page.tsx exists. Widget title link (set in home/page.tsx getWidgetTitleHref → "/people") targets src/app/(dashboard)/people/page.tsx, which exists. Invite
OK: Fetch effect uses a cancelled flag with cleanup, so rapid tab/filter switching cannot apply stale responses out of order | Response-shape contract holds end to end: route returns a bare array, component checks res.ok before res.json(), and the | Avatar fallback is robust: initials derive from name OR email, split on whitespace/@/. with a final '?' guard, so null n | Loading skeleton (3 pulsing rows) and a real empty state with an actionable 'Invite teammates' CTA both exist (people-wi | Name column uses truncate + flex-1 + min-w-0 and pills use flex-shrink-0, so rows never overflow horizontally at either  | Stats API avoids N+1: one user query plus three Promise.all'd groupBy queries, with null assigneeId rows filtered before | Rows, tabs, and CTAs are real <button> elements (keyboard focusable/activatable), and the filter dropdown is Radix Dropd | Frequent-collaborator ranking preserves shared-project-count order after the second user lookup via an id->user map (rou

## widget:status-updates — minor-issues
GET /api/projects/status-overview?limit=8 (src\app\api\projects\status-overview\route.ts) exists, checks auth via getCurrentUserId, and its response shape exactly matches the widget's ProjectStatusRow[] (id/name/color/status/gate/lastUpdate/daysSinceUpdate). GET /api/projects (src\app\api\projects\route.ts) exists, auth-checked, returns a superset of the picker's ProjectListItem needs. Row clicks and picker selections push to /projects/[projectId]?view=overview[&compose=status] — the page (src\app\(dashboard)\projects\[projectId]\page.tsx:19) reads `view`, project-content.tsx:927 renders the O
OK: API route is real, auth-checked, and efficient server-side: 3 batched queries (projects, DISTINCT ON latest update per p | Deep-link wiring is fully functional end-to-end: ?view=overview&compose=status is consumed by the project page, opens th | Polling is disciplined: 60s interval that pauses via visibilitychange when the tab is hidden, refetches immediately on r | Project picker list is lazy-loaded only when the dropdown opens, avoiding a wasted /api/projects call on every home load | Loading skeleton (shimmer rows) exists; empty state has a clear illustration, copy, and a working CTA to /projects. | Rows are real <button> elements (keyboard focusable/activatable), project name uses truncate inside min-w-0, summary use | Never-updated / stale (>=14d) tiers are visually distinct (amber border/background + STALE badge) and match the API's so | Date badge math in formatRelative has no pluralization or off-by-one errors across its buckets (today / N days / weeks /

## widget:portfolios — broken
Fetches GET /api/portfolios?limit=5 (src\app\api\portfolios\route.ts — route exists, auth-checked via getCurrentUserId with 401, privacy-gated to owner/member/PUBLIC). Wiring is partially wrong: (1) the route ignores the ?limit=5 query entirely (GET() takes no request object), and (2) the response shape does not match the component — the API returns Prisma objects with `_count: { projects }` plus `projects[]` and `stats`, but the widget destructures `projectCount`, which never exists. Links: "View all" -> /portfolios (exists: src\app\(dashboard)\portfolios\page.tsx); row click -> /portfolios/[
OK: API auth and permissions are correct for the list: GET /api/portfolios returns 401 without a session and applies an Asan | Loading state exists: 3 animate-pulse skeleton rows render while fetching (portfolios-widget.tsx:51-56). | Portfolio name truncation is implemented correctly for both half and full width: the name wrapper has min-w-0 flex-1 and | Row hover feedback is present and smooth: hover:bg-gray-50 with transition-colors (line 74). | Rows are real <button> elements, so they are keyboard-focusable and Enter/Space-activatable. | Navigation redundancy matches Asana: widget title is a link to /portfolios (widget-container titleHref, home\page.tsx:18 | Null color is handled with an on-palette fallback (line 83: portfolio.color || '#c9a84c'). | No timezone/date bugs in the widget itself — it renders no dates (the server-side overdue calc in the list API uses serv

## widget:private-notepad — broken
Fetches GET/POST/PUT /api/workspace/notes (src\app\api\workspace\notes\route.ts — exists, auth via getCurrentUserId on all verbs, response shapes match: GET returns an array of notes with id/title/content, POST returns the created note with id), GET /api/users?limit=20 (src\app\api\users\route.ts — exists, auth checked, returns an array the widget maps correctly), and POST /api/ai/assist (src\app\api\ai\assist\route.ts — exists, auth checked, returns { result } which the widget reads; gracefully 503s when ANTHROPIC_API_KEY is unset). The widget has no router.push/Link of its own; the container
OK: Real server-side persistence with correct auth: every verb of /api/workspace/notes checks getCurrentUserId, PUT verifies | XSS hygiene on load: all persisted and localStorage HTML is passed through DOMPurify.sanitize before being injected into | Sensible save strategy on paper: 2s debounced auto-save (lines 215-219), save-on-blur (line 772), localStorage cached be | Loading state exists: a skeleton renders while the note loads (lines 735-744), so there is no flash of an editable-then- | AI assist modal is well-behaved: per-option loading spinner, disabled state while loading or with no text, error message | Rich-text UX details: Enter on an empty list item exits the list (lines 640-685), Tab/Shift+Tab indent/outdent inside li | Mention picker has a real empty state ('No users found') and search filters by name and email (lines 449-452, 896-899).

## widget:draft-comments — minor-issues
GET /api/users/preferences (src\app\api\users\preferences\route.ts) — exists, auth-checked via getCurrentUserId (401 when unauthenticated), returns the full UserPreferences row; widget reads prefs.uiState.draftComments, which matches the response shape (uiState Json? column in prisma\schema.prisma:143). PATCH /api/users/preferences — exists, auth-checked, and its one-level deep-merge of uiState (route.ts:64-89) means the widget's `{ uiState: { draftComments } }` write replaces only that key without wiping sibling keys (home.period, home background, notepad, etc.) — the wiring is correct. route
OK: Graceful degradation is real: skeleton (2 pulse rows, lines 84-89) -> API fetch -> localStorage fallback -> empty state  | API wiring is correct end-to-end: GET/PATCH /api/users/preferences both check auth and the PATCH deep-merge protects sib | Truncation primitives are correct for both half and full width: the clickable area uses min-w-0 flex-1 (line 107), task  | Draft count badge pluralizes correctly ('1 draft' vs 'N drafts', line 80) and only renders when drafts exist. | The Customize modal description is honest about the state of the feature: 'Coming soon · saves unsent comments as drafts | Delete is optimistic and mirrors to both localStorage and the DB (lines 56-68), so the deletion survives reloads on the 

## widget:forms — minor-issues
Widget fetches GET /api/forms?limit=20 (exists, auth via getCurrentUserId, response array matches the widget's FormRow interface incl. projectId/newCount) and lazily GET /api/projects when the picker opens (exists, auth checked, array shape has id/name/color — but massively overfetched). Row click fetches GET /api/forms/[formId]?settings=1 (exists, auth + project edit-role gate; shape matches FullFormRow). Inline FormBuilderDialog saves via POST /api/forms (create — BROKEN for blank description, see finding 1) and PATCH /api/forms/[formId] (works; response includes name/isActive which the widg
OK: Polling lifecycle is well engineered: 30s silent refresh pauses when document.hidden, resumes + immediately refreshes on | Per-row in-flight state: the clicked row is disabled and its FileText icon swaps to a spinner while the full form record | Truncation is handled correctly at both half and full width: the left block has min-w-0 with truncate on both form name  | Optimistic local updates after save: edit updates name/isActive in place, create prepends the new row with correct shape | Projects list is lazy-loaded only when the New form picker opens, avoiding a wasted /api/projects call on every home loa | All consumed API routes check auth: /api/forms GET/POST require getCurrentUserId; /api/forms/[formId] settings/PATCH/DEL | Keyboard access is native: rows and the footer CTA are real <button> elements, and project pickers are Radix DropdownMen | Nice microcopy details: singular/plural on responses count with tabular-nums, and the gold 'N new' pill carries an expla

## widget:mentions — minor-issues
The widget fetches GET /api/mentions?limit=5 which maps to C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\mentions\route.ts — the route exists, checks auth (401 via getCurrentUserId), and returns an array whose shape exactly matches the widget's Mention interface (id, content, taskId, taskName, projectId, createdAt, author{name,image}). The mention filter is genuine: comments are stored with the rich-text editor's markup `<span ... data-user-id="{user.id}">@Name</span>` (src\components\editor\rich-text-editor.tsx:450) and the API filters `content contains data-user-id="<curr
OK: The mention filter is real, not cosmetic: /api/mentions matches the exact span markup the rich-text editor inserts (data | Auth is checked: unauthenticated requests get 401 (route.ts:11-13), and the limit param is safely clamped to 1-100 (rout | All three UI states exist: a 3-row pulse skeleton during load (mentions-widget.tsx:66-71), a proper empty state with ico | Overflow discipline on the comment body is correct at both half and full width: avatar is flex-shrink-0, the text column | No XSS risk: HTML is stripped server-side (stripHtmlTags, route.ts:86-100) and the widget renders content as React text, | Rows are real <button> elements, so they are keyboard-focusable and Enter/Space-activatable (line 84). | Relative timestamps are computed from the UTC ISO string diff, which is timezone-safe (createdAt serialized via toISOStr | Response payload is lean: the API takes only `limit` rows, and the transform (route.ts:62-73) drops author id/email befo

## widget:learning — minor-issues
GET + PATCH /api/users/preferences (C:\Users\Juan Tercero\Dropbox\14. BuildSync\buildsync\src\app\api\users\preferences\route.ts) — route exists, auth-checked via getCurrentUserId() (401 when absent), and the response shape matches what the component reads: the full UserPreferences row includes the uiState Json column (prisma\schema.prisma line 143) from which the widget reads learningDismissed/learningCompleted. PATCH deep-merges uiState one level deep, so the widget's partial writes ({ uiState: { learningCompleted } } / { learningDismissed }) correctly do not clobber other uiState keys (dash
OK: Completed-tutorial toggles persist to the DB (PATCH /api/users/preferences with uiState.learningCompleted) with a localS | Dismissal is persisted server-side too (uiState.learningDismissed), not just localStorage, so it follows the user across | Auth is properly enforced in the API route: getCurrentUserId() returns 401 before any DB access, and the row is keyed by | The completion checkbox button calls e.stopPropagation() (line 207) so toggling complete never accidentally navigates, a | Horizontal overflow is handled correctly: the text button has min-w-0 flex-1 (line 222) and the checkbox/chevron have fl | Incomplete rows have hover:bg-slate-50 with transition-colors, the empty checkbox circle has hover:border-black, and the | All fetch failures are caught and degrade gracefully to localStorage, and malformed localStorage JSON is caught too (lin | No timezone/date logic exists in the widget (durations are static strings), so there is nothing to get wrong there.

## widget:ai-assistant — minor-issues
Four fetches, all wired to real routes: (1) POST /api/ai/assist -> src\app\api\ai\assist\route.ts — exists, auth-checked via getCurrentUserId, returns { result } which matches the widget's data.result destructure; 503 when ANTHROPIC_API_KEY is missing. (2) GET /api/users?limit=20 -> src\app\api\users\route.ts — exists, auth-checked, returns a bare array matching users.forEach. (3) GET /api/projects?limit=20 -> src\app\api\projects\route.ts — exists, auth-checked, returns a bare array matching projects.forEach, BUT the limit param is silently ignored and the payload is heavily over-included. (4
OK: Ask flow round-trip works: POST /api/ai/assist exists, checks auth (route lines 7-10), validates body (14-19), returns { | Loading state is real: spinner + 'Thinking...' replaces suggestions during the request (lines 290-295), and the send but | Past-topics persistence is dual-written (localStorage + PATCH /api/users/preferences uiState.aiPastTopics) with a DB-fir | Past-topics list has a proper empty state ('No past topics yet', line 318-321), correct truncation (flex-1 min-w-0 + tru | Mention picker is solidly built for what it is: backdrop click-to-close (lines 349-352), search filter, results capped a | The widget never crashes on network failure — every fetch has a catch (mention fetch line 109, prefs load line 147, save | Both API list endpoints scope data to the caller's workspace (users route lines 36-42, projects route visibility clauses

## Home grid drag & drop + resize mechanics (dnd-kit sortable grid) — minor-issues
All wiring verified. Layout persists via PATCH /api/users/preferences (src/app/api/users/preferences/route.ts — exists, auth-checked via getCurrentUserId, accepts widgetPreferences JSON pass-through; hook reads data.widgetPreferences which matches the Prisma row returned). Header chips fetch /api/dashboard/ceo (route exists). All titleHref / router.push targets resolve to real pages: /my-tasks, /goals, /portfolios, /people (src/app/(dashboard)/...), /projects/all (src/app/(dashboard)/projects/all/page.tsx), /portal/admin/forms (src/app/(portal)/portal/admin/forms/page.tsx). The only wiring gap
OK: CSS.Translate (not CSS.Transform) is deliberately used to strip dnd-kit's scaleX/scaleY on variable-sized grid items (wi | DragOverlay pattern is used (page.tsx:369-373), so the dragged card is a portal overlay rather than a transformed grid i | PointerSensor activationConstraint { distance: 5 } (page.tsx:123-125) plus e.stopPropagation() on the title Link and men | Keyboard reorder is genuinely wired: {...attributes} on the header provides tabIndex/role, KeyboardSensor + sortableKeyb | Persistence is robust: 500ms debounced PATCH with localStorage offline fallback, plus migratePreferences() scrubs sunset | Custom 250ms cubic-bezier(0.25,1,0.5,1) sortable transition (widget-container.tsx:83-86) gives displaced cards a profess | Page has real loading (spinner while !isLoaded) and error-with-retry states (page.tsx:293-315).

## Home grid layout persistence (use-widget-preferences.ts, use-ui-state.ts, /api/users/preferences, UserPreferences Prisma model) — minor-issues
Wiring is correct: useWidgetPreferences and useUiState both call GET/PATCH /api/users/preferences, which exists at src\app\api\users\preferences\route.ts, checks getCurrentUserId() on both methods, and returns the full UserPreferences row — so the client reads data.widgetPreferences and data.uiState match the Json? columns widgetPreferences/uiState in prisma\schema.prisma (lines 141-143). GET auto-creates the row if missing. All widget titleHref targets on home/page.tsx (/my-tasks, /goals, /portfolios, /projects/all, /people) were verified to exist under src\app\(dashboard). The failures below
OK: Auth is enforced on both GET and PATCH (route.ts:8-11, 36-39); PATCH whitelists scalar fields so arbitrary columns can't | uiState PATCH deep-merges one level server-side (route.ts:64-89), so useUiState's per-key writes ({ uiState: { period: . | migratePreferences scrubs removed widget IDs (PMI tiles) on load and falls back to defaults when visibleWidgets ends up  | Malformed widgetPreferences JSON cannot permanently break home: a non-object or wrong-typed field throws inside the load | Home gates rendering on isLoaded with a spinner (page.tsx:293-299), so the default layout never flashes before server pr | useUiState avoids the React hydration mismatch correctly (default in useState initializer, cache applied in useEffect, u | useUiState's debounced PATCH lives at module level (pendingPatches, use-ui-state.ts:103-121), so unlike the widget hook 

## Customize widgets modal + home background (customize-widgets-modal.tsx, home-background.ts, home/page.tsx, use-widget-preferences.ts, use-ui-state.ts) — minor-issues
All wiring verified real: widget prefs persist via PATCH /api/users/preferences (src/app/api/users/preferences/route.ts — auth via getCurrentUserId with 401, response shape matches: GET returns the UserPreferences row and both consumers read data.widgetPreferences / data.uiState). Background persists under uiState key 'home.background-color' through useUiState → debounced PATCH { uiState: { [key]: value } }, and the route deep-merges uiState one level so the background write does not wipe 'home.period' or draftComments (route.ts:64-89). Header chips fetch /api/dashboard/ceo (route exists). All
OK: Modal reflects live state correctly: it is fully controlled by props — Switch checked comes from preferences.visibleWidg | Modal a11y fundamentals are solid: Sheet is Radix Dialog (focus trap + Esc + overlay click close for free), has SheetTit | Background persistence design is genuinely good: useUiState uses a module-level shared cache + single in-flight fetch so | Reset to defaults DOES reset sizes, not just visibility/order — resetToDefaults recomputes widgetSizes via calculateAuto | All 8 background tints are 50-tier pastels (#fff1f2, #fffbeb, ...) behind white bg-white widget cards and black header t | Preference migration is defensive: migratePreferences scrubs removed PMI widget ids from visible/order/sizes, falls back | No duplicate widget definitions: AVAILABLE_WIDGETS ids are unique with unique defaultOrder values, so the modal list ren

## Home header (greeting + period selector + summary chips) and its data feed: src/components/home/home-header.tsx, src/app/(dashboard)/home/page.tsx, src/app/api/dashboard/ceo/route.ts — broken
page.tsx fetches /api/dashboard/ceo (route exists at src/app/api/dashboard/ceo/route.ts; auth checked via getCurrentUserId -> 401, hierarchy scoping via getEffectiveAccess; response shape matches CockpitData in src/components/cockpit/types.ts field-for-field, including viewerCapabilities typed optional to tolerate old cached payloads). Period persistence goes through useUiState('home.period') -> GET/PATCH /api/users/preferences (route exists) with debounce + localStorage cache — wiring is correct. All widget title hrefs verified to exist: /my-tasks, /goals, /portfolios, /projects/all, /people,
OK: Auth + access control in /api/dashboard/ceo is real: 401 without a user, L1-L3 viewers get membership-scoped projects/ta | Period preference is DB-backed and hydration-safe: useUiState renders the default on first paint to avoid hydration mism | Response shape vs CockpitData type matches exactly; activity items carry completedAt/updatedAt as typed, and completedTa | No mixed Spanish/English in the UI: all widget titles in src/types/dashboard.ts and all widget strings are English; Span | SummaryChip handles pluralization (singular/plural at count===1) and uses tabular-nums to prevent digit-width jitter (ho | Greeting fallback is safe: null/undefined name degrades to bare 'Good morning/afternoon/evening', first name extracted v | All six widget title-link targets and both quick-action router.push targets resolve to real pages under src/app.

## QuickCreateTaskModal (Gmail-style composer) + Home page integration — minor-issues
All wiring targets exist and are authenticated. Submit POSTs to /api/tasks (src/app/api/tasks/route.ts POST) — auth via getCurrentUserId (401 without session), zod createTaskSchema validation, verifyProjectAccess for projectId, position computed inside a prisma.$transaction, activity log written, assignee notification fired best-effort. Payload shape (name, description, assigneeId, projectId, startDate, dueDate as YYYY-MM-DD strings) matches the schema exactly. Assignee picker GETs /api/users (src/app/api/users/route.ts, exists, auth'd, returns a flat array of {id,name,email,image,...} — match
OK: Persistence is real and safe: POST /api/tasks checks auth (route.ts:195-199), validates with zod (line 202), verifies pr | Double-submit guard and loading state are correct: the Create button is disabled while creating and shows a Loader2 spin | Server errors are surfaced: non-OK responses parse the body and toast err.error, with a catch fallback toast for network | Client-side required-field gating is enforced twice: handleCreateTask early-returns without title/project (line 194) and | Title input autofocuses on open (autoFocus, line 291), and the Gmail-style minimize rail preserves the draft (title show | Pickers have proper hover/cursor affordances: dropdown items use cursor-pointer (lines 332, 378), pills have hover borde | Assignee notification is best-effort and non-destructive: notifyTaskAssigned failure is caught and logged without undoin | fmtIso builds the date string from local Y/M/D components (lines 188-191), correctly avoiding the classic toISOString() 
